---
id: TASK-2026-08-28-main-040
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-040
source_path: backlog/2026-08-28.md
kind: generic
wbs: exempt
wbs_exempt_reason: WBS-4.3.5/4.5.8 이 남긴 결함 보수 — 새 leaf 가 아니라 이미 done 인 leaf 의 배선 누락
---

# TASK-2026-08-28-main-040 — 판정 대기열 순위 편입 (Q2·Q6·Q7·Q8)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/web/dashboard.py`
  - `src/agentic_service_desk/web/templates/index.html`
  - `tests/test_q1_dashboard.py`

- Description: 티켓 없는 판정 대기열이 next_up 에 오르지 못해 §8.2 의 '영원히 밀리는' 문제가 그대로 남아 있던 것을 고친다. 순위의 입력을 티켓에서 '대기 중인 것'으로 일반화한다.
- Completion criteria: Q2·Q6·Q7·Q8 이 방치 비용 × 경과 시간 순위에 오른다
- Completion criteria: 판정은 대기열당 한 줄만 오르고 나머지는 건수로 표시된다
- Completion criteria: FR-59 가 순위에도 걸린다 — 켜지지 않은 단계의 판정은 빠진다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 22:06` 기준 티켓 없는 판정 대기열이 next_up 에 오르지 못해 §8.2 의 '영원히 밀리는' 문제가 그대로 남아 있던 것을 고친다.
- Next session starting point:
- Remaining risks: 판정이 대기열당 한 줄이라 같은 대기열에 아주 오래된 건이 여럿 쌓여도 순위에는 하나만 보인다 — 건수는 backlog 로 말하지만 두 번째로 오래된 건의 나이는 목록을 열어야 안다.

## ✅ Outcome

- Result: WorkItem 을 티켓 전용에서 '순위에 오르는 것'으로 일반화했다 — queue·ref·href·label·age_hours 를 들고 ticket 은 있으면 붙는다. 태우려고 티켓을 발행하지 않은 것이 핵심 판단이다: 발행하면 §6.4.4 가 갈라 놓은 작업과 판정을 다시 섞어 판정 하나하나가 Q1 에도 뜬다.
- Result: 판정은 대기열당 한 줄만 올린다 — 판정 대기열은 목록 화면 하나를 가리키므로 여럿 올리면 같은 곳으로 가는 줄이 다섯 자리를 채우고, 각자 다른 화면을 가진 작업 항목이 그 뒤로 밀려난다. 오르는 것은 가장 오래된 건이고 나머지는 backlog 수로 말한다.
- Result: 대기열 정의를 dashboard 에서 다시 쓰지 않고 소유 모듈의 같은 함수(draft_store.pending · tracking.awaiting_confirmation · promotion.awaiting_decision · knowledge_gaps)를 부른다 — 따로 질의하면 언젠가 어긋나 순위에 뜬 것이 대기열에 없게 된다.
- Result: 경과 시간의 기준점을 대기열별로 정했다: Q2 는 초안 created_at, Q6·Q8 은 qna_item.closed_at, Q7 은 티켓의 state_at(자동 종결 시각)이다. Q7 에 opened_at 을 쓰면 파이프라인이 오래 걸린 건이 더 밀린 것처럼 보인다.
- Verification: uv run pytest -q — 727 passed (신규 7). 라이브 렌더 확인: S3 대시보드 '다음에 볼 것' 에 Q8 항목이 40일·점수 960 으로 질문 원문과 함께 떴다.
- Follow-up: Q5 는 티켓(correction)으로 오르고 건수는 correction.pending 으로 세어 두 출처가 다르다 — 어긋날 여지가 남아 있다.
