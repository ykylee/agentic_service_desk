---
id: TASK-2026-09-03-main-003
status: done
created_at: 2026-09-03
source_anchor: generic-task-2026-09-03-main-003
source_path: backlog/2026-09-03.md
kind: generic
wbs: M-005/WBS-5.7.1
---

# TASK-2026-09-03-main-003 — WBS-5.7.1 지식에 묻기 — 운영자 질의 콘솔

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-03
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `docs/REQUIREMENTS.md`
  - `src/agentic_service_desk/web/app.py`
  - `src/agentic_service_desk/web/templates/ask.html`

- Description: 지식베이스에 물어볼 수 있는 사람이 아무도 없었다. 답변 파이프라인 1~3단계를 그대로 태워 보여 주되 아무것도 저장하지 않는다
- Completion criteria: 질문 하나에 근거·강도·경계·멈춘 이유가 나온다
- Completion criteria: 답변 이력·초안·티켓이 생기지 않고 지식베이스도 그대로다
- Completion criteria: 근거가 없으면 답을 만들지 않고 그 사실을 화면이 말한다 (FR-18)

## 🛠️ Implementation / Content

- Progress: **§8.1 이 금지가 아니라 근거였다.** 그 절은 대시보드가 "조회 화면이 아니라 작업대"라고 못 박았지만 경계한 것은 **차트 구경 화면이 되는 것**이다. 같은 절이 이렇게도 적었다 — *"대시보드는 사람이 접근하는 유일한 창구이므로, 여기서 할 수 없는 일은 아무도 할 수 없는 일이 된다."* 지식베이스에 물어볼 수 있는 사람이 아무도 없었다: 답변 파이프라인은 모 시스템에서 온 질문에만 걸린다. 커버리지는 숫자로만 보이고 그 숫자가 무엇을 뜻하는지 볼 수 없었다.
- Progress: **라이브에서 고리가 닫히는 것을 봤다.** "대시보드 API 가 500 을 낼 때 어디부터 보나요"를 물으니 최상위 근거(점수 17.0, `keyword · vocabulary`)가 **한 시간 전 승격 경로 A 로 만든 항목**(`k-457c5d0f8f1a`)이었다. 운영자가 메신저 문의를 등록한 것이 지식이 되어 다음 질문에 답했고, **표현 사전 다리까지 걸렸다** — QnA 유래 항목이라 그 질문의 표현이 색인에 들어간 것이다 (§2.5.4).
- Progress: **두 원천이 한 답에서 합쳐졌다.** 진술 4개 중 둘은 그 QnA 항목에서, 하나는 소스 유래 항목(`k-de4ca2c0ef24` — sync def 가 coroutine 을 await 없이 호출해 500 이 나는 함정)에서, 마지막 하나는 `추론` 으로 표시됐다. **소스코드 지식과 QnA 지식이 실제로 섞여 답이 된 첫 사례다** (D2 가 두 원천을 요구한 이유).
- Progress: **동기로 답한다.** 40~60초가 걸리지만 이것은 대기열이 아니라 도구다 — 배치로 미루면 확인하러 다시 와야 하고, 그 순간 대기열이 하나 더 느는 것과 같아진다(§8.6 이 경계한 바로 그것이다). 모델이 없으면 3단계에서 멈추고 조회 결과만 보여 준다: "지식베이스가 이만큼은 갖고 있다"도 답이다.
- Next session starting point: (완료)
- Remaining risks: **이 화면이 대기열에서 눈을 끌 수 있다.** §8.1 이 경계한 것이 정확히 그것이다 — 지금은 nav 끝에 두고 "묻는 자리이지 답하는 자리가 아니다"를 화면이 말하지만, 운영자가 여기만 쓰게 되는지는 써 봐야 안다.
- Remaining risks: **비용이 질문마다 든다.** 한 번 물을 때마다 LLM 호출이고 40~60초다. 잠금도 상한도 없다 — 1인 운영 전제라 두지 않았다.
- Remaining risks: **여기서 나온 답은 검수를 지나지 않는다.** 저장하지 않으므로 지표는 오염되지 않지만, **운영자가 이 답을 그대로 사람에게 전달하면** 검수 체계 밖으로 나가는 셈이다. 화면이 그 사실을 적었을 뿐 막지는 않는다.

## ✅ Outcome

- Result: FR-60 을 §8.1 근거로 세우고 왜 그 절이 근거가 되는지를 요구사항 표 아래 주로 남겼다.
- Result: **저장하지 않는 것이 요구의 절반이다.** `AnswerPipeline` 은 그 자체로 아무것도 쓰지 않는다 — 초안·티켓·답변 이력·게재는 전부 유입 처리가 이어 붙이는 것이라, 그 배선을 붙이지 않는 것으로 충분했다. 검수를 지나지 않은 산출이 이력에 남으면 지표가 오염되고 게재 출구가 하나라는 규약(NFR-3)이 흐려진다.
- Result: **단계를 보지 않는다.** 지식베이스는 S0 부터 있고 이 화면은 아무것도 내보내지 않는다 — FR-59 의 점증은 *내보내는* 기능의 대기열을 다룬다.
- Verification: uv run pytest -q 1156 passed (신규 7) · 라이브: 무관 질문은 조회 0건으로 멈추고, 실제 질문은 근거 5건 위에서 진술 4개(약한 지점 1)·경계 2건을 냈다
- Follow-up:
