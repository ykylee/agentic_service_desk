---
id: TASK-2026-08-28-main-019
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-019
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.2.6
---

# TASK-2026-08-28-main-019 — WBS-4.2.6 Lint

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/knowledge/lint.py`

- Description: 지식베이스 정합성을 주기 검사한다 — 모순 미해결 · stale · 고아 · 끊어진 링크 · 참조 부재. 결과는 Q4·Q5 대기열로 이어지며 아무것도 삭제하지 않는다 (FR-7·8, ADR-002 결정 4).
- Completion criteria: 다섯 검사가 돌고 결과가 대기열로 이어진다 (FR-7)
- Completion criteria: 출처 커밋이 낡으면 stale 로 표시한다 — 삭제하지 않는다 (FR-8)
- Completion criteria: 깨진 링크는 삭제하지 않고 Q5 로 올린다 (ADR-002 결정 4)
- Completion criteria: 주기 실행이므로 같은 소견이 대기열에 쌓이지 않는다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 14:15` 기준 지식베이스 정합성을 주기 검사한다 — 모순 미해결 · stale · 고아 · 끊어진 링크 · 참조 부재. 결과는 Q4·Q5 대기열로 이어지며 아무것도 삭제하지 않는다 (FR-7·8, ADR-002 결정 4).
- Next session starting point:
- Remaining risks: ingest 가 lint 보다 먼저 돌므로 소스 변경으로 인한 stale 은 대개 ingest 가 먼저 갱신해 없앤다. stale 이 실제로 뜨는 것은 **에이전트가 연결을 못 알아본 항목과 사람이 고쳐 덮이지 않는 항목**이다 — 의도한 분업이지만, 그래서 stale 건수만으로는 지식 노후를 재기 어렵다
- Remaining risks: 고아 검사가 자가 치유라 '고아였다'는 사실이 index 재생성 수로만 남고 이력에는 남지 않는다

## ✅ Outcome

- Result: 다섯 검사를 넣었다. 모순 미해결은 Q4(이미 티켓 있음), stale·끊어진 링크·참조 부재는 Q5(source=correction) 티켓, 고아는 index.md 재생성으로 그 자리에서 고친다 — **사람이 판단할 것이 없는 고장은 대기열에 넣지 않는다.** 대기열은 판단이 필요한 것만 담아야 소화된다
- Result: 고아를 'OKF 번들 목록(index.md)에 등재되지 않은 항목' 으로 정의했다. 소비자는 그 목록으로 번들을 훑으므로 등재되지 않으면 파일이 있어도 없는 것과 같다 — 그동안 index.md 가 만들어지기만 하고 비어 있었다
- Result: **stale 은 가장 최근에 반영한 출처 커밋 하나만 본다.** 라이브에서 잡은 설계 결함이다 — 출처는 갱신마다 쌓이는데(ADR-003) 쌓인 것을 전부 보면 오래된 커밋 이후로는 당연히 경로가 바뀌었으므로 **갱신된 항목이 영원히 stale 이 된다.** 그러면 stale 이 아무것도 가리키지 않고 Q5 가 거짓 소견으로 찬다
- Result: linked 는 커밋으로, periodic 은 시간으로 판정한다. 시간 기준점은 그 파일의 마지막 지식 커밋 날짜다 — 항목에 '마지막 확인일' 필드를 더하지 않았다. git 이 이미 답을 알고 있고, 필드를 더하면 두 곳이 어긋날 자리가 생긴다
- Result: 아무것도 삭제하지 않는다. 깨진 링크를 지우면 그 답변이 무엇에 근거했는지가 함께 사라진다 — 답은 이미 사람에게 나갔는데 근거만 없어지는 것이다
- Result: 버그 셋을 더 잡았다: ① 커밋이 하나도 없는 지식 저장소에서 last_commit_date 가 터져 **Lint 가 첫 주기에 통째로 죽었다** ② index.md 가 다시 써졌는데 커밋되지 않아 **작업 트리가 계속 더러운 채로 남았다**(그 변경이 다음 ingest 커밋에 섞인다) ③ 미러가 없을 때 모든 항목을 참조 부재로 올릴 뻔했다 — 없는 것과 사라진 것은 다르다
- Verification: uv run pytest -q — 283 passed (기존 259 + 신규 24). 라이브 S0 전 구간 3주기 실행(pi + MiniMax-M3): 수집 → 필터 → ingest → lint. 소스를 바꾸자 ingest 가 항목을 갱신했고 lint 는 거짓 stale 을 붙이지 않았다. 3주기째 조용하고 지식 저장소 git status 는 깨끗하다.
- Follow-up: WBS-4.2.7 대시보드 최소 — Q4 는 contradiction.list_open(), Q8·Q5 는 lint.list_open() 이 자료다. 지식베이스 현황(항목 수·출처 구성·stale 비율·미해결 모순)도 이 둘로 만든다
- Follow-up: 4.2 완료 조건은 'Lint 가 안정' 이다. LintReport.clean 이 그 판정(모순 0 · 소견 0)이며, 실데이터로 안정되는지는 실제 모 시스템 저장소를 붙여 봐야 안다
