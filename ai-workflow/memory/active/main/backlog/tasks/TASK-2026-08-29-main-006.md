---
id: TASK-2026-08-29-main-006
status: done
created_at: 2026-08-29
source_anchor: generic-task-2026-08-29-main-006
source_path: backlog/2026-08-29.md
kind: generic
wbs: M-004/WBS-4.8.2
---

# TASK-2026-08-29-main-006 — WBS-4.8.2 알림 웹훅

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-29
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/operations/alert.py`
  - `src/agentic_service_desk/operations/schema.py`
  - `src/agentic_service_desk/operations/migrations.py`
  - `src/agentic_service_desk/config.py`
  - `src/agentic_service_desk/web/app.py`
  - `src/agentic_service_desk/web/templates/index.html`
  - `src/agentic_service_desk/worker/runner.py`
  - `tests/test_alert.py`

- Description: 위험 대기열(Q4·Q5)의 방치와 국면 역행을 웹훅 한 곳으로 보낸다. 설정하지 않으면 대시보드 배너로만 알린다 (ADR-007 결정 2, O28).
- Completion criteria: Q4·Q5 가 임계 시간 넘게 밀리면 웹훅이 한 번 간다 — 같은 상황으로 도배하지 않는다
- Completion criteria: 국면 자동 후퇴가 웹훅으로 나간다
- Completion criteria: 웹훅이 없으면 대시보드 배너로 같은 것을 알린다
- Completion criteria: 알림에 질문 원문도 지식 본문도 싣지 않는다 — 세는 것만 보낸다

## 🛠️ Implementation / Content

- Progress: `2026-08-29 23:39` 기준 위험 대기열(Q4·Q5)의 방치와 국면 역행을 웹훅 한 곳으로 보낸다. 설정하지 않으면 대시보드 배너로만 알린다 (ADR-007 결정 2, O28).
- Next session starting point: WBS-4.8.3 보존 만료 (FR-51, PO-4) — M-004 의 마지막 leaf
- Remaining risks: payload 는 {"text": ...} 한 형식뿐이다 — 실제 사내 메신저에 붙여 봐야 안다 (형식 설정을 두지 않은 것은 표면을 하나로 제한한 ADR-007 의 결정).
- Remaining risks: 알림 임계(3일)와 후퇴 알림 기간(7일)은 O53 로 남은 값이다.

## ✅ Outcome

- Result: 알림은 세는 것만 보낸다 — 채널이 어디에 보관되는지 통제할 수 없어 원문을 싣지 않는다. Q4·Q5 는 건수가 아니라 시간으로 재고, 지문(가장 오래 밀린 것)이 같으면 다시 보내지 않는다.
- Verification: uv run pytest -q — 981 passed (신규 18). 라이브: v11 DB 에 asd migrate 로 계단 012 적용(선언 대조 통과) → 실제 HTTP 수신기를 띄우고 워커 _notify 실행, 위험 대기열과 국면 후퇴 두 건이 도착(본문에 모순 제목·본문 없음, 건수와 경과일만) → 같은 주기 재실행은 무발신 → 수신기를 죽이고 새 상황을 만들자 '알림 실패' 후 alert_sent 에 적히지 않았고, 수신기를 되살리자 다음 주기가 새 지문으로 발신 → 대시보드 배너에 같은 경고 두 줄, 후퇴 시각을 9일 전으로 돌리자 후퇴 배너만 내려갔다.
- Follow-up:
