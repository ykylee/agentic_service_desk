---
id: TASK-2026-08-28-main-022
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-022
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.3.2
---

# TASK-2026-08-28-main-022 — WBS-4.3.2 종결 기록

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/operations/resolution.py`

- Description: 티켓 종결 기록을 지식 항목의 초안 형식으로 받는다. 필수 4필드를 갖추고, 무효화 조건은 초안에서 비워 두어 사람이 채워야 승인이 성립하게 한다 (FR-13·14, §6.5·5.6.4).
- Completion criteria: 일반화된 질문·답·근거·무효화 조건 넷 중 하나라도 비면 종결되지 않는다 (FR-13)
- Completion criteria: 무효화 조건이 초안에서 비어 있고 기본값이 채워지지 않는다 (§5.6.4 강제 입력 지점)
- Completion criteria: 연결형·주기형 둘 다 없으면 승격 대기에 남는다 (FR-14)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 14:59` 기준 티켓 종결 기록을 지식 항목의 초안 형식으로 받는다. 필수 4필드를 갖추고, 무효화 조건은 초안에서 비워 두어 사람이 채워야 승인이 성립하게 한다 (FR-13·14, §6.5·5.6.4).
- Next session starting point:
- Remaining risks: 스키마 변경에 이행 경로가 없다. CREATE TABLE IF NOT EXISTS 는 기존 표에 열을 더하지 않으므로, 옛 DB 로 새 코드를 돌리면 'table ticket has no column named state_at' 로 터진다. 실제로 재현해 확인했다 — 조용히 틀리지는 않고 크게 실패하지만, 배포가 생기기 전에 정해야 한다. 이번 세션에서만 ticket 과 ticket_resolution 두 표가 바뀌었다
- Remaining risks: 모순·Lint 티켓의 종결 기록이 승격 대상이 아니라는 것을 코드 주석으로만 구분한다. 승격 경로(4.3.4)가 출처를 보고 걸러야 하며, 안 걸리면 '판정: kept_human' 같은 것이 지식 항목이 된다

## ✅ Outcome

- Result: draft() 가 무효화 조건 인자를 아예 받지 않는다. 인자에 없는 것이 요점이다 — 있으면 언젠가 누군가 기본값을 넘기고 그 순간 강제 입력 지점이 사라진다(§5.6.4). 시험이 함수 시그니처를 직접 검사한다
- Result: 에이전트는 후보(invalidation_candidates)를 제시할 수 있지만 그것이 값이 되지는 않는다. 선택 자체가 사람의 행위이고, 그 답을 쓰려면 초안을 읽을 수밖에 없으므로 검증이 부산물로 실질이 된다
- Result: ticket_resolution.invalidation 을 NULL 허용으로 바꿨다. NOT NULL 이면 초안 자체를 저장할 수 없어 '비워 둔 칸' 이라는 장치가 성립하지 않는다 — 비어 있는 것이 초안의 정상 상태다
- Result: 근거에 종류(code/commit/config/person)를 뒀다. person 이 있는 것이 중요하다 — 담당자 확인이 근거인 지식은 코드에 묶이지 않아 stale 자동 판정이 안 되고, 그것이 §6.5.3 이 무효화 조건을 필수로 만든 바로 그 경우다. code_traceable 로 그 위험을 셀 수 있다
- Result: 모순·Lint 티켓의 자리만 채운 한 줄을 실제 종결 기록으로 바꿨다. 강제 입력 지점을 이들에게 다시 묻지는 않는다 — 그 장치의 목적은 사람이 초안을 읽지 않고 승인하는 것을 막는 것인데, 모순 판정은 양쪽을 읽어야만 누를 수 있는 버튼이라 이미 달성됐다. 무효화 조건은 대상 항목에 묶는다
- Verification: uv run pytest -q — 346 passed (기존 326 + 신규 20). 라이브 강제 입력 지점 완주: 기록 없이 닫기 거부 → 에이전트 초안(무효화 조건 None, 후보 1개, 승격불가) → 초안만으로 닫기 거부 → 대기 목록에 뜸 → 사람이 채움(승격가능) → 종결.
- Follow-up: WBS-4.3.3 질문 직접 등록 — 두 칸 입력 + 초안 자동 생성 (FR-10·11, ADR-007). 초안을 만드는 것이 거기다. resolution.draft 가 그 착지점이고, 무효화 조건을 비워 두는 규칙이 이미 서 있다
- Follow-up: WBS-4.3.4 승격 경로 A — resolution.promotable 이 자격 판정이고, Ground 에서 Provenance 로의 변환이 필요하다. person 근거는 커밋이 없어 Provenance(qna=...) 로 가야 한다
