---
id: TASK-2026-08-29-main-002
status: done
created_at: 2026-08-29
source_anchor: generic-task-2026-08-29-main-002
source_path: backlog/2026-08-29.md
kind: generic
wbs: M-004/WBS-4.7.2
---

# TASK-2026-08-29-main-002 — WBS-4.7.2 칼럼

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-29
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/content/production.py`
  - `src/agentic_service_desk/content/qna_stats.py`
  - `src/agentic_service_desk/content/review.py`
  - `src/agentic_service_desk/content/store.py`
  - `src/agentic_service_desk/content/publication.py`
  - `src/agentic_service_desk/operations/schema.py`
  - `src/agentic_service_desk/operations/migrations.py`
  - `src/agentic_service_desk/worker/runner.py`
  - `src/agentic_service_desk/web/app.py`
  - `tests/test_column.py`

- Description: 칼럼을 해설·권고까지만 낸다. 관찰을 함께 밝히고, P6·P7 의미 판정이 어디를 볼지 지목한다 (FR-40·41, §7.6).
- Completion criteria: both 읽개가 지식베이스와 관찰을 함께 준다 — 반쪽으로 돌지 않는다
- Completion criteria: 권고가 관찰을 함께 밝히고, 그 관찰이 초안에 박혀 검수가 대조할 수 있다
- Completion criteria: P6·P7 과 관찰 없는 권고를 의미 판정이 지목한다 — 판정이 아니라 소견이다

## 🛠️ Implementation / Content

- Progress: `2026-08-29 22:12` 기준 칼럼을 해설·권고까지만 낸다. 관찰을 함께 밝히고, P6·P7 의미 판정이 어디를 볼지 지목한다 (FR-40·41, §7.6).
- Next session starting point:
- Remaining risks: 의미 판정이 모델의 자기 신고에 기댄 곳이 하나 있다 — 어느 관찰을 밝혔는지(cited)는 검증하지 않는다. 잘못 신고하면 나가는 근거 목록이 어긋나지만, 본문과 목록을 사람이 함께 보므로 Q3 에서 드러난다.
- Remaining risks: 칼럼 반려 사유 목록이 P1~P8 전부다. §7.6.4 는 P1~P5 가 사실 진술을 전제한다고 하지만 칼럼의 해설은 사실 진술이라 그대로 쓰인다 — 다만 검수자에게 여덟 선택지가 보이는 것이 최선인지는 실제 반려 분포를 봐야 안다.
- Remaining risks: 관찰의 기간이 트리거 주기(30일)에 묶여 있다. 주기를 바꾸면 관찰 창도 함께 바뀌는데, 둘은 다른 이유로 정해질 수 있는 값이다.
- Remaining risks: O52 가 남아 있다 — 의견을 금지하고 나면 해설 위주 칼럼이 가이드의 서사형 판본에 가깝다. 두 타입을 모두 유지할 값이 있는지는 실제 발행을 해 보고 판단한다.

## ✅ Outcome

- Result: 읽는 것이 둘인 이유는 쓸 수 있는 것이 둘이기 때문이다. 해설은 지식베이스에, 권고는 관찰에 기댄다(§7.6.1) — 그래서 both 를 반쪽으로 돌리면 관찰 없는 권고가 나가고 그것은 그 순간 의견이 된다(§7.6.2).
- Result: 저장소 히스토리를 별도 읽개로 두지 않았다. §2.2.1 이 커밋 메시지를 원천에 넣은 이유가 '왜 그렇게 바뀌었는가'였고 ingest 가 그것을 항목으로 만든다 — 원문을 따로 꺼내면 ingest 를 우회한 글이 발행물에 실리고 검수 화면이 대조할 수 없는 근거가 된다. 라이브에서 실제로 그 배경 항목(k-3)이 해설의 뼈대가 됐다.
- Result: 관찰을 초안에 박았다 (content_draft.observations, ADR-010 계단 009). 발행물은 회수할 수 없는데 지금 다시 세면 숫자가 달라져 검수가 대조할 수 없다 — 답변의 근거 버전 고정(D20)과 같은 이유다. grounding 과 다른 열에 둔 것은 저쪽에 stale 판정과 지식베이스 커밋 고정이 걸려 있기 때문이다.
- Result: 검수에서만 근거와 관찰을 합친다(sources_of). 합치지 않으면 '지난 30일 동안 4건'의 4 가 근거 원문에 없는 수치가 되어 **P1 이 FR-41 을 지킨 바로 그 문장을 지목한다**.
- Result: 의미 판정은 배치에서 돌고 화면에서 돌지 않는다. 화면이 열릴 때마다 모델을 부르면 같은 초안이 볼 때마다 다른 소견을 내고 검수자가 곧 그것을 믿지 않게 된다. agent_findings 의 NULL 은 '아직 안 봤다'이고 [] 는 '봤는데 없다'다 — 구분하지 않으면 판정이 돌지 않은 초안이 통과한 것처럼 보인다(§5.6.1).
- Result: 라이브 결함: 본 관찰을 전부 근거 목록에 실었더니 **글이 다루지도 않은 관찰이 근거로 붙었다.** 검수는 전부를 봐야 지어낸 관찰을 가려낼 수 있지만 나가는 글에는 쓴 것만 실려야 한다 — 모델이 밝힌 번호(observations)를 받아 cited 로 표시하고, 화면은 전부를 '본문이 밝힘/보기만 함'으로 갈라 보여 준다.
- Verification: uv run pytest -q — 883 passed (신규 22, tests/test_column.py). ADR-010 계단 009 를 v8 DB 에 적용하고 선언 대조를 통과시켰다. 라이브(pi 0.84.1 + MiniMax-M3): 기간 안 관찰 2건 + 기간 밖 1건으로 칼럼을 만들었고, 두 지식 항목을 이어 쓴 해설과 관찰을 밝힌 문단이 나왔다. 정상 초안에는 의미 판정 소견 0건, 의견을 섞은 초안에서는 P6('훨씬 낫습니다') · P7('다들 선호합니다' — 2건으로 '다들') · 관찰 없는 권고('예외 신청을 자제해 주십시오') 셋을 인용과 함께 지목했다. P8 은 기계가 먼저 잡았다. 근거 목록 결함을 잡아 고치고 재검증했다. /status · /queues/Q3 상세에서 관찰 표시를 화면으로 확인했다.
- Follow-up: WBS-4.7.3 뉴스레터 — 같은 both 읽개를 쓰되 기간 내 변경·발행 요약이 주 입력이다 (§7.2). 발송하지 않고 게시한다
