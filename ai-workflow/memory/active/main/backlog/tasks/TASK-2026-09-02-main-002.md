---
id: TASK-2026-09-02-main-002
status: done
created_at: 2026-09-02
source_anchor: generic-task-2026-09-02-main-002
source_path: backlog/2026-09-02.md
kind: generic
wbs: exempt
wbs_exempt_reason: 실데이터 검증에서 드러난 결함 수정. 신규 기능이 아니라 기존 4.x 산출물(조회·파이프라인)의 정확성 보강이라 WBS leaf 를 새로 세우지 않는다
---

# TASK-2026-09-02-main-002 — 답변 경로를 실지식베이스로 태운다 — 조회 오탐부터

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-02
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/knowledge/search.py`
  - `src/agentic_service_desk/pipeline/answer.py`

- Description: 소스 쪽만 실데이터로 검증됐다. 답변 파이프라인을 실지식베이스·실모델로 끝까지 태우고, 무관한 질문이 근거로 걸리는 오탐(FR-18 의 문을 미는 것)을 좁힌다
- Completion criteria: 무관한 질의가 근거를 얻지 못한다 — 실지식베이스(185항목)로 재현과 수정 후를 함께 보인다
- Completion criteria: 정상 질의는 여전히 근거를 얻는다 — 수정이 너무 넓게 잡지 않았음을 함께 보인다
- Completion criteria: 답변 파이프라인을 실지식베이스·실모델로 끝까지 태운다 (답이 나오는 질문과 티켓으로 가는 질문 둘)

## 🛠️ Implementation / Content

- Progress: `2026-09-02 17:0x` 재현. 기록된 "김치찌개 끓이는 법 점수 1.00" 은 **더는 재현되지 않는다** — kit 출처 115개를 지운 뒤라 근거 0건으로 티켓에 간다. 대신 다른 모양의 오탐이 살아 있다: `_matches` 의 부분 일치가 **양방향**이라 문서 토큰 "테스트"가 질의 토큰 "백테스트"에 걸리고, 그것이 제목이면 `TITLE_WEIGHT` 3.0 을 받는다. "백테스트 결과는 어떻게 보나요"가 `rotate_keys 테스트 시간 폭탄 fixture` 에 3.00 으로 걸렸다. 조회에는 점수 하한이 없고(`if not hits` 하나뿐) 재랭킹은 **일부러 버리지 않으므로**, 이 한 건이 그대로 생성 단계로 간다.
- Progress: **접두 일치로 좁혔다.** 조사·어미는 **뒤에** 붙으므로 어간이 앞에 온다는 것만 가정하면 넘으려던 변형("결재/결재를")은 그대로 넘는다. 실측(185항목·질의 7개)에서 사라진 쌍은 **셋뿐**이고 전부 사고다 — ("백테스트","테스트") · ("취소되지","되지") · ("한도는","도는"). 정상 질의의 최상위는 점수까지 그대로였다: "주문 체결이 안 되는데요" 6.0 · "전략 시그널은 어떻게 만들어지나요" 4.0 · "리스크 한도는 어떻게 정해지나요" 3.0.
- Progress: **낱말 하나만 맞은 것을 버리는 안은 재 보고 버렸다.** 표현 사전의 `MIN_VOCAB_MATCHES` 와 같은 규칙인데, 키워드 다리에 얹으면 **정상 질의가 죽는다** — 단일 낱말 질의("가드")가 근거 0건이 되고, "리스크 한도는…"의 정답(`결재 한도 결정 규칙` 3.0)이 관계없는 2.0 항목에 밀린다. 키워드 다리는 **식별자 하나로 묻는 것**을 받아야 하는 자리라 표현 사전과 전제가 다르다. 짐작으로 정했으면 반대로 갔을 결정이다.
- Progress: **라이브 검증 — 실지식베이스(185항목) + 실모델로 네 질문을 태웠다.** (1) "김치찌개 끓이는 법" → 조회 0건, **멈춤(근거 없음)** 0.1초. (2) "오늘 점심 뭐 먹지" → 조회는 3건이 걸렸으나(오탐 잔존) **생성 단계가 "질문의 핵심이 근거 밖이다"로 멈췄다** 16.0초. (3) "주문이 체결됐는지 어떻게 확인하나요" → 근거 5건·진술 7개(약한 지점 1)·경계 3건, 41.5초. (4) "가드가 무엇을 막나요" → 근거 5건·진술 9개(약한 지점 3)·경계 3건, 58.2초. 게재(5단계)는 부르지 않아 모 시스템에 나가지 않았다.
- Progress: **문이 하나가 아니었다 — 이것이 이번 검증의 실소득이다.** handoff 가 걱정한 것은 "약한 오탐이 FR-18 의 문을 밀고 들어온다"였는데, 실제로는 **생성 단계에 두 번째 문**이 있다(`핵심을 모른다`). 조회 오탐이 남아도 엉뚱한 답이 만들어지지는 않는다 — 다만 **LLM 호출 한 번(16초)을 치르고 멈춘다.** 조회를 좁히는 값어치는 오답 방지가 아니라 그 비용이다.
- Next session starting point: (완료)
- Remaining risks: **흔한 낱말 하나로 걸리는 오탐은 남아 있다** — "오늘 점심 뭐 먹지"가 `doc-headers-update … 오늘 날짜를 찍지 않는다` 에 제목 가중치 3.0 으로 걸린다. 접두 일치로는 못 막는다(정확히 맞은 낱말이다). 낱말의 흔함을 재는 장치(IDF 류)가 필요한데, **언어 중립 원칙**(스테머·형태소 분석기 없음)과 어긋나지 않는 형태인지부터 정해야 한다.
- Remaining risks: **조회에는 여전히 점수 하한이 없다.** `find` 의 `if not hits` 하나가 FR-18 의 문이고, 재랭킹은 **일부러 버리지 않는다**(버리면 FR-18 이 잘못 발동한다는 판단). 하한을 두는 것은 그 판단을 뒤집는 일이라 이번에 손대지 않았다.

## ✅ Outcome

- Result: **`_matches` 를 접두 일치로 좁혔다** (`knowledge/search.py`). 조사·어미가 뒤에 붙는다는 것만 가정한다. 실지식베이스에서 사고 3쌍이 사라지고 **정상 질의의 최상위는 점수까지 그대로**다. 시험 셋을 더했다 — 뒤에서 겹치는 것은 같은 낱말이 아니다 / 앞에서 겹치면 여전히 찾는다 / 단일 낱말 질의는 여전히 근거를 얻는다.
- Result: **기록된 "김치찌개 점수 1.00" 은 재현되지 않는다** — kit 출처 115개를 지운 뒤라 근거 0건으로 티켓에 간다. 살아 있던 오탐은 다른 모양이었다(양방향 부분 일치).
- Result: **답변 경로가 실데이터로 검증됐다.** 무관 질문 둘은 각각 조회·생성에서 멈춰 사람에게 갔고, 정상 질문 둘은 근거 5건 위에서 진술 7·9개와 경계 3건씩을 냈다. **QnA 쪽이 mock 인 것은 그대로다** — 검증한 것은 파이프라인이지 유입이 아니다.
- Verification: `uv run pytest -q` **1102 passed** (신규 3). 라이브 4건(실지식베이스 185항목 · 실모델, 게재 미호출). 오탐 재현·수정 전후 비교는 185항목 전수 스캔으로 냈다.
- Follow-up: **흔한 낱말 오탐**과 **점수 하한 부재**는 남았다 — 위 Remaining risks 참조. 둘 다 "무엇을 근거로 볼 것인가"를 다시 정하는 일이라 별도 판단이 필요하다.
- Follow-up: **승격 경로 A(`/entry` → 초안 → 무효화 조건 → 종결)는 이번에 태우지 않았다.** 라이브 운영 DB 에 사람이 볼 대기열 항목을 남기게 되므로 사용자의 판단이 먼저다.
