---
id: TASK-2026-09-02-main-008
status: done
created_at: 2026-09-02
source_anchor: generic-task-2026-09-02-main-008
source_path: backlog/2026-09-02.md
kind: generic
wbs: exempt
wbs_exempt_reason: 기존 4.2.6(Lint) 산출물의 정확성 보강이다. 신규 기능이 아니라 검사 하나가 못 보던 구멍을 메운다
---

# TASK-2026-09-02-main-008 — Lint 가 배제를 알게 한다 — 회복 불가 무효화를 지목한다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-02
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/knowledge/lint.py`
  - `src/agentic_service_desk/worker/runner.py`

- Description: 무효화가 배제 경로를 가리키면 stale 은 뜨는데 ingest 가 읽지 않아 갱신될 길이 없다. Lint 는 그 구조를 몰랐다
- Completion criteria: 배제 경로를 가리키는 linked 무효화가 소견으로 뜬다
- Completion criteria: 한 고장이 두 소견이 되지 않는다 — 죽은 무효화로 중복해 세지 않는다
- Completion criteria: 실지식베이스의 4건이 실제로 잡힌다

## 🛠️ Implementation / Content

- Progress: **방향이 반대인 두 결함을 나눠 센다.** `DEAD_INVALIDATION` 은 *발동하지 않는* 조건이고(경로가 변경분에 나타날 수 없다), 새 `EXCLUDED_INVALIDATION` 은 *발동하고 나서 회복하지 못하는* 조건이다 — stale 판정은 `changed_paths_since` 를 쓰고 배제를 적용하지 않아 표시가 뜨는데, ingest 는 `ASD_SOURCE_EXCLUDE` 로 그 경로를 읽지 않아 갱신될 길이 없다. **한 번 stale 이면 영영 stale 이다.** 2026-08-31 에 kit 출처 115개를 지운 이유가 이 구조였고, 순수 kit 출처가 아니라 그물에 안 걸린 4건이 남아 있었다.
- Next session starting point:
- Remaining risks: **남은 4건은 판정되지 않았다** — Q5 에 떴을 뿐이다. 배제를 되돌리면 지식의 40%가 모 시스템 것이 아니게 되고, 주기형으로 바꾸면 코드 연동을 잃고, 지우면 그 개념이 사라진다. 셋 다 대가가 있어 사람이 정할 일이다.

## ✅ Outcome

- Result: `Lint(exclude=...)` 를 더하고 워커가 ingest 와 **같은 배제**를 넘기게 했다. 두 쪽이 같은 원천을 다르게 보면 회복 불가 상태가 조용히 생긴다.
- Result: **미러 없이도 검사한다** — 배제는 선언된 glob 이라 대조가 정확해 거짓 소견이 나올 수 없다. 형제 검사가 미러를 요구하는 이유("없는 것과 사라진 것을 구분 못 한다")가 여기엔 없다.
- Result: **지우지 않는다.** 배제를 되돌릴지·주기형으로 바꿀지·항목을 버릴지는 지식베이스 구성을 정하는 판단이라 Q5 로 올리고 사람이 정한다. 지난번처럼 내가 지우지 않았다.
- Verification: uv run pytest -q 1139 passed (신규 7) · 실지식베이스 187항목에서 4건이 실제로 잡힌다(k-14304ef82be4 · k-6a16ca5d5e3c · k-5dfa2b83fd81 · k-f594b3eeb0cf) · 배제 밖 경로와 주기형은 잡지 않는다
- Follow-up:
