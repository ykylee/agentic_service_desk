---
id: TASK-2026-09-03-main-002
status: done
created_at: 2026-09-03
source_anchor: generic-task-2026-09-03-main-002
source_path: backlog/2026-09-03.md
kind: generic
wbs: exempt
wbs_exempt_reason: 기존 4.2.4(Ingest) 산출물의 정확성 보강이다. 신규 기능이 아니라 갱신 인식이 못 하던 것을 하게 만든다
---

# TASK-2026-09-03-main-002 — 색인이 항목을 알아보게 한다 — stale 이 회복되지 않던 이유

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-03
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/ingest/agent.py`
  - `src/agentic_service_desk/ingest/run.py`

- Description: 라이브에서 stale 21% 를 잡았다. 커서는 HEAD 인데 회복되지 않는다 — 색인이 제목만 줘서 모델이 어느 항목이 어느 파일에서 왔는지 모른다
- Completion criteria: 색인이 항목의 묶인 경로를 싣는다
- Completion criteria: 지시가 묶인 항목의 재확인을 요구한다
- Completion criteria: 같은 파일로 옛/새 프롬프트를 각각 불러 인식률 차이를 숫자로 낸다

## 🛠️ Implementation / Content

- Progress: **라이브 서비스 테스트에서 잡았다.** 대시보드 현황이 stale 21%(38건, 전부 linked)를 보여 줬는데 **커서가 HEAD 와 같았다** — ingest 가 그 변경들을 이미 읽고 지나갔다는 뜻이다. stale 항목의 출처는 HEAD 에서 중앙값 26커밋 뒤(정상 linked 는 16). 중복이 아니었다: 같은 경로의 stale 과 정상은 서로 다른 개념이다.
- Progress: **원인은 배선이었다.** 색인이 `(id, 제목)` 뿐이라, 모델은 `app.py` 를 읽으면서 항목 185개의 **제목만** 본다 — `ARM64 클러스터 토폴로지` 가 그 파일에서 나온 항목인지 알 방법이 없다. 지시 4번은 "같은 개념이면 그 id 로 갱신하라"고 요구하면서 **무엇이 지금 읽는 파일에 묶여 있는지는 알려주지 않았다.**
- Progress: **회복 조건이 하나뿐인 것도 함께 드러났다.** `to_knowledge_item` 이 항목을 쓸 때만 `stale=False` 가 된다 — **"다시 확인했고 그대로다"를 표현할 길이 없다.** 모델이 그 항목을 다시 내놓아야만 풀린다. 그래서 색인만 고쳐서는 부족하고 지시로 재확인을 요구해야 한다.
- Progress: **고친 것 둘.** (1) 색인 줄에 `← 경로` 를 싣는다 — **출처 경로를 먼저, 무효화 refs 를 뒤에**(앞엣것이 "무엇을 보고 썼는가"라 더 곧다), 상한 3개. (2) 지시 4번에 재확인 요구를 더했다 — "지금 읽는 파일에 묶인 항목은 다시 확인해 낸다. 달라졌으면 고쳐서, 그대로면 그대로 다시 낸다."
- Progress: **사람 편집을 함께 처리했다.** 재확인을 기계적으로 시키면 사람이 고친 항목까지 매번 다시 내게 되고, 그것은 덮이지 않고 **모순으로 남아**(FR-6) 대기열을 채운다. 색인에 `사람이 고침` 을 표시하고 "판단이 실제로 다를 때만 내라"를 지시에 못 박았다.
- Progress: **A/B 실측 (`src/auto_trading/dashboard_api/app.py` · 묶인 항목 12, 그중 stale 10).** 옛 색인(제목만 + 옛 규칙)은 제안 5개를 냈는데 **갱신 대상으로 고른 id 가 0개**였다 — 전부 새 항목이다. 새 색인(경로 + 재확인 지시)은 제안 13개 중 **10개가 이 파일에 묶인 기존 항목의 갱신**이었고 **그중 8개가 stale 이던 것**이다. 대가는 프롬프트 42,514 → 52,571자(+24%)와 소요 97 → 169초인데, **산출도 5 → 13 으로 늘어 순수 오버헤드가 아니다.**
- Progress: **n=1 이라 효과 크기는 주장하지 않는다.** 파일 하나, 호출 각 한 번이고 모델은 비결정적이다. 다만 **0 대 10 은 방향이 아니라 유무의 차이**이고, 기전(제목만으로는 연결할 수 없다)이 코드로 확인된 것과 맞는다.
- Next session starting point: (완료)
- Remaining risks: **항목의 25%(46개)는 색인에서 여전히 익명이다** — 출처 경로도 무효화 refs 도 없다. 그 항목들은 이 고침의 혜택을 받지 못하고, 알아보려면 제목이 유일한 단서다.
- Remaining risks: **재확인 요구가 출력량을 늘린다.** 묶음마다 관련 항목을 다시 내므로 소요·커밋·토큰이 는다. A/B 한 쌍으로는 런 전체의 대가를 알 수 없고, **다음 대규모 런에서 재야 한다.**
- Remaining risks: **효과를 라이브에서 아직 못 봤다.** 커서가 HEAD 라 읽을 것이 없어 stale 38건은 그대로다. **auto-trading 이 그 경로들을 다시 건드려야** 회복이 시작된다.

## ✅ Outcome

- Result: **색인이 항목의 묶인 곳을 싣고, 지시가 재확인을 요구한다.** 제목만 주던 것이 원인이었다 — 모델은 "같은 개념이면 갱신하라"는 요구를 받으면서 **어느 항목이 지금 읽는 파일에서 나왔는지 알 수 없었다.**
- Result: **A/B 에서 갱신 인식이 0 → 10 이다** (같은 파일, 묶인 항목 12·stale 10). stale 회복 8건.
- Result: 색인 비용 13,439 → 23,236자(+73%). 프롬프트 중앙값이 35,000자대이고 **색인이 주범이 아님은 전수(0→241항목)로 이미 확인됐다.**
- Verification: `uv run pytest -q` **1149 passed** (신규 4 — 경로 표시 · 상한 · 사람 편집 표시 · 재확인 지시). A/B 는 지식베이스에 쓰지 않았다.
- Follow-up: **다음 대규모 런에서 재확인 요구의 대가(소요·출력량)를 잰다.** A/B 한 쌍으로는 런 전체를 알 수 없다.
- Follow-up: 색인에서 익명인 46개(25%)를 어떻게 할 것인가 — `used_paths` 가 빈 항목들이다.
