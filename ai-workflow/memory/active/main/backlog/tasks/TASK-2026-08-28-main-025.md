---
id: TASK-2026-08-28-main-025
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-025
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.3.4
---

# TASK-2026-08-28-main-025 — WBS-4.3.4 승격 경로 A

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/operations/promotion.py`

- Description: 종결 기록을 지식 항목으로 승격한다. 사람이 무효화 조건을 채운 것이 곧 승격 승인이므로 Q7 을 거치지 않는다 (FR-15, D40, §6.8.1).
- Completion criteria: 종결 후 대응하는 지식 항목이 생긴다 (FR-15)
- Completion criteria: 경로 A 는 Q7 을 거치지 않는다 — 이중 승인은 1인 겸업에게 대기열 정체다 (§6.8.1)
- Completion criteria: 모순·Lint 티켓은 승격 대상이 아니다 — 출처로 걸러진다
- Completion criteria: 출처는 코드가 붙이고 사람이 채운 무효화 조건이 그대로 간다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 15:25` 기준 종결 기록을 지식 항목으로 승격한다. 사람이 무효화 조건을 채운 것이 곧 승격 승인이므로 Q7 을 거치지 않는다 (FR-15, D40, §6.8.1).
- Next session starting point:
- Remaining risks: 승격된 항목의 제목이 물음표로 끝난다(일반화된 질문 그대로). ADR-003 의 개념 단위 이름('결재 한도가 결정되는 규칙')과 형태가 달라, ingest 에이전트가 색인에서 같은 개념을 알아보기 어려울 수 있다. 다시 쓰지 않는다는 원칙과 개념 이름 형태 사이의 긴장이며 실데이터에서 중복 항목이 생기는지 봐야 안다
- Remaining risks: promote() 의 무효화 조건 검사는 정상 경로에서 걸리지 않는다. 티켓이 닫히려면 이미 채워져 있기 때문이며, 검증할 수 없는 지식은 승격 앞이 아니라 종결 앞에서 멈춘다. 함수 밖 호출을 위한 방어로 남겼다

## ✅ Outcome

- Result: Q7 을 거치지 않는다. 사람이 무효화 조건을 채운 것이 곧 승격 승인이며, 여기에 또 승인을 붙이면 이중 승인이고 1인 겸업에게 그 중복이 곧 대기열 정체다 (§6.8.1). Q7 을 거치는 것은 경로 B 하나뿐이다
- Result: 승격된 항목에 edited_by_human 을 켠다. 본문은 에이전트가 썼지만 가장 중요한 칸을 사람이 골랐다 — 표시하지 않으면 다음 ingest 가 이 항목을 자기 것으로 알고 **사람이 고른 무효화 조건을 자기 기본값으로 갈아 치운다.** 강제 입력으로 얻은 것이 조용히 사라지는 경로를 막았다
- Result: 코드 근거를 경로로만 남기고 커밋을 고정하지 않는다. 담당자가 '이 파일이 그렇게 동작한다'고 말한 것이지 우리가 그 커밋을 읽은 것이 아니다 — 모르는 것을 아는 척하면 Lint 의 참조 부재 검사가 헛돈다. QnA 출처는 언제나 하나 붙어 출처 없는 항목이 나오지 않는다
- Result: 다시 쓰지 않는다. 일반화된 질문이 곧 제목이고 답이 곧 본문이다 — 여기서 또 문장을 만들면 사람이 승인한 것과 지식이 된 것이 달라진다
- Result: PROMOTABLE_SOURCES 로 QnA 유래만 태운다. 모순 판정과 Lint 처리도 종결 기록을 남기지만 이미 있는 지식 중 하나를 고르거나 정합성을 되돌리는 일이지 새 지식을 만드는 일이 아니다
- Verification: uv run pytest -q — 413 passed (기존 393 + 신규 20). 실제 서버와 pi + MiniMax-M3 로 S1 전 구간 완주: 메신저 질문 등록 → 초안(이OO 걷힘, 근거 person, 적용 범위 추출) → 사람이 주기형 90일 선택(에이전트 제안 180 을 물리침) → 티켓 closed → 지식 항목 생성(edited_by_human true, provenance qna, 사람이 고른 무효화 조건) → promote 커밋 1개.
- Follow-up: WBS-4.4 S2 답변 초안 — 검색·파이프라인 1~3단계·불확실성 표시·검수·대시보드 확장. 이제 지식이 두 경로(ingest, 승격)로 쌓이므로 조회할 재료가 있다
- Follow-up: S1 전체를 실데이터로 한 번 돌려 봐야 한다. 승격된 항목과 ingest 항목이 같은 개념에서 중복되는지가 가장 궁금한 지점이다
