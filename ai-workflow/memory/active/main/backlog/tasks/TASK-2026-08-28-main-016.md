---
id: TASK-2026-08-28-main-016
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-016
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.2.3
---

# TASK-2026-08-28-main-016 — WBS-4.2.3 산출물 필터

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/ingest/output_filter.py`
  - `src/agentic_service_desk/config.py`
  - `tests/test_output_filter.py`
  - `.env.example`

- Description: 되먹임 차단(§5.3)을 ingest 입구 한 지점에서만 집행한다. 봇 계정 답변은 명시적 해결분만 통과시키고, 사람 답변은 언제나 통과시킨다 (NFR-4, FR-31).
- Completion criteria: 봇/사람 판정이 계정 단위로 이뤄지고 봇 계정 목록이 설정값이다
- Completion criteria: 봇 답변은 명시적 해결분만 통과한다 (FR-31) — 암묵·미해결은 차단
- Completion criteria: ingest 원천을 얻는 경로가 필터 하나뿐이고 우회 경로가 없다 (NFR-4)
- Completion criteria: Raw Layer 실적재분으로 mock 시드 다섯 분기가 검증된다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 13:28` 기준 되먹임 차단(§5.3)을 ingest 입구 한 지점에서만 집행한다. 봇 계정 답변은 명시적 해결분만 통과시키고, 사람 답변은 언제나 통과시킨다 (NFR-4, FR-31).
- Next session starting point:
- Remaining risks: 로컬 .env 에 ASD_BOT_ACCOUNTS 가 없다. 지금은 필터가 실행 경로에 걸려 있지 않아 문제없지만, WBS-4.2.4 가 이 값을 읽으므로 그 전에 채워야 한다 (.env.example 에 예시를 넣어 뒀다)

## ✅ Outcome

- Result: 필터가 판정(judge)뿐 아니라 **원천을 꺼내는 질의(ingestible_answers)까지 소유**한다. 배제 조건이 WHERE 안에 있어 꺼내면서 걸러지므로, 필터를 잊고 원천을 읽는 경로가 생기지 않는다 (NFR-4)
- Result: 봇 계정 목록을 ASD_BOT_ACCOUNTS 설정값으로 뺐다. 쉼표로 여럿 — 계정을 갈아 끼웠을 때 옛 계정을 빼면 그때부터 옛 산출물이 지식 원천이 된다
- Result: 목록이 비면 **동작을 거부한다**(BotAccountsNotConfigured). 전부 통과시키면 §5.3 이 조용히 무력화되고, 전부 막으면 '질문이 없다'와 구분되지 않는다 — 설정하라고 말하는 쪽을 택했다
- Result: 규칙이 judge() 와 SQL 두 벌로 쓰여 있다. 어긋나면 잡는 대조 시험(TestRuleIsStatedOnce)을 뒀다 — 문 구조의 대가를 시험으로 갚는다
- Result: 차단된 원문은 Raw Layer 에 그대로 남는다. 지식으로 삼지 않는 것과 기록하지 않는 것은 다르며, 여기서 지우면 통계와 FAQ 후보까지 사라진다 (§5.3)
- Verification: uv run pytest -q — 175 passed (기존 160 + 신규 15). ASD_PARENT_ADAPTER=mock + ASD_BOT_ACCOUNTS=svc-agentic-desk 로 실제 수집분 검증: 적재 답변 4건 중 2건 통과(Q-2 봇+명시적해결, Q-4 사람), 2건 차단(Q-3·Q-5 봇+미해결, reason=bot_unresolved). 계정 목록 없이 생성 시 BotAccountsNotConfigured.
- Follow-up: WBS-4.2.4 Ingest 에이전트 — filter.ingestible_answers(conn) 가 QnA 원천의 입구다. 시작 시점에 build_output_filter 를 불러 설정 누락을 배치 중간이 아니라 기동에서 드러내는 것이 좋다
- Follow-up: §7 콘텐츠도 같은 문을 지나야 한다 (§5.3 — 봇 답변과 콘텐츠 둘 다 우리 산출물이다). 콘텐츠의 해제 조건은 명시적 해결이 아니라 사람의 편집·승인이다 — WBS-4.6 에서 이 필터에 붙인다. 지금 미리 넣지 않은 이유는 콘텐츠 자료구조가 없어 닿을 수 없는 분기가 되기 때문
