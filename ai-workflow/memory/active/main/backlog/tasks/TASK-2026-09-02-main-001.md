---
id: TASK-2026-09-02-main-001
status: done
created_at: 2026-09-02
source_anchor: generic-task-2026-09-02-main-001
source_path: backlog/2026-09-02.md
kind: generic
wbs: exempt
wbs_exempt_reason: WBS 리프가 아니다 — 저장소 산출물이 아니라 워크플로우 메타 층(ai-workflow 킷 진입점)의 유지보수다
---

# TASK-2026-09-02-main-001 — 워크플로우 킷 v1.8.0 bootstrap 재적용

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-02
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `CLAUDE.md`
  - `.claude/commands/workflow-{session-start,backlog-update,doc-sync,session-end}.md`
  - `.claude/skills/standard-ai-workflow/SKILL.md`

- Description: 진입점 6개를 v1.8.0 으로 갱신하고, 손으로 채운 PROJECT_PROFILE.md 는 포크 선언으로 지킨다
- Completion criteria: wk ensure-entrypoints 의 stale 이 0
- Completion criteria: PROJECT_PROFILE.md 의 손으로 채운 1~5절이 그대로 남는다
- Completion criteria: state.json 이 재생성되어 in_progress·done 목록이 유지된다

## 🛠️ Implementation / Content

- Progress: 진입점 6개(CLAUDE.md · slash command 4 · SKILL.md)가 v1.7.0 에 멈춰 있었다. 본문 차이는 없고 marker·날짜만 올랐다.
- Progress: **`--force` 는 포크 선언을 이긴다** (`upgrade_diff.decide_action` — "포크는 '덮지 마라' 가 아니라 '모르고 덮지 마라'이므로 명시적 요구는 통과시킨다"). v1.8.0 wheel 에도 프로파일 템플릿이 번들되지 않아 force 를 줬으면 `docs/PROJECT_PROFILE.md` 가 `MISSING TEMPLATE` 한 줄이 됐다. **force 없이** 돌렸고 `forked` 판정으로 지켜졌다.
- Progress: **entry-mode 는 기본값(`aggressive`)이어야 한다.** `skill-only` 는 CLAUDE.md 를 건너뛴다 — 처음 `skill-only` 로 돌렸더니 `.claude/` 5개만 올라가고 **CLAUDE.md 가 v1.7.0 에 남았다.** 이 저장소엔 `AGENTS.md` 가 없어 aggressive 가 더 건드리는 것은 CLAUDE.md 하나뿐이다.
- Progress: **`state.json` 은 PRESERVE 선언에도 덮인다.** manifest 는 `ai-workflow/memory/active/**` 를 `preserved` 로 보고하지만 state.json 은 별도 경로(`build_workflow_state_payload`)로 쓰여 그 판정을 타지 않는다. done 5건과 in_progress 1건이 날아가고 `latest_backlog_path` 가 없는 파일을 가리켰다 — `wk refresh-state` 로 되돌려 diff 0.
- Progress: **`wk backlog-update` 가 `--progress-note` 반복 지정에 출력 없이 rc=2 로 죽는다** (v1.8.0). stdout·stderr 둘 다 비어 실패 이유가 어디에도 남지 않는다. 이 task 의 Progress 넷 중 셋은 그래서 손으로 적었다.
- Next session starting point:
- Remaining risks: PROJECT_PROFILE.md 는 포크라 이후 킷 갱신이 자동 반영되지 않는다 — 킷의 프로파일 템플릿이 바뀌면 손으로 대조해야 한다. 게다가 설치된 wheel 에 템플릿이 없어 대조할 원본조차 설치본에는 없다

## ✅ Outcome

- Result: 진입점 6개를 v1.8.0 으로 재적용. 본문 변경은 없고 marker·Last updated 만 올랐다.
- Result: PROJECT_PROFILE.md 는 v1.6.0 marker + 포크 선언 그대로 — forked 판정으로 한 줄도 바뀌지 않았다.
- Result: bootstrap 이 만든 placeholder(2026-09-02 backlog·task·repository_assessment.md)는 지웠고, state.json 은 wk refresh-state 로 되돌려 diff 0.
- Verification: wk ensure-entrypoints stale 0 · missing 0 · current_count 6 · wk session-start 진입점 warning 0 · state.json git diff 없음 · uv run pytest -q 1099 passed
- Follow-up:
