---
id: TASK-2026-08-28-main-027
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-027
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.4.2
---

# TASK-2026-08-28-main-027 — WBS-4.4.2 파이프라인 1~3단계

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/pipeline/answer.py`

- Description: 산출 파이프라인의 분석·조회·생성을 세운다. 질문 언어를 판정하고, 근거가 없으면 답을 만들지 않으며, 아는 만큼 답하고 경계를 밝힌다 (FR-16·17·18·19, D6·D17).
- Completion criteria: 단계를 건너뛰지 않고 단계 기록이 남는다 (FR-16)
- Completion criteria: 질문 언어를 판정하고 그 언어로 쓴다 — 근거는 원문 인용 (FR-17)
- Completion criteria: 근거 0건이면 초안이 생성되지 않는다 (FR-18)
- Completion criteria: 부분 답변 + 경계 명시가 기본값이다 — 억지 완성도 과잉 침묵도 아니다 (FR-19)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 15:59` 기준 산출 파이프라인의 분석·조회·생성을 세운다. 질문 언어를 판정하고, 근거가 없으면 답을 만들지 않으며, 아는 만큼 답하고 경계를 밝힌다 (FR-16·17·18·19, D6·D17).
- Next session starting point:
- Remaining risks: 영어 질문이 한국어 지식에 닿지 못한다. 키워드도 표현 사전도 언어를 넘지 못하고 그것을 넘는 것이 임베딩인데 O57 로 막혀 있다 — 라이브에서 'How is the approval limit determined?' 가 근거 0건이었다. 다언어 질문이 실제로 오는 환경이면 이 구멍이 커버리지 문제로 드러난다
- Remaining risks: 표현 사전의 두 낱말 요구가 짧은 질의를 막는다. '결재 반려' 같은 두 낱말 질의는 통과하지만 한 낱말 질의는 표현 사전 경로로 못 찾는다 — 정밀도를 위해 재현율을 내준 거래다

## ✅ Outcome

- Result: 언어 판정을 모델에 묻지 않고 글자로 센다. 판정이 결정적이어야 같은 질문이 같은 언어로 답해지고, 이 정도 구분에 LLM 호출을 붙이면 지연만 는다. 한글이 하나라도 있으면 한국어로 본다 — 식별자 개수로 다수결하면 코드 용어가 많은 사내 질문이 영어로 뒤집힌다
- Result: 근거 0건이면 생성 단계에 가지 않는다. 지어낼 기회 자체를 주지 않는 것이 FR-18 의 집행 형태다 — 시험이 하네스가 한 번도 불리지 않는지 본다
- Result: 근거를 하나도 안 가리킨 초안은 받지 않는다. 답이 근거에서 나왔는지 알 수 없으므로 검수 이전에 형식으로 걸러 낸다 (D3). 지어낸 id 도 버린다 — 없는 것을 가리키는 근거는 Lint 의 끊어진 링크가 되고 그때는 이미 답이 나간 뒤다
- Result: **라이브가 프롬프트 결함을 잡았다.** '제 결재 한도가 왜 300만원인가요?' 에 모델이 answerable=false 를 냈는데, §5.4.2 는 바로 그 질문을 부분 답변의 예시로 든다. 규칙 3 이 침묵 쪽으로 밀고 있었다 — 부분 답변이 기본값임을 앞에 두고 §5.4.2 의 예시를 프롬프트에 그대로 실었다. 과잉 침묵도 실패다
- Result: **라이브가 검색 정밀도 결함도 잡았다.** 'VPN 접속이 안 되는데 어떻게 하나요' 가 '결재 한도 결정 규칙' 에 걸렸고 이유가 '어떻게' 하나였다 — 표현 사전이 질문의 틀까지 담고 있었다. 불용어 목록 대신 두 가지로 막았다: 여러 항목을 함께 가리키는 말은 버리고(QnA 가 쌓일수록 정확해진다), 표현 사전은 두 낱말 이상 맞아야 걸린다
- Result: 시험이 환경의 ASD_* 를 걷어내게 했다. Settings(_env_file=None) 은 .env 만 막고 실제 환경변수는 읽어서, 그 변수를 내보낸 셸에서 돌리면 기본값을 쓰는 시험이 남의 디렉터리를 보고 엉뚱하게 실패한다 — 실제로 밟았고 원인을 찾는 데 시간이 걸렸다
- Verification: uv run pytest -q — 468 passed (기존 440 + 신규 28). 라이브(pi + MiniMax-M3, 실제 ingest 지식 2건): '제 결재 한도가 왜 300만원인가요?' → 규칙은 답하고 현재 값은 조회 대상이 아님을 밝힌 부분 답변 + 경계 2건(§5.4.2 예시 그대로), 'VPN 접속이 안 되는데' → 근거 없음으로 생성 단계에 가지도 않음(FR-18), 영어 질문 → 언어 en 판정.
- Follow-up: WBS-4.4.3 불확실성 표시 — 진술별 근거 강도 세 단계(확인됨/추론/근거 얇음, ADR-007 결정 3). 생성 단계가 그것을 함께 내야 하므로 여기 프롬프트가 확장된다
- Follow-up: WBS-4.4.4 검수 — 생성과 분리하고 P1~P5 를 반려 사유로 삼는다. 라이브 초안에 '이 분산 한 300 만원' 같은 문장 깨짐이 있었는데 그런 것이 검수에서 걸려야 한다
