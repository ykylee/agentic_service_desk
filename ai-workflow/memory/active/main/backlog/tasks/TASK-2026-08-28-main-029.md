---
id: TASK-2026-08-28-main-029
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-029
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.4.4
---

# TASK-2026-08-28-main-029 — WBS-4.4.4 검수

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/pipeline/review.py`

- Description: 검수를 생성과 분리한다. 입력은 초안과 근거 원문뿐이며, P1~P5 를 반려 사유로 삼고 분류해 기록한다 (FR-20·21·22, §5.5).
- Completion criteria: 검수 입력에 생성 컨텍스트가 없다 — 질문 의도도 생성 추론도 주지 않는다 (FR-20)
- Completion criteria: P1~P5 패턴이 반려된다 (FR-21)
- Completion criteria: 반려 사유별 분포를 집계할 수 있다 (FR-22)
- Completion criteria: 기계적으로 잡히는 것(P4·P1)은 모델에 묻지 않는다 (§5.5.1)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 16:19` 기준 검수를 생성과 분리한다. 입력은 초안과 근거 원문뿐이며, P1~P5 를 반려 사유로 삼고 분류해 기록한다 (FR-20·21·22, §5.5).
- Next session starting point:
- Remaining risks: P5(근거 범위를 넘긴 일반화)를 의미 판정에만 맡겼다. §5.5.1 은 준기계적으로 볼 수 있다고 했지만 그러려면 지식 항목에 적용 범위 필드가 있어야 하는데 없다 — 종결 기록에는 scope 가 있으나 KnowledgeItem 에는 없다
- Remaining risks: P1 이 숫자만 본다. §5.5.1 은 수치·기간·고유명사를 함께 대조하라 했는데 고유명사는 언어별 판정이 필요해 의미 판정으로 넘겼다 — 지어낸 고유명사는 기계적으로 걸리지 않는다
- Remaining risks: 국면별 검수 강도(§5.5.3)와 위험 신호 기반 선별(§5.5.4)이 아직 없다. 지금은 모든 초안이 같은 검수를 받으며, 자동 게재 여부를 가르는 것은 FR-25 로 WBS-4.5 의 몫이다

## ✅ Outcome

- Result: 검수의 입력은 초안 본문과 근거 원문뿐이다. ReviewInput.of() 가 분리의 집행 지점이며 그 생성자가 받는 것 밖의 무엇도 검수에 닿지 않는다 — 질문도, 분석 결과도, 생성 측이 매긴 불확실성 표시도 넘기지 않는다. 자기 평가를 보면 대조가 아니라 확인이 된다
- Result: **분리에 대가가 있다.** 질문을 모르므로 이용자가 말한 수치를 되받은 문장과 지어낸 수치를 구분하지 못한다 — 둘 다 근거에 없기 때문이다. P1 이 넉넉하게 걸리는 것을 받아들였다: 반려는 버리는 것이 아니라 사람에게 보내는 것이고(1국면은 사람 검수가 기본) 근거에 없는 수치는 검수 입장에서 확인할 수 없는 수치라는 점에서 판정 자체는 옳다
- Result: 다섯이 같은 종류의 판정이 아니라는 §5.5.1 을 구조로 옮겼다. P4(기계적)·P1(준기계적)을 먼저 보고, 걸리면 모델을 부르지 않는다 — 확정 가능한 것을 확률적 판정에 맡기지 않고, 반려는 하나면 충분하다. 라이브에서 모델 호출 0회를 확인했다
- Result: 판정할 수 없으면 통과시키지 않는다. 검수가 없는 것과 통과한 것은 다르며 후자는 '검증했다'는 라벨을 남긴다 (§5.6.1 — 형식적 승인은 무검증보다 나쁘다)
- Result: 반려된 초안도 남긴다. 버리면 왜 반려됐는지의 분포를 잃고, 그 분포가 지식 공백 탐지(P1·P5 가 몰리면 근거가 부족하다)·신뢰 계측·자동 검수의 학습 자료 셋으로 쓰인다 (§5.5.6). grounding_gap_share 가 그 첫째를 바로 읽어 준다
- Result: 사유 없는 반려는 가장 넓은 것(P5)으로 분류하고 상세를 남긴다. 사유 없는 반려는 기록으로 쓸 수 없다
- Verification: uv run pytest -q — 502 passed (기존 481 + 신규 21). 라이브(pi + MiniMax-M3, 실제 지식): 의미 판정이 네 경우를 정확히 갈랐다 — 정직한 인용 통과, P2(예외 없다고 단정)·P3(야간 배치 갱신이라는 업계 통념)·P5(모든 권한이 등급으로 결정된다) 각각 정확한 사유와 설명으로 반려. 기계적 검사는 근거 없는 수치를 P1, 낡은 근거를 P4 로 잡았고 두 경우 모두 모델 호출 0회.
- Follow-up: WBS-4.4.5 대시보드 확장 — Q2 답변 검수 대기열. 반려 사유별 분포(distribution)와 약한 지점(weak_points)이 화면의 재료다. unmatched_terms 가 사람이 어디를 먼저 볼지 알려주는 데 쓰인다
