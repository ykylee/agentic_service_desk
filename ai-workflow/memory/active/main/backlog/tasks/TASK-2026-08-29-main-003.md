---
id: TASK-2026-08-29-main-003
status: done
created_at: 2026-08-29
source_anchor: generic-task-2026-08-29-main-003
source_path: backlog/2026-08-29.md
kind: generic
wbs: M-004/WBS-4.7.3
---

# TASK-2026-08-29-main-003 — WBS-4.7.3 뉴스레터

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-29
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/content/period.py`
  - `src/agentic_service_desk/content/production.py`
  - `src/agentic_service_desk/content/store.py`
  - `src/agentic_service_desk/content/registry.py`
  - `src/agentic_service_desk/content/types.toml`
  - `src/agentic_service_desk/knowledge/repository.py`
  - `tests/test_newsletter.py`

- Description: 기간 내 변경·발행 요약으로 뉴스레터 한 회차를 만든다. 발행 면 create 이고 발송하지 않는다 (FR-38, §7.2·§7.7.3).
- Completion criteria: 기간 내 변경·발행 요약으로 뉴스레터 회차가 만들어져 Q3 에 오른다
- Completion criteria: 센 사실이 초안에 박히고 본문이 쓴 것만 나가는 글의 근거로 실린다
- Completion criteria: 바뀐 지식이 없으면 회차를 내지 않는다

## 🛠️ Implementation / Content

- Progress: `2026-08-29 22:47` 기준 기간 내 변경·발행 요약으로 뉴스레터 한 회차를 만든다. 발행 면 create 이고 발송하지 않는다 (FR-38, §7.2·§7.7.3).
- Next session starting point:
- Remaining risks: 뉴스레터에는 P6~P8 이 붙지 않는다 (선언대로, §7.6.4). '잘 되고 있습니다' 같은 논평을 막는 것은 프롬프트뿐이고 세지 않는다 — 세지 않는 것을 센 척하지 않는다.
- Remaining risks: 회차 번호가 없다. 제목을 모델이 매번 새로 짓는데(라이브에서 '지난 30일 사내 시스템 소식'), 시계열로 쌓이는 발행물이라 몇 호인지가 글 안에 없다. 모 시스템의 발행 면이 그것을 매기는지는 연동해 봐야 안다.
- Remaining risks: 지난 회차가 무엇을 적었는지 보지 않는다. 프롬프트가 '이번 기간의 일만' 이라고 말하지만 기간이 겹치지 않게 하는 것은 트리거뿐이다 — 주기 전에 손으로 돌리면 같은 소식이 두 번 나간다.

## ✅ Outcome

- Result: 칼럼과 같은 발행 면이지만 **읽는 것이 다르다** (§7.2). 주 입력에 period_summary 를 더했다 — 새 주 입력을 더하는 것은 코드를 더하는 일이 맞다: FR-42 가 없애려던 것은 '타입마다 구현이 쌓이는 것'이지 '새 원천을 읽는 것'이 아니다.
- Result: 요약은 세는 것이지 판단하는 것이 아니다. 모델에게 '이번 기간이 어땠는가'를 묻지 않는다 — 검증할 수 없는 서술이 발행물에 실리면 회수할 방법이 없다(§7.3). 세어서 만든 문장을 주고 그것으로 쓰게 한다 (diff 를 모델에게 묻지 않는 것과 같은 자리).
- Result: 새로 생긴 것과 고쳐진 것을 나눴다 — 한 말로 적으면 읽는 사람은 없던 것이 생긴 줄로 읽는다. 지식베이스가 자라는 것과 틀린 것을 고치는 것은 다른 소식이다.
- Result: 할 말이 없으면 내지 않는다. 근거는 기간 안에 바뀐 지식 항목이고, 아무것도 안 바뀌었으면 초안을 만들지 않는다 — 발행 면에 빈 회차가 쌓이는 것도 회수할 수 없는 종류다. 없는 것(콘텐츠 0건)도 싣지 않는다: 사실이지만 소식이 아니고, 있는 것이 그 사이에 묻힌다.
- Result: 박은 사실을 store.Fact 로 모았다. 관찰(§7.6.2)이든 기간 요약(§7.2)이든 검수·게재·화면이 아는 것은 셋뿐이다 — 번호·문장·본문이 그것을 밝혔는가. 만드는 쪽만 다르다.
- Result: 라이브 결함 ①: git 이 비 ASCII 경로를 따옴표로 감싸 내놓아 changed_since 가 **아무것도 못 찾았다.** 지식 항목 제목이 대부분 한국어라 사실상 전부가 빠진다 — staged_item_paths 가 이미 밟은 자리라 같은 처방(-z)을 썼다.
- Result: 라이브 결함 ②: 본문이 요약 셋을 그대로 옮겨 적고도 신고하지 않아 **적힌 숫자의 출처가 근거 목록에서 빠졌다** (칼럼의 부풀림과 정반대). 신고를 받되 **신고하지 않아도 본문이 그 사실에만 있는 수치를 적었으면 싣는** 안전망을 넣었다 — 여러 사실에 함께 있는 수치(기간 30)는 아무것도 가리키지 않으므로 뺀다. 부풀리는 것보다 빠뜨리는 쪽이 나쁘다: '5건'이라 적혀 있는데 출처가 없으면 근거 없이 나간 것과 같다.
- Verification: uv run pytest -q — 899 passed (신규 16, tests/test_newsletter.py). 라이브(pi 0.84.1 + MiniMax-M3): 기간 안 지식 2건 신규 · 콘텐츠 1건 게재 · 답변 5건(해결 3건) · 반복 문의 3건으로 회차를 만들었다. 본문이 네 사실을 모두 적었고 근거 목록에 '집계:' 로 함께 실렸다. 결함 둘(git 경로 인코딩 · 신고 누락)을 잡아 고치고 두 번 재검증했다. /status 콘텐츠 현황과 Q3 상세를 화면으로 확인했다.
- Follow-up: WBS-4.8.1 국면 판정과 전환 — ASD_PHASE 가 수동인 것이 남은 가장 큰 구멍이다
- Follow-up: WBS-4.8.4 표본 재검증 — 국면을 2 로 올리기 전에 있어야 한다 (§6.8.4-a)
