---
id: TASK-2026-09-03-main-005
status: done
created_at: 2026-09-03
source_anchor: generic-task-2026-09-03-main-005
source_path: backlog/2026-09-03.md
kind: generic
wbs: M-005/WBS-5.7.2
---

# TASK-2026-09-03-main-005 — 정제를 게재까지 잇는다 — 저장에서 떨어지고 있었다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-03
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/pipeline/draft_store.py`
  - `src/agentic_service_desk/operations/migrations.py`
  - `src/agentic_service_desk/web/templates/q2.html`

- Description: 파이프라인에 정제를 넣었지만 초안이 DB 를 왕복하며 사라졌다. Q2 를 지나 나가는 것은 원본이었다
- Completion criteria: 정제된 글이 저장을 왕복해도 남는다
- Completion criteria: Q2 검수 화면이 나갈 글을 보여 준다
- Completion criteria: 정제되지 않은 초안은 종전대로 원본이 나간다

## 🛠️ Implementation / Content

- Progress: **"이 답변 방식이 적용되었니?"에 확인해 보니 아니었다.** 파이프라인 4단계는 `Draft.rendered` 를 채우는데 `draft_store` 가 `statements`·`grounding`·`unanswered` 만 저장한다 — Q2 를 지나 게재되는 경로에서 **정제가 통째로 떨어지고 진술을 이어 붙인 원본이 나간다.** 파이프라인에만 넣고 저장을 잊은 채 한 번 커밋됐다.
- Progress: **스키마 계단 014 를 놓았다** (ADR-010). `answer_draft.rendered` 는 비워 두는 것이 기본이다 — 이행 시점의 옛 초안은 정제된 적이 없으므로 `NULL` 이고, 그때는 종전대로 진술이 나간다. 선언(`SCHEMA_SQL`)과 계단을 **함께** 고쳤다.
- Progress: **기동 거부가 라이브에서 실제로 작동했다.** 코드가 14 를 요구하는데 DB 가 13 이라 `asd-web` 이 뜨지 않고 `거부 — 스키마 버전이 13 인데 코드는 14 를 요구한다. asd migrate 를 먼저 돌린다 (대기: 014)` 를 냈다. ADR-010 이 설계대로 도는 것을 처음 본 자리다.
- Progress: **Q2 가 나갈 글을 보여 주지 않고 있었다.** 화면이 `statements`(강도가 붙은 원본)만 렌더했는데, 그러면 **검수자가 실제 게재될 글을 못 본다.** 나갈 글을 위에 두고 진술을 그 아래 근거로 내렸다. 정제되지 않은 초안이면 그 사실을 화면이 말한다.
- Progress: **`PendingDraft.body` 를 `Draft.body` 와 같은 규칙으로 맞췄다** — 갈리면 검수 화면이 보여 준 글과 게재된 글이 달라진다.
- Next session starting point: (완료)
- Remaining risks: **정제된 글이 길다.** 지식베이스가 아는 원인 세 갈래를 다 담는다 — 사람이라면 가장 가능성 높은 것부터 짚거나 되물었을 것이다. "묻는 것에만" 지시를 넣었지만 근거가 여럿이면 다 쓰는 경향이 남는다. **실제 QnA 가 붙은 뒤 실제 질문들로 조정한다** (2026-09-03 사용자 결정 — 길이는 두고 기록만).

## ✅ Outcome

- Result: **정제가 게재까지 닿는다.** `answer_draft.rendered` 로 저장을 왕복하고, `PendingDraft.body` 가 정제된 쪽을 낸다 — 검수 화면이 보여 준 글이 곧 게재된 글이다.
- Result: **Q2 가 나갈 글을 먼저 보여 준다.** 진술과 강도는 그 아래 근거로 남아 "이 글이 어디서 왔고 어디가 약한가"를 답한다.
- Verification: `uv run pytest -q` **1180 passed** (신규 2) · `asd migrate` v13 → v14 · **이행 전 기동이 실제로 거부됐다** · 재기동 후 health 200 · `answer_draft` 에 `rendered` 칸 확인.
- Follow-up: 답변 길이 조정은 실제 QnA 유입 뒤로 미룬다.
