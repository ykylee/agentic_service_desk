---
id: TASK-2026-08-28-main-041
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-041
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.6.1
---

# TASK-2026-08-28-main-041 — WBS-4.6.1 콘텐츠 타입 레지스트리

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/content/registry.py`
  - `src/agentic_service_desk/content/types.toml`
  - `src/agentic_service_desk/config.py`
  - `src/agentic_service_desk/web/metrics.py`

- Description: 다섯 선언(주 입력·트리거·성격·게재처·검수 강도)으로 콘텐츠 타입을 더한다. 코드 변경 없이 (FR-42, §7.5).
- Completion criteria: 기본 넷(FAQ·가이드·칼럼·뉴스레터)이 선언으로 등록된다
- Completion criteria: 새 타입이 코드 변경 없이 선언만으로 추가된다 (FR-42 검증)
- Completion criteria: 성격과 게재처가 어긋나거나 발행물 검수가 하한 미만이면 등록이 거부된다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 22:14` 기준 다섯 선언으로 콘텐츠 타입을 더한다. 코드 변경 없이 (FR-42, §7.5).
- Next session starting point:
- Remaining risks: 게재처의 path 는 선언되지만 아직 아무도 쓰지 않는다 — 실제 문서 면 경로 규약은 WBS-4.6.3 이 붙이며, 그때 'faq/index' 같은 지금 값이 맞는지 드러난다.
- Remaining risks: T4(자기 참조) — 콘텐츠도 이 시스템이 쓴 것이라 ingest 되먹임 대상이다. 산출물 필터가 콘텐츠 게재물을 아직 보지 않는다: 게재가 붙는 4.6.3 에서 함께 막아야 한다.

## ✅ Outcome

- Result: 선언을 코드가 아니라 파일에 뒀다 — 파이썬 상수로 두면 'FR-42: 새 타입 추가에 코드 변경이 필요 없다' 를 시험이 실제로 확인할 수 없고, 확인할 수 없는 요구는 지켜지지 않아도 아무도 모른다. 기본 넷은 패키지의 types.toml 로 오고 ASD_CONTENT_TYPES_PATH 가 거기 더한다.
- Result: 선언은 등록 시점에 검사받는다: ① 모르는 열쇠를 무시하지 않는다 — final_chek 오타 하나로 발행물의 최종 확인이 꺼진 채 지나가는데 회수 불가인 글이 나간 뒤에야 드러난다 ② 성격과 자리가 어긋나면 거부한다(§7.3→§7.7) — 살아있는 문서를 발행 면에 내면 갱신이 회차 누적이 되고, 발행물을 문서 면에 올리면 지난 회차를 덮어쓴다 ③ 발행물의 검수를 하한 아래로 낮출 수 없다
- Result: 검수자는 선언 항목이 아니다. FR-39 가 콘텐츠를 국면 무관 전수 사람 승인으로 못 박았으므로 타입이 고를 수 있게 두면 그 요구가 선언 한 줄로 꺼진다 — 타입이 고르는 것은 범위(diff/full)와 추가 반려 사유(칼럼의 P6~P8)뿐이다. 언어도 선언하지 않는다(FR-43).
- Result: 기본 타입을 덮지 못하게 했다 — 덮게 두면 발행물의 검수 강도가 설정 한 줄로 변경분 검수가 되고 그 사실이 어디에도 남지 않는다.
- Result: content 패키지를 새로 열었다(아홉째 경계). 소유 범위는 타입 선언과 그 집행이지 생성 절차가 아니다 — 절차는 pipeline 에 있고, 콘텐츠는 트리거만 다른 같은 것이다(D10, §7.1).
- Result: /status 콘텐츠 현황이 '아직 만들지 않았다' 에서 '등록된 넷과 각각의 성격·자리·검수 강도' 로 바뀌었다. 제작·게재 수는 여전히 0 을 내지 않는다 — 등록된 것과 만들어진 것은 다르다.
- Verification: uv run pytest -q — 748 passed (신규 21). 라이브: ASD_CONTENT_TYPES_PATH 로 다섯째 타입 '릴리스 노트' 를 선언만으로 추가해 /status 콘텐츠 현황에 떴고, final_check 를 final_chek 로 오타 낸 선언은 기동에서 InvalidDeclaration 으로 거부됐다.
- Follow-up: WBS-4.6.2 가이드 제작 — 트리거 해석(period_days · source_changed 임계)이 여기서 처음 실행된다.
