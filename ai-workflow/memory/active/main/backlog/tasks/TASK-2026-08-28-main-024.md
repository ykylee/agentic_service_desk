---
id: TASK-2026-08-28-main-024
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-024
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.3.5
---

# TASK-2026-08-28-main-024 — WBS-4.3.5 대시보드 확장 (Q1 티켓 대기열)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/web/dashboard.py`

- Description: Q1 티켓 대기열 화면 — 작업 화면은 상세와 상태 전이를 갖고, 무효화 조건을 채워 티켓을 닫는다. 시스템이 다음에 볼 것을 제시하며 정렬은 방치 비용 곱하기 경과 시간이다 (FR-45·46, D32, §8.6.3).
- Completion criteria: 작업 화면이 상세와 상태 전이를 갖는다 — 판정 화면과 다르다 (FR-45)
- Completion criteria: 시스템이 다음에 볼 것을 제시하고 정렬이 방치 비용 곱하기 경과 시간이다 (FR-46, D32)
- Completion criteria: 무효화 조건을 골라 채우면 티켓이 닫힌다 — 등록에서 종결까지 고리가 이어진다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 15:18` 기준 Q1 티켓 대기열 화면 — 작업 화면은 상세와 상태 전이를 갖고, 무효화 조건을 채워 티켓을 닫는다. 시스템이 다음에 볼 것을 제시하며 정렬은 방치 비용 곱하기 경과 시간이다 (FR-45·46, D32, §8.6.3).
- Next session starting point:
- Remaining risks: 대시보드에 여전히 인증도 CSRF 보호도 없다. 이제 상태 전이와 종결까지 POST 로 가능해져 표면이 넓어졌다
- Remaining risks: next_up 이 티켓을 낳는 작업 대기열만 줄 세운다. 판정 대기열(Q2·Q6·Q7·Q8)은 티켓이 없어 같은 자에 오르지 못하는데, §8.2 가 영원히 밀린다고 걱정한 것이 바로 Q6·Q7·Q8 이다 — 그것들이 채워지는 4.4~4.5 에서 순위에 넣어야 한다

## ✅ Outcome

- Result: 정렬을 방치 비용 곱하기 경과 시간으로 했다. **곱셈인 것이 요점이다** — 덧셈이면 낮은 비용의 대기열은 아무리 오래돼도 높은 비용을 넘지 못해 §8.2 가 걱정한 '영원히 밀리는' 문제가 그대로 남는다. 시험이 Q3(중간) 100시간이 Q4(높음) 10시간을 앞지르는지 본다
- Result: 작업 대기열 넷이 모두 티켓을 낳는다는 점을 이용해 순위를 티켓 하나로 통일했다 (§6.4.4). 티켓 출처에서 대기열로 가는 사상 하나면 Q1·Q3·Q4·Q5 가 같은 자로 줄을 선다
- Result: 종결 버튼을 상태 전이에서 뺐다. **닫는 길은 무효화 조건을 채우는 것 하나뿐**이고, 채우면 곧바로 닫힌다 — 따로 닫기 버튼을 두면 채워 놓고 닫지 않은 티켓이 쌓인다
- Result: 근거가 코드에 닿지 않으면 화면이 경고한다 — 그때 무효화 조건이 유일한 stale 장치다(§6.5.3). 라이브에서 실제로 person 근거가 나와 그 경고가 떴다
- Result: 질문 등록 화면을 Q1 이 보이는 단계로 게이팅했다. 등록이 만드는 것은 티켓인데 티켓을 볼 수 없는 단계에서 등록을 받으면 보이지 않는 대기열이 쌓인다 — 4.2.7 에서 stale 을 Q5 에서 뺀 것과 같은 이유다. 화면만이 아니라 POST 쪽에도 걸었다
- Result: 상세 화면이 '지금 할 일'을 상태별로 말한다. 닫힌 뒤에도 '닫을 수 있다' 를 계속 말하면 화면이 거짓말을 하므로 종결·보류를 따로 처리했다
- Verification: uv run pytest -q — 393 passed (기존 371 + 신규 22). 실제 서버로 고리 전체 완주: 폼 등록 → pi + MiniMax-M3 초안(개인 식별자 걷힘, 근거 person, 후보 2개, 무효화 조건 None) → 상세 화면에서 잘못 채우기 거부('연결형에는 묶을 대상이 필요하다') → 직접 채우기 → 303 → 티켓 closed, 승격가능 True, Q1 목록 '열린 티켓이 없다'.
- Follow-up: WBS-4.3.4 승격 경로 A — 이제 승격할 재료가 실제로 만들어진다. resolution.promotable 이 자격이고 Ground 에서 Provenance 로의 변환이 필요하다. 모순·Lint 티켓의 기록은 승격 대상이 아니므로 ticket.source 로 걸러야 한다
