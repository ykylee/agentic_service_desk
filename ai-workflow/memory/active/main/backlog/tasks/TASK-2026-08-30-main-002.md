---
id: TASK-2026-08-30-main-002
status: in_progress
created_at: 2026-08-30
source_anchor: generic-task-2026-08-30-main-002
source_path: backlog/2026-08-30.md
kind: generic
wbs: exempt
wbs_exempt_reason: M-001~M-004 모두 done 이라 열린 마일스톤이 없다. 다음 마일스톤 선언 전의 실데이터 검증 실행이며, 결과가 M-005 범위를 정하는 입력이 된다.
---

# TASK-2026-08-30-main-002 — 시뮬레이션 검증 — 실저장소 지식베이스 구축

## 📝 Description

- Status: in_progress
- Priority: high
- Request date: 2026-08-30
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/llm/policy.py`
  - `src/agentic_service_desk/config.py`
  - `.env.example`
  - `src/agentic_service_desk/ingest/run.py`
  - `src/agentic_service_desk/knowledge/lint.py`
  - `src/agentic_service_desk/pipeline/publication.py`

- Description: 모 시스템을 로컬 저장소 둘(standard_ai_workflow · auto-trading)로 가정하고 S0 서비스를 올려 실제 소스로 지식베이스를 구축한다. 4.2 완료 판정(LintReport.clean)의 예행이다.
- Completion criteria: 두 저장소 ingest 완료 · 지식 항목 축적 · Lint 안정(모순 미해결 0 · 깨진 링크 0)

## 🛠️ Implementation / Content

- Progress: 실운영이 결함 둘을 더 잡았다. (6) 중단 신호가 ingest 에 닿지 않아 pkill 이 통하지 않았다 — 워커 다섯이 같은 지식베이스에 동시에 쓰고 있었고 넷은 옛 코드였다. IngestRun 이 should_stop 을 받아 묶음 경계에서 나가고 읽다 만 커서는 두지 않는다(실측 2분45초). (7) pi 를 -p 만 주고 불러 도구·컨텍스트 파일이 켜진 채 앱 저장소에서 돌았다 — 에이전트가 모 시스템이 아니라 우리 커밋과 파일을 읽고 있었고, 도구로 턴을 소진해 ingest 6건이 전부 실패했다. NON_AGENTIC 6플래그 + 빈 임시 디렉터리. 시험 1063→1076.
- Progress: **지식베이스 커밋 0건을 조사했다 — 결함이 아니라 설계대로다.** `IngestRun.run()` 은 미러 전부를 돌고 나서 커밋 하나를 만들고 **그 뒤에** 커서를 옮긴다 (`ingest/run.py:142`). `_ingest_one_source` 는 묶음을 다 끝내야 HEAD 를, `_ingest_source` 는 미러를 다 돌아야 heads 를 돌려주므로 **두 저장소 ~770묶음이 런 하나**이고 커밋은 그 끝에 하나 생긴다. 없는 해시를 지어내지도 않는다: `head()`→None (`repository.py:268`), `pin()`→None (`publication.py:214`), `publish()`→`Refused(UNPINNABLE)` (`publication.py:339`). 시험이 덮는다 — `test_answer_record.py:169`(커밋 없는 저장소)·`:190`(저장소 부재), 둘 다 "기록도 남기지 않는다"까지 센다. 실측도 일치: `answer_record` 0 · `answer_draft` 0.
- Progress: **런 하나가 40시간대가 되면서 드러나는 것 둘.** (1) 주기형 무효화 검사가 전 구간 조용히 건너뛰어진다 — `lint.py:304` 이 `last_commit_date` 가 없으면 `return None` 이라 소견조차 남지 않는다. `LintReport.clean` 은 모순+findings 만 보므로 **4.2 판정 자체는 무관**하지만, 그 축은 아직 한 번도 안 돌았다. (2) 부트스트랩 내내 모든 게재가 UNPINNABLE 이라 **답변 경로는 이번 검증 범위에서 빠진다.** S0 이라 대외 노출이 없어 지금은 무해하다.
- Progress: **제안(미실행) — 커밋과 커서는 분리할 수 있다.** 주석이 원자성으로 지키려는 것은 *커서*이고, 커밋을 자주 하는 것은 그 구멍을 만들지 않는다: 중간에 죽어도 커서가 제자리라 다음 런이 같은 구간을 다시 읽고 항목은 `by_id`/`index` 로 갱신된다. 걸리는 곳 하나는 `_mark_ingested(answer_ids, result.commit)` — 커밋이 여럿이면 답변별로 짝지어야 한다. **돌고 있는 워커는 옛 코드로 계속 도므로 지금 고치지 않는다** (지난 세션이 워커 다섯으로 밟은 그 문제). 부트스트랩 완주와 4.2 판정 뒤에 손댄다.
- Progress: 부트스트랩 실측 00:02 — 워커 1 · pi 1 · 항목 12 · 커서는 `qna` 뿐(`source:` 두 줄 아직 없음) · 실패 0. 항목 생성이 23:16:44 에 1개, 23:55:48~49 에 11개로 뭉쳐 있다(기동 23:12). 표본이 둘이라 **아직 속도를 단정하지 않는다.**
- Progress: **지식베이스 1차 점검 (항목 12, 00:15).** 전부 `auto-trading` 한 저장소의 HEAD 커밋 하나(`15098666`)에서 나왔다 — `standard_ai_workflow` 는 미러 순서상 아직 차례가 아니다. **죽은 무효화 ref 0/29 (0%)** — 이전 실행 37% 에서 내려왔고 `a666f41`(ingest 쪽 집행)이 듣는다는 실측이다. 그 밖에 `<think>` 누출 0 · 물음표 제목 0 · 중복 개념 후보 0 · `edited_by_human` 전부 false · 원천 오염 없음(우리 앱 저장소가 섞이지 않았다) · 자격증명은 키 **이름**만 있고 값 없음. 내용도 개념 단위로 잡혔고 무효화 조건이 실제 소스 경로에 묶여 있다.
- Progress: **FR-9 이 systemd 유닛 파일을 못 잡는다 — 12개 중 2개에서 설정값이 그대로 실렸다.** `디스크-자기-정리` 에 `deploy/systemd/auto-trading-clean-disk.timer` 의 `OnCalendar=Sun *-*-* 03:00:00 Asia/Seoul` · `Persistent=false` · `AccuracySec=1h` 가, `arm64-클러스터-토폴로지` 에 SSH config 형식의 `IdentitiesOnly=yes` 가 들어 있다. **방어 둘이 모두 비껴간다**: (1) 경로 필터 — `.timer`·`.service` 가 `CONFIG_SUFFIXES` 에 없고 `deploy/systemd/*` 도 `CONFIG_PATTERNS` 에 없다(실측: `exclude_config_paths(['deploy/systemd/x.timer'])` → 통과). (2) 값 필터 — 이름 정규식이 `[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)+` 라 **밑줄이 필수**인데 systemd 는 `OnCalendar` 같은 CamelCase 다. `config_values.py` 가 예고한 "아직 안 본 모양"이 이것이다.
- Progress: **제안(미실행) — `CONFIG_SUFFIXES` 에 `.timer`·`.service`·`.socket` 을, `CONFIG_PATTERNS` 에 `*/systemd/*` 를 더한다.** 한 줄이고 `config_paths.py` 가 선언한 "경계는 넉넉하게"와도 맞는다. 판정 자체는 사람 몫으로 남는다 — 타이머 스케줄이 §2.2.2 의 *현재 상태*인지 "디스크는 주 1회 정리된다"는 *규칙*인지. 스케줄이 바뀌어도 개념은 그대로고 항목만 낡으므로 전자에 가깝다. 완화는 이미 걸려 있다: 두 항목 다 `linked` 무효화가 그 파일을 가리켜 바뀌면 stale 이 된다. **돌고 있는 워커가 옛 코드로 계속 도므로 지금 고치지 않는다.**
- Progress: **Lint 는 일부러 돌리지 않았다.** `LintReport` 는 stale 을 표시하며 `repo.save()` 를 호출하는데 ingest 워커가 같은 지식베이스에 쓰고 있어 **지난 세션이 워커 다섯으로 밟은 경합**을 다시 만든다. 모순·깨진 링크(=4.2 완주 판정)는 부트스트랩 완주 뒤에 잰다. `index.md`·`log.md` 가 비어 있는 것은 정상이다 — 둘 다 커밋 직전에 쓰인다.
- Progress: **부트스트랩은 완주하지 않았다 — 08-31 02:27:57 에 SIGTERM 으로 멈췄다.** 정상 종료다(`중단 요청 (signal=15). 묶음 경계에서 멈춘다.`). 커밋 `ac4d127`(신규 52 · 갱신 115)은 남았고 `ingest_checkpoint` 에는 `qna` 한 줄뿐 — **`source` 행 0**, 즉 커서를 옮기지 않았고 기다리던 완주 신호는 오지 않았다. provenance 61건이 전부 `auto-trading` 미러 HEAD(`15098666`)라 **`standard_ai_workflow`(커밋 1108 · 경로 1984)는 항목 0건**이다. 두 소스 중 하나도 못 끝냈다.
- Progress: **묶음의 48%(62/128)가 버려졌다.** 빈 응답 48 · 만들다 잘린 JSON 11 · 모델이 JSON 대신 되물음 3(`I have the compaction summary…` — pi 쪽 컨텍스트 압축) · `items` 대신 `create` 1 · 타임아웃 1 · SIGTERM 2. 파서는 무죄다(`extract_json` 이 울타리를 훑고 중괄호 균형으로 최외곽을 찾는다) — **출력이 애초에 완결되지 않았다.** 다만 이 표본은 **이미 고쳐진 코드**의 것이다: 워커 기동 23:12:22, `--no-tools` 를 넣은 `01087b1` 이 23:14:13 로 **1분 52초 늦었다.** 로그의 `Let me explore the…` 가 그 증거다.
- Progress: **재시도를 넣었다 (`f3f4db4`).** `IngestAgent` 가 `AgentOutputError` 만 `DEFAULT_ATTEMPTS=3` 까지 다시 부른다. `HarnessError` 는 다시 부르지 않는다 — 중단 신호(`code=143`)와 타임아웃이 그 길로 오고, 다시 부르면 종료를 방해하거나 같은 시간을 한 번 더 태운다. 재시도마다 워커 로그에 한 줄 남긴다(조용히 덮으면 실패율이 올라가도 성공 건수만 봐서는 안 보인다). 시험 5개 추가, 기존 셋은 **의미 있게 깨졌다** — "이 호출은 실패한다"를 응답 하나로 쓰면 두 번째 시도가 성공해 버려서, `conftest.failing()` 이 횟수를 상수에서 끌어오게 고쳤다. 1076→1081.
- Progress: **`PiHarness` 가 stdin 을 물려줘 pi 가 표준입력을 기다린다 (같은 커밋에서 닫았다).** 프롬프트는 이미 인자로 다 줬는데도 그렇다. 실측: 물려주면 90초 한도를 그대로 태우고, `stdin=DEVNULL` 이면 같은 프롬프트가 **1.2초**. EOF 인 stdin 은 무해해서 앞선 실행에서는 드러나지 않았다 — **48% 실패의 원인은 아니다**(그랬다면 128건이 전부 타임아웃이었을 것이다). 파이프로 띄우는 순간 터지는 잠복 결함이고, 재는 과정에서 걸렸다.
- Progress: **묶음 크기는 줄이려다 실측을 보고 그만뒀다.** stdin 을 고친 뒤 실제 미러 파일로 재니 24,000 · 12,000 · 6,000자가 **56 · 48 · 52초**로 거의 같았고 **셋 다 완결된 JSON** 이 왔다. 소요는 입력이 아니라 생성량이 지배한다 — 줄이면 호출당 시간은 그대로인 채 호출 수만 늘어 부트스트랩이 느려진다. 판단 근거를 `MAX_CHARS_PER_CALL` 주석에 남겼다. 타임아웃만 300→600초(한도 넘는 파일 하나가 통째로 묶음이 되는 경우 대비). **소요 재추정: 묶음당 56초면 770묶음이 약 12시간** — 인계 문서의 40시간대는 도구로 턴을 소진하던 때의 값이다.
- Progress: **Lint 가 처음으로 섰다 — `93be017 lint: 모순 0 · 소견 0 (새로 연 것 0) · stale 0`.** 정지 직전 워커가 스스로 돌려 커밋했다(내가 경합을 피해 미뤄 둔 그 검사다). 항목 52개 기준이라 **완주 판정은 아니지만**, `LintReport.clean` 이 서는 것을 처음 확인했다. 같은 실행에서 임베딩은 실패했다 — `MiniMax 임베딩 실패 (status_code=1002): rate limit exceeded(RPM)`. 검색은 키워드·표현 사전으로 계속 돈다(ADR-004).
- Progress: **재시작 완료 (08-31 22:5x).** `PYTHONUNBUFFERED=1 nohup uv run asd-worker > var-worker.log 2>&1 < /dev/null &`. 첫 기동은 로그가 블록 버퍼링돼 진행이 안 보여 즉시 다시 띄웠다(커서 미이동이라 잃은 것 없음). 정지에 6분 걸렸다 — **SIGTERM 은 묶음 경계에서만 듣는다**(그때 pi 가 4분째 돌고 있었다). 옛 로그는 `var-worker.log.*.bak` 로 남겼다.
- Next session starting point: 재시작한 부트스트랩 완주 대기(추정 약 12시간) → 커서에 source: 두 줄이 뜨면 (a) 죽은 ref 비율 (b) 지식이 모 시스템 것만인지 (c) Lint 안정으로 4.2 완주 판정. 도는 동안 볼 것: `grep '다시 부른다' var-worker.log | wc -l` 로 재시도가 무엇을 덮고 있는지, `grep 'ingest 실패' var-worker.log | wc -l` 로 48% 가 실제로 내려갔는지
- Remaining risks: 이전 실행들의 지식은 신뢰할 수 없다 — 워커 다중 실행과 에이전트 오염이 섞였다. 이번 실행이 첫 유효 측정이다. pi 가 판올림되며 기본 능력이 늘면 NON_AGENTIC 목록을 다시 봐야 한다.
- Remaining risks: 주기형 무효화 축은 커밋 0인 동안 한 번도 검사되지 않았다 — **4.2 를 판정할 때 이 축은 '통과'가 아니라 '미측정'이다.**
- Remaining risks: **재시도 3회로도 빈 응답이 남는다** — 재시작 직전 실행에서 한 묶음이 세 번 다 빈 응답이었다(`ingest 실패 — 소스 4df5e1ee`). 도구를 끈 코드에서도 나온 것이라 pi/MiniMax 쪽 원인이 따로 있다. 완주 뒤 재시도 로그와 최종 실패 수의 비로 크기를 잰다.
- Remaining risks: **커밋 메시지 126건이 `MAX_COMMIT_MESSAGES=50` 을 넘어 실리지 않았다** — 최초 부트스트랩에서만 닿는 한도라고 주석이 예고한 그대로다. 그만큼 '왜'가 빠진 채 지식이 지어진다.
- Remaining risks: FR-9 의 두 필터가 **INI/CamelCase 형식 설정 파일 전반**을 못 잡을 수 있다 — systemd 로 확인했을 뿐 `.ssh/config` · `.gitconfig` · `.editorconfig` 등 같은 모양이 더 있다. 완주 뒤 항목 전수를 이 모양으로 다시 훑어야 한다.

## ✅ Outcome

- Result: uv run pytest tests -q → 1076 passed. 라이브: SIGTERM 22:53:32 → 종료 22:56:17 로 정지 확인. 재시작 후 워커 1개, ingest 실패 0.
- Verification: uv run pytest tests -q → 1076 passed. 라이브: SIGTERM 22:53:32 → 종료 22:56:17 로 정지 확인. 재시작 후 워커 1개, ingest 실패 0.
- Follow-up:
