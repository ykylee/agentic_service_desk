---
id: TASK-2026-09-01-main-001
status: done
created_at: 2026-09-01
source_anchor: generic-task-2026-09-01-main-001
source_path: backlog/2026-09-01.md
kind: generic
wbs: exempt
wbs_exempt_reason: 실데이터 검증에서 드러난 결함 수정. 신규 기능이 아니라 기존 4.x 산출물의 정확성 보강이라 WBS leaf 를 새로 세우지 않는다
---

# TASK-2026-09-01-main-001 — pin() 이 커밋에 없는 항목을 고정하지 못하게 한다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-01
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 게재 근거를 고정할 때 head 유무만 보고 그 커밋에 항목이 실재하는지는 보지 않는다. 긴 ingest 런 도중 디스크에만 있는 항목이 재현 불가 해시로 박힌 채 나갈 수 있다 (ADR-002 결정 3·4).
- Completion criteria: pin() 이 대상 커밋에 실재하지 않는 항목을 만나면 게재를 거부한다 (UNPINNABLE 계열)
- Completion criteria: 회귀 시험: 디스크에만 있는 항목을 근거로 넣으면 publish 가 거부되고 answer_record 가 남지 않는다

## 🛠️ Implementation / Content

- Progress: 2026-09-01 07:4x 실측: 디스크 항목 204개 중 HEAD(93d52a16) 커밋에 실재하는 것은 71개. 나머지 148개가 근거로 잡히면 존재하지 않는 위치로 고정된다. pin() 은 본문을 디스크에서 읽고 해시는 repo.head() 에서 가져오는데, 방어가 head is None 만 본다. 읽기 전용 검증에서 질의 5개 중 5개가 근거를 찾았고 pin() 은 전부 성공했다 — 즉 지금 답하면 실제로 나간다. S0 이고 QnA 가 mock 이라 유입이 없어 현재는 잠재 위험이다.
- Progress: **고쳤다 (2026-09-01, 시험 1088→1090).** `KnowledgeRepository.exists_at(path, commit)` 을 더해 `git cat-file -e <commit>:<rel>` 로 그 커밋에 실재하는지 묻고, `pin()` 이 하나라도 없으면 **`None` 을 돌려 전체를 거부**한다. 일부만 빼지 않는 이유: 그 답변의 근거 목록은 이미 신뢰할 수 없는데 남은 것으로 답이 나가 버린다. `Refusal.UNPINNABLE` 메시지도 두 경우를 함께 말하도록 고쳤다 — "커밋이 없거나, 근거 항목이 아직 그 커밋에 들어 있지 않다".
- Progress: **회귀 시험 둘.** (1) 커밋된 `k-1` 과 커밋되지 않은 `k-2` 를 근거로 게재하면 `UNPINNABLE` 로 거부되고 모 시스템에 나가지 않으며 **`answer_record` 도 남지 않는다.** (2) 검사가 너무 넓게 잡아 멀쩡한 게재까지 막지 않는지 — 커밋된 항목만 근거면 정상 고정된다.
- Progress: **실지식베이스 확인 (읽기 전용).** 디스크 295개 전부가 HEAD `00479cb2` 에 실재해 정상 게재 경로는 영향받지 않는다. 이 결함이 드러났던 조건(런 도중 204개 중 71개만 커밋에 존재)은 부트스트랩이 완주하면서 해소됐지만, **다음 긴 런에서 다시 생긴다** — 그래서 고쳐 둔다.
- Next session starting point: (완료)
- Remaining risks: **근본 원인은 남아 있다** — `IngestRun` 이 런 끝에 한 번만 커밋하므로 긴 런 도중에는 계속 디스크와 커밋이 어긋난다. 이제는 조용히 거짓 해시를 박는 대신 **게재를 거부**하지만, 부트스트랩 중에는 답변이 전면 중단된다는 뜻이기도 하다. 커밋 시점과 커서 전진을 분리하는 제안은 `TASK-2026-08-30-main-002` 에 있다.

## ✅ Outcome

- Result: **완료.** `pin()` 이 대상 커밋에 실재하지 않는 항목을 만나면 `UNPINNABLE` 로 게재를 거부한다. 회귀 시험이 거부와 **`answer_record` 미기록**까지 센다. 1090 passed.
- Result: **검증**: 시험 1090 통과 + 실지식베이스(295개, HEAD `00479cb2`)에서 정상 고정 확인 — 검사가 멀쩡한 게재를 막지 않는다.
- Follow-up:
