---
id: TASK-2026-08-28-main-018
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-018
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.2.5
---

# TASK-2026-08-28-main-018 — WBS-4.2.5 모순 표시와 사람 편집 보존

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/knowledge/contradiction.py`
  - `src/agentic_service_desk/knowledge/human_edit.py`

- Description: 에이전트가 사람의 수정을 덮어쓰지 않고 양쪽을 남긴 채 모순(Q4)으로 올린다. 사람 편집은 세 조건(왜 고쳤는지·무효화 조건 갱신·사람 표시) 없이는 반영되지 않는다 (FR-6·54, D37·D38).
- Completion criteria: 충돌 시 덮어쓰지 않고 양쪽이 남는다 — 사람 것은 파일에, 에이전트 것은 모순 기록에 (FR-6)
- Completion criteria: 모순이 티켓(source=contradiction)으로 발행되어 Q4 대기열이 성립한다
- Completion criteria: 사람 편집 3조건이 없으면 커밋이 거부된다 (FR-54)
- Completion criteria: ingest 자신의 커밋은 그 검사를 지나지 않는다 — 우리 코드가 이미 불변식을 지킨다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 14:02` 기준 에이전트가 사람의 수정을 덮어쓰지 않고 양쪽을 남긴 채 모순(Q4)으로 올린다. 사람 편집은 세 조건(왜 고쳤는지·무효화 조건 갱신·사람 표시) 없이는 반영되지 않는다 (FR-6·54, D37·D38).
- Next session starting point:
- Remaining risks: 훅은 지식 저장소가 처음 만들어질 때 걸린다. 저장소를 손으로 clone 하거나 .git/hooks 를 지우면 검사가 사라진다 — 그때 편집이 조용히 통과한다. ensure_initialized 가 매번 다시 걸지만, 배치가 안 도는 환경에서는 걸리지 않는다

## ✅ Outcome

- Result: **덮어쓰지 않는 것과 없던 일로 하는 것은 다르다.** 에이전트의 판단을 contradiction 표에 보관한다 — 버리면 사람이 무엇과 어긋났는지 볼 수 없어 판정 자체가 불가능하다. 사람 쪽은 파일에, 에이전트 쪽은 기록에. 그것이 '양쪽을 남긴다'다
- Result: 대립 주장을 지식 파일이 아니라 운영 DB 에 둔 이유: 파일 본문에 넣으면 **다음 ingest 가 그것을 본문으로 읽는다.** 원천이 아닌 것이 원천처럼 되돌아오는 경로를 만들지 않는다 — 산출물 필터를 세운 것과 같은 이유
- Result: 모순은 티켓으로 발행된다(source=contradiction, qna_item_id 없음). 티켓 출처가 넷이라는 §6.4.1 이 여기서 실제로 쓰인다. **같은 항목에 쌓이지 않는다** — ingest 는 주기마다 도는데 매번 티켓을 찍으면 대기열이 같은 항목으로 메워져 1인 겸업이 소화할 수 없다
- Result: 판정 시 kept_human / took_agent / merged 를 남긴다. 에이전트가 자주 이기면 사람 편집이 틀리고 있다는 뜻이고 반대면 ingest 프롬프트가 틀리고 있다는 뜻이다 — 분포가 곧 지표다
- Result: **FR-54 를 commit-msg 훅으로 강제한다.** 요구사항이 '셋 없이는 편집이 반영되지 않는다' 이므로 커밋을 막는 것이 그 말 그대로이고, 작업 트리는 그대로 남아 아무것도 잃지 않는다. 사유는 길이·상투어로도 본다 — 빈 메시지만 막으면 -m '수정' 한 번으로 조건 1 이 무력해진다
- Result: 시험 중 버그 셋을 잡았다: ① git 이 비 ASCII 경로를 이스케이프해 내놓아 **한국어 제목 항목이 전부 검사에서 빠지고 있었다**(-z 로 해결) ② pre-commit 훅은 이번 커밋 메시지를 볼 수 없어 직전 ingest 커밋 메시지를 읽고 **검사를 통째로 면제**하고 있었다(commit-msg 로 이동) ③ 훅이 asd 콘솔 스크립트를 불러 가상환경 밖에서 'not found' 로 죽었다(sys.executable 을 박음)
- Verification: uv run pytest -q — 259 passed (기존 239 + 신규 20). 가상환경 밖 순수 셸에서 수동 검증: ① git commit -m '수정' → 세 조건 모두 위반으로 거부 ② 사유만 채움 → 조건 2·3 남아 거부 ③ 셋 다 채움 → 커밋 성공. 작업 트리 수정은 매번 그대로 남았다. ingest 커밋은 --no-verify 로 지나간다.
- Follow-up: WBS-4.2.6 Lint — 모순 대기열이 생겼으므로 '미해결 모순 0' 이 4.2 완료 조건의 절반이다. repo.scan() 의 깨진 파일 목록과 출처 커밋 실재 검사(ADR-002 결정 4)가 나머지
- Follow-up: WBS-4.2.7 대시보드 — contradiction.list_open() 이 Q4 대기열의 자료다. §8.5.5 대로 화면은 지목만 하고 편집은 평소 도구로
- Follow-up: 답변 생성 시 '이 항목에 열린 모순이 있는가'를 봐야 한다 (§8.2 Q4 방치 비용 = 모순된 지식이 계속 답변에 쓰인다). 지금은 조회 경로가 없다 — WBS-4.4 파이프라인의 몫
