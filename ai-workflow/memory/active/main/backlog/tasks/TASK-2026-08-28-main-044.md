---
id: TASK-2026-08-28-main-044
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-044
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.6.3
---

# TASK-2026-08-28-main-044 — WBS-4.6.3 문서 면 게재

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/content/publication.py`
  - `src/agentic_service_desk/operations/schema.py`
  - `src/agentic_service_desk/web/app.py`

- Description: 승인된 콘텐츠를 모 시스템 문서 면(upsert)·발행 면(create)으로 내보낸다 (XR-6, §7.7).
- Completion criteria: 승인된 살아있는 문서가 문서 면에 upsert 된다
- Completion criteria: 발행물은 발행 직전 최종 확인 없이 나가지 않는다
- Completion criteria: 게재 출구가 하나임을 시험이 센다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 23:36` 기준 승인된 콘텐츠를 문서 면·발행 면으로 내보낸다 (XR-6).
- Next session starting point:
- Remaining risks: 게재 실패가 화면 한 줄로만 알려진다 — 대기열에 남지 않으므로 운영자가 그 줄을 놓치면 다음 배치까지 모른다. 문서 면은 배치가 스스로 낫지만 발행 면의 미확정은 Q3 화면을 열어야 보인다.
- Remaining risks: T4 는 지금 표면이 없어서 막혀 있다. 나중에 문서 읽기 표면이 생기면 **산출물 필터가 그것을 함께 봐야 한다** — 지금은 그 배선이 없다.

## ✅ Outcome

- Result: **출구는 하나다** — upsert_document·create_publication 을 부르는 파일이 content/publication.py 하나뿐임을 시험이 센다. 답변 게재에서 쓴 방식 그대로다.
- Result: **in_flight 를 닫는 방법이 자리마다 다르다**(D46). 문서 면은 upsert 라 멱등하므로 결과를 몰라도 다음 주기가 그냥 다시 보내고, 발행 면은 다시 보내면 회차가 둘 생겨 사람이 확인한다. 답변 게재의 조심성을 멱등한 연산에까지 옮기면 **고칠 수 있는 것을 사람에게 미룬다.**
- Result: 발행물은 최종 확인 없이 나가지 않는다(§5.5.5). 인자에 기본값을 두지 않았다 — 기본값이 있으면 언젠가 누군가 넘기지 않고 부른다. 배치도 대신 누르지 않는다.
- Result: 승인과 게재를 나눴다. 게재는 모 시스템에 닿아야 해서 실패할 수 있고, 실패해도 승인은 남아 다음 배치가 다시 시도한다 — 연동이 없어도 판정은 되어야 한다(그러지 않으면 대기열이 막힌다).
- Result: 귀속과 근거를 본문에 싣는다(PO-2 와 같은 이유). 조립된 본문을 그대로 기록해 **무엇이 나갔는지와 무엇을 남겼는지가 갈리지 않게** 했다.
- Result: T4(자기 참조)는 **표면의 부재로 막힌다** — 모 시스템에서 문서를 읽어 오는 표면이 없어(§9.8.1 여섯 표면) 게재한 콘텐츠가 수집을 거쳐 ingest 로 돌아올 경로 자체가 없다. 시험이 그 사실을 센다. 승격 쪽은 PROMOTABLE_SOURCES 가 막는다.
- Result: 자리표를 실제 표로 바꿨다. 골격 단계의 content_publication 은 열 여섯짜리였고 **한 번도 쓰인 적이 없어** DROP 후 다시 만들었다 — 열을 하나씩 맞추는 것보다 읽기 쉽다.
- Result: 라이브 결함 ①: 판정 뒤의 초안을 다시 읽지 않아 게재 관문이 '승인되지 않았다'며 막았다 — 방금 승인한 사람에게는 이유를 알 수 없는 거절이다. ②: 근거 버전 고정을 배치에서만 박아, 보통의 경로인 화면 승인에서는 **그 칸이 늘 비어 있었다.**
- Verification: uv run pytest -q — 834 passed (신규 26). 라이브: 제작 → 승인 → 문서 면 게재 → 갱신 → 재게재까지 화면으로 돌렸고 **같은 경로를 덮었다**(멱등). asd migrate 로 v7 → v8 통과. 자리표였던 content_publication 을 실제 표로 바꿨다.
- Follow-up: WBS-4.7 S5 — FAQ · 칼럼 · 뉴스레터. QnA 통계 읽개와 P6·P7 의미 판정이 거기서 붙는다.
