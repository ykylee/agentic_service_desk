---
id: TASK-2026-09-02-main-009
status: done
created_at: 2026-09-02
source_anchor: generic-task-2026-09-02-main-009
source_path: backlog/2026-09-02.md
kind: generic
wbs: M-005/WBS-5.6.1
---

# TASK-2026-09-02-main-009 — WBS-5.6.1 묶음 단위 커밋과 원자적 쓰기

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-02
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/ingest/run.py`
  - `src/agentic_service_desk/knowledge/serialize.py`

- Description: 긴 ingest 런 중에 게재가 전면 중단된다. 커밋을 묶음마다 남겨 어긋남을 한 묶음으로 줄인다
- Completion criteria: 긴 런 중에도 게재가 성립한다 — 근거 항목이 한 묶음 안에 커밋된다
- Completion criteria: 파일 쓰기가 원자적이라 쓰는 중에 읽어도 MalformedItem 이 나지 않는다
- Completion criteria: 중단된 런이 이력에 남아도 완주와 구분된다

## 🛠️ Implementation / Content

- Progress: **공존이 깨지는 지점은 게재 하나다.** 검색·초안은 `scan()` 이 작업 트리를 읽어 긴 런 중에도 돈다(오히려 최신이다). 그런데 `pin()` 은 근거를 **HEAD 커밋**에 고정하는데 `IngestRun` 이 런 끝에 한 번만 커밋하므로, 부트스트랩 5~6시간 동안 답변이 하나도 못 나간다 — 2026-09-01 에 `UNPINNABLE` 로 거부하게 만든 것의 다른 면이다. 증분은 묶음 1개(2~3분)라 문제의 크기는 **부트스트랩과 대규모 재적재에만** 있고, 모 시스템이 크면 그것이 하루를 넘긴다.
- Progress: **요구사항을 고쳐야 했다 — FR-5 가 "1 회 ingest = 1 커밋"이다.** 계획 단계에서 "요구사항 번호에 묶여 있지 않다"고 판단했는데 **틀렸다**(FR-53 을 보고 FR-5 를 놓쳤다). 개정하고 이유를 `docs/REQUIREMENTS.md` §4.1 아래 주로 남겼다 — **바뀐 것은 커밋의 단위이지 이력의 성격이 아니다.** llm-wiki 가 말한 "무엇을 읽어 무엇이 바뀌었는가를 한 덩어리로 남긴다"(O30)의 몫은 `log.md` 의 한 줄이 계속 진다.
- Progress: **묶음마다 커밋한다** (`_commit_chunk`). 메시지에 **진행이 들어간다** — `ingest: auto-trading 43/116 — 신규 3 · 갱신 1`. 중간에 죽은 런의 결과도 이력에 남는데, 진행 표시가 있어야 마지막 ingest 커밋만 보고 **완주 여부를 가릴 수 있다.** 아무것도 못 낸 묶음은 빈 커밋을 만들지 않는다(기존 규칙 그대로).
- Progress: **`write_item` 을 원자화했다** — 같은 디렉터리에 `*.md.part` 로 쓰고 `os.replace` 로 바꿔 끼운다. 질의는 `scan()` 으로 작업 트리를 매번 통째로 읽는데 `write_text` 는 자리에 바로 쓰므로, 그 사이에 읽으면 **쓰다 만 내용**이 `MalformedItem` 이 되어 **없는 고장이 대기열에 오른다.** 접미사가 `.md` 로 끝나지 않아 `rglob("*.md")` 에도 안 걸린다.
- Progress: **지식 저장소에 `.gitignore`(`*.part`) 를 둔다.** 바꿔 끼우기 직전에 죽으면 임시 파일이 남는데 `commit()` 이 `git add -A` 라 다음 커밋이 그것까지 담는다. 워커 재기동 후 실제로 생성되는 것을 확인했다.
- Progress: **핵심 회귀 시험 하나가 이 변경의 이유다** — `test_런_도중에도_근거를_고정할_수_있다`. 두 번째 묶음이 도는 시점에 첫 묶음의 항목이 **커밋에** 있는지 `git ls-tree HEAD` 로 직접 본다. 없으면 그 사이에 온 질문은 근거를 고정하지 못해 게재가 거부된다.
- Next session starting point: (완료)
- Remaining risks: **중단된 런의 읽다 만 상태가 이력에 남는다.** 커서는 여전히 전진하지 않아 다음 주기가 다시 읽고 갱신하지만, 이력만 보면 완주와 구분하려면 **커밋 메시지의 진행 표시를 읽어야 한다.** 자동으로 판정하는 장치는 없다.
- Remaining risks: **부트스트랩 이력이 길어진다** — 116묶음이면 ingest 커밋이 최대 116개다. 근거 버전 고정에는 오히려 유리하지만, 대시보드의 "최근 커밋" 목록은 이제 **런이 아니라 묶음**을 보여 준다.

## ✅ Outcome

- Result: **긴 런 중에도 게재가 성립한다.** 디스크와 커밋의 어긋남이 런 전체(실측 4.96시간)에서 **최대 한 묶음**(중앙값 약 2분)으로 줄었다.
- Result: **FR-5 개정** — 커밋 단위를 런에서 묶음으로. 검증 기준도 함께 고쳤다("묶음마다 커밋이 생기고 커밋 메시지가 진행을 담는다"). 딸린 문장 일곱 곳(요구사항·로드맵·모듈 docstring·시험 주석)을 맞췄다.
- Result: **원자적 쓰기로 `broken_files` 오탐이 사라진다** — 쓰는 중에 읽어도 옛 내용이나 새 내용 중 하나만 보인다.
- Verification: `uv run pytest -q` **1145 passed** (신규 6). 워커를 새 코드로 재기동해 지식 저장소에 `.gitignore` 가 실제로 생기는 것을 확인했다.
- Follow-up: 대시보드의 "최근 커밋"이 이제 묶음 단위라 화면 문구를 손볼 여지가 있다 — docstring 은 고쳤고 화면 라벨은 그대로다.
