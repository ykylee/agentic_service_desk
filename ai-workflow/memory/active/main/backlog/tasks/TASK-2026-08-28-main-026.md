---
id: TASK-2026-08-28-main-026
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-026
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.4.1
---

# TASK-2026-08-28-main-026 — WBS-4.4.1 검색

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/knowledge/search.py`

- Description: 지식베이스 조회 — 키워드와 임베딩을 합치고 LLM 이 재랭킹한다. 사용자 표현 사전으로 어휘 격차를 메운다 (ADR-004, O31, §2.5.4).
- Completion criteria: 키워드 검색이 조사 변형을 넘어 찾는다 — 언어별 도구를 쓰지 않는다 (ADR-003 제약 1)
- Completion criteria: 표현 사전이 어휘 격차를 메운다 — QnA 가 쌓일수록 좋아진다 (§2.5.4)
- Completion criteria: 임베딩과 LLM 재랭킹이 없어도 검색이 돈다 — 개발 환경에서 임베딩이 막혀 있다 (O57)
- Completion criteria: 근거를 못 찾으면 빈 결과를 준다 — 지어내지 않는다 (FR-18 의 입력)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 15:43` 기준 지식베이스 조회 — 키워드와 임베딩을 합치고 LLM 이 재랭킹한다. 사용자 표현 사전으로 어휘 격차를 메운다 (ADR-004, O31, §2.5.4).
- Next session starting point:
- Remaining risks: 라이브 표본에서 표현 사전만으로 찾는 경우를 분리해 보이지 못했다. ingest 에이전트가 이용자 표현을 본문에 그대로 옮겨 키워드도 함께 맞았기 때문이다 — 나쁜 일은 아니지만 사전의 값은 원천이 갈릴수록 드러나므로 실데이터로 봐야 안다. 격리된 경우는 단위 시험이 덮는다
- Remaining risks: 임베딩 다리가 라이브에서 한 번도 성공하지 못했다. O57 이 열려 있는 한 하이브리드의 한 다리는 미검증이며, 벡터 차원조차 확인되지 않았다
- Remaining risks: 검색이 매 질의마다 지식 저장소를 통째로 읽는다. 수백~수천 항목이면 견디지만 파일 I/O 라 온라인 경로의 지연에 그대로 더해진다 — §8.6.3 이 경고한 답변 지연에 폴링 주기와 함께 쌓인다

## ✅ Outcome

- Result: 언어별 도구 없이 조사 변형을 넘는다 — 양방향 부분 일치다. 질의가 '결재를'이고 문서가 '결재'일 수도, 그 반대일 수도 있어 한쪽만 보면 절반을 놓친다. 형태소 분석기를 들이면 ADR-003 제약 1(언어 수만큼 도구를 만들지 않는다)이 깨진다
- Result: 표현 사전에 별도 추출 에이전트를 두지 않았다. 연결이 이미 있다 — QnA 에서 온 지식 항목은 provenance.qna 로 원래 질문을 가리키므로 그 질문의 표현이 곧 그 개념을 부르는 말이다. 없는 장치를 만드는 대신 있는 것을 읽는다. 수동 등록 질문도 이용자가 쓴 말이므로 함께 들어간다
- Result: 임베딩과 LLM 이 없어도 검색이 돈다. 개발 환경에서 임베딩이 막혀 있고(O57) 1국면에는 표현 사전이 비어 있어(D14), 어느 하나가 빠졌다고 서면 그 환경에서 아무것도 시험할 수 없다. 제공자가 터져도 키워드 다리는 살아 있다
- Result: 못 찾으면 빈 목록이다. 낮은 점수 항목을 끼워 넣으면 근거 없이는 답을 만들지 않는다는 FR-18 이 무력해진다
- Result: 재랭킹이 후보를 버리지 않는다. 모델이 빠뜨린 것은 뒤에 붙이고 지어낸 id 는 무시한다 — 버리면 근거가 있는데 없다고 판정되어 FR-18 이 잘못 발동하고, 지어낸 근거가 답변에 실리는 것은 §2.2.3 이 경계한 고장이다
- Result: 벡터를 운영 DB 에 둔다. 지식 저장소는 git 이라 생성물을 커밋하면 diff 가 벡터로 가득 차 '어느 커밋이 지식을 바꿨는가'가 읽히지 않는다
- Verification: uv run pytest -q — 440 passed (기존 413 + 신규 27). 라이브: 실제 ingest 로 만든 지식 2건에 대해 조사 변형('결재를' → '결재')·식별자 검색 동작 확인, 무관한 질의는 빈 결과(FR-18 입력). LLM 재랭킹이 실제로 순서를 바꿨다 — 키워드가 '결재 한도'를 1위로 뒀는데 휴가 질문에 대해 모델이 '휴가 신청 임시저장 조회'를 앞으로 올렸다. 임베딩은 예상대로 레이트 리밋(O57)이었고 배치가 그것을 알리고 계속 갔다.
- Follow-up: WBS-4.4.2 파이프라인 1~3단계 — 분석(질문 언어 판정 FR-17) · 조회(이 검색) · 생성. 근거 0건이면 답을 만들지 않고 티켓으로 보내는 FR-18 이 여기서 집행된다
