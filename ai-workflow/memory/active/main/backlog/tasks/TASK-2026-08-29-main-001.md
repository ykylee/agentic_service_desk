---
id: TASK-2026-08-29-main-001
status: done
created_at: 2026-08-29
source_anchor: generic-task-2026-08-29-main-001
source_path: backlog/2026-08-29.md
kind: generic
wbs: M-004/WBS-4.7.1
---

# TASK-2026-08-29-main-001 — WBS-4.7.1 FAQ

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-29
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/content/qna_stats.py`
  - `src/agentic_service_desk/content/production.py`
  - `src/agentic_service_desk/worker/runner.py`
  - `tests/test_faq.py`

- Description: 반복 질문 탐지에서 FAQ 를 생성·갱신한다. 무엇을 다룰지는 QnA 통계가, 무엇에 기대어 쓸지는 지식베이스가 정한다 (FR-37, §7.2, §5.3).
- Completion criteria: 반복 질문이 임계에 닿으면 FAQ 초안이 만들어져 Q3 에 오른다
- Completion criteria: 봇 답변·미해결 질문도 반복 세기에 포함되되 그 답변이 FAQ 본문이 되지 않는다 (§5.3)
- Completion criteria: 반복되는데 근거가 없는 질문은 지식 공백으로 남고 지어내지 않는다

## 🛠️ Implementation / Content

- Progress: `2026-08-29 00:25` 기준 반복 질문 탐지에서 FAQ 를 생성·갱신한다. 무엇을 다룰지는 QnA 통계가, 무엇에 기대어 쓸지는 지식베이스가 정한다 (FR-37, §7.2, §5.3).
- Next session starting point:
- Remaining risks: churn 이 FAQ 에서는 구조적으로 높게 나온다. 문항 추가가 정상 성장인데 churn 은 추가된 줄도 변경으로 세어 라이브 갱신이 73% 로 나왔다 — 순수 추가였다. 검수자가 '다시 쓰기' 신호로 오해할 수 있다.
- Remaining risks: 재료가 있어도 첫 제작이 전부를 다루지 않는다. 라이브 1회차에서 covered 2건 중 하나만 문항이 됐고 나머지는 갱신에서 들어왔다 — 근거를 지어내지 않았으므로 결함은 아니지만, '만들었으니 다 다뤘다'로 읽으면 안 된다.
- Remaining risks: 군집화가 어휘로만 돈다. 표현이 완전히 다른 같은 질문(어휘 격차)은 갈린다 — 검색에는 임베딩 다리가 있지만 여기에는 없다. 실데이터로 SIMILARITY 를 다시 볼 값이다.
- Remaining risks: 반복 질문이 지식 공백으로 남아도 Q8 에 오르지 않는다. 배치 로그로만 알려지며, Q8 은 여전히 미해결 종료 QnA 만 본다 — 되먹임 경로를 잇는 것은 별도 결정이다.

## ✅ Outcome

- Result: 주 입력이 정하는 것과 조회가 정하는 것을 나눴다. 반복 질문 분포가 **무엇을 다룰지**를, 지식베이스 검색이 **무엇에 기대어 쓸지**를 정한다 — 봇의 미검증 답변을 FAQ 본문으로 옮기면 §5.3 이 막으려던 되먹임이 ingest 보다 나쁜 자리(이용자에게 바로 나가는 자리)에서 일어난다.
- Result: §5.3 의 표를 그대로 집행했다 — 반복 질문 탐지는 산출물 필터를 지나지 않는다. 봇이 답했든 아무도 답하지 않았든 같은 질문이 세 번 들어온 것은 사실이다. 필터를 통과시키면 대부분의 QnA 가 통계에서 사라지고 FAQ 는 영영 자라지 않는다.
- Result: 개인정보를 애초에 읽지 않는다 (PO-3). asker_account 를 꺼내 오지 않고, 질문 본문의 개인·상황 요소는 프롬프트가 일반화를 요구한다 — 라이브에서 '제 결재 한도가 300만원인데...'가 '결재 한도는 어떻게 정해지나요'로 일반화됐다.
- Result: 임계 하나가 트리거이자 후보 자격이다 (O37). repeat_questions=3 은 '가장 많이 반복된 질문의 횟수'이고, 그 숫자가 곧 후보로 볼 반복 횟수다 — 둘을 다른 숫자로 두면 돌긴 도는데 재료는 없는 주기가 생긴다.
- Result: 모르는 임계는 조용히 '안 찼다'가 되지 않는다 (UnknownThreshold). False 로 두면 주기 없는 타입이 영영 돌지 않는데 그 침묵은 '아직 안 찼다'와 구분되지 않는다.
- Result: 라이브 결함: 겹침 비율을 양쪽의 최솟값으로 재던 것이 같은 주제를 갈랐다 — '결재 한도가 어떻게 정해지나요'와 '결재 한도는 어떻게 정해지는지 알고 싶습니다'가 0.5 로 따로 묶였다. 짧은 쪽 기준으로 바꿔 0.75 가 됐다. 여기서는 **덜 묶는 쪽이 안전하지 않다** — 임계에 못 닿은 반복은 조용히 사라져 '물어본 사람이 없다'와 같은 모양이 된다 (O37).
- Verification: uv run pytest -q — 861 passed (신규 28, tests/test_faq.py). 라이브(pi 0.84.1 + MiniMax-M3): 반복 질문 3주제 × 3회 + 임계 미달 1건으로 첫 제작 → 승인 → 갱신까지 돌렸다. 결재/반려/휴가 문항이 근거와 함께 나왔고, 지식베이스가 모르는 VPN 반복 3회는 uncovered(지식 공백)로 남아 지어내지 않았다. 개인 표현('300만원인데')이 일반화돼 실렸다. 첫 회 클러스터링 결함을 잡아 고치고 재검증했다. /status 콘텐츠 현황과 /queues/Q3 에서 FAQ 를 화면으로 확인했다.
- Follow-up: WBS-4.7.2 칼럼 — both 읽개와 P6·P7 의미 판정이 거기서 붙는다
- Follow-up: 지식 공백으로 잡힌 반복 질문을 Q8 에 올릴 것인가 — ingest 우선순위 되먹임 경로 (§6.2)
