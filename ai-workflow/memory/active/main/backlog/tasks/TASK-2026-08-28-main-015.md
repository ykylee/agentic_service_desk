---
id: TASK-2026-08-28-main-015
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-015
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.2.2
---

# TASK-2026-08-28-main-015 — WBS-4.2.2 QnA 수집

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/ingest/qna.py`
  - `src/agentic_service_desk/operations/schema.py`
  - `src/agentic_service_desk/worker/runner.py`
  - `tests/test_qna_collect.py`

- Description: 모 시스템 QnA 이력(질문·답변·후속·해결 표시)을 분 단위 폴링으로 Raw Layer 에 적재한다. 답변의 작성자 계정과 해결 등급을 함께 보관한다 (FR-52, NFR-7).
- Completion criteria: Raw Layer 에 질문·답변·후속·해결이 적재되고 답변의 작성자 계정이 남는다
- Completion criteria: 해결 등급(explicit/implicit)이 보관되고 상향만 가능하다 (D8, §5.3.2)
- Completion criteria: 폴링이 재실행돼도 중복되지 않고, 커서는 적재가 끝난 뒤에만 옮긴다

## 🛠️ Implementation / Content

- Progress: `2026-08-28 13:17` 기준 모 시스템 QnA 이력(질문·답변·후속·해결 표시)을 분 단위 폴링으로 Raw Layer 에 적재한다. 답변의 작성자 계정과 해결 등급을 함께 보관한다 (FR-52, NFR-7).
- Next session starting point:
- Remaining risks: unsettled_question_ids() 는 끝내 해결 표시를 못 받은 질문을 계속 다시 조회한다. 일 단위 수십 건 규모라 지금은 문제없지만, 이 범위를 닫으려면 미해결 종료 판정이 필요하고 그것은 타임아웃 기간(O18)에 달려 있다 — WBS-4.5.4 의 몫

## ✅ Outcome

- Result: Raw Layer 4개 테이블(raw_question · raw_answer · raw_followup · raw_resolution) 추가. raw_answer.author_account 는 NOT NULL 이며 빈 문자열도 적재 단계에서 거부한다 — D7 이 스키마와 코드 양쪽에서 집행된다
- Result: 해결 등급을 raw_resolution.grade 에 보관하고 상향만 허용한다(§5.3.2). 강등 시도는 GradeDowngradeRejected 로 거부한다
- Result: 해결 방법이 비어 있는 해결 표시는 암묵으로 민다 — 잘못 부여된 명시적 해결은 틀린 지식을 만든 뒤에야 드러나므로 애매한 쪽을 안전한 방향으로 둔다
- Result: 새 질문만 보지 않는다 — 아직 명시적으로 해결되지 않은 저장분을 매 주기 다시 훑는다. 이것이 없으면 어제 질문에 오늘 달린 후속과 해결 표시를 영영 못 보고 ingest 자격이 열리지 않는다
- Result: QNA 커서는 **수집 지점**이라 적재 커밋 뒤에 옮긴다. 소스 커서(ingest 지점)와 의미가 다르며, ingest(WBS-4.2.4)는 Raw Layer 에서 다시 읽으며 자기 진행 지점을 따로 든다
- Verification: uv run pytest -q — 160 passed (기존 139 + 신규 21). ASD_PARENT_ADAPTER=mock 로 BatchRunner._tick() 2회 실행: raw_question 5 · raw_answer 4 · raw_followup 1 · raw_resolution 5, 2회차에 증가 없음(멱등), Q-2 grade=explicit/method=user_marked, 답변 작성자 계정으로 봇(svc-agentic-desk)/사람(emp-999) 구분 확인.
- Follow-up: WBS-4.2.3 산출물 필터 — OutputFilter.is_ingestible 이 아직 NotImplementedError 다. Raw Layer 가 채워졌으므로 이제 실제 데이터로 다섯 분기를 시험할 수 있다. 봇 계정 목록을 설정값으로 뺄 자리도 여기다
- Follow-up: raw_resolution.grade 와 qna_item.resolution_grade 의 동기화는 추적 상태를 다루는 WBS-4.5.4 에서 잇는다
