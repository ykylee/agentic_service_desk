---
id: TASK-2026-08-28-main-042
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-042
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.6.2
---

# TASK-2026-08-28-main-042 — WBS-4.6.2 가이드 제작

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/content/production.py`
  - `src/agentic_service_desk/content/store.py`
  - `src/agentic_service_desk/operations/schema.py`
  - `src/agentic_service_desk/operations/migrations.py`
  - `src/agentic_service_desk/worker/runner.py`

- Description: 지식베이스에서 시스템 사용 가이드를 만들고 갱신한다. 1차 언어 고정, 살아있는 문서의 갱신 (FR-36·43, D50).
- Completion criteria: 소스코드 파생 지식으로 가이드 초안이 만들어져 Q3 에 오른다
- Completion criteria: 갱신이 직전 판본을 고치고 diff 를 세어서 만든다
- Completion criteria: 언어가 1차 언어로 고정된다 (근거가 영어여도)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 22:36` 기준 지식베이스에서 시스템 사용 가이드를 만들고 갱신한다 (FR-36·43, D50).
- Next session starting point:
- Remaining risks: 인용의 원문 일치를 세지 않는다. 라이브에서 k-1 본문이 바뀌었는데 가이드가 옛 문장을 그대로 인용한 채 남았다 — 답변에는 _coverage 가 있지만 콘텐츠에는 없다. 4.6.4 검수가 볼 자리다.
- Remaining risks: Q3 건수는 세지만 /queues/Q3 화면과 티켓 배선이 없어 링크가 404 다 (4.6.4).
- Remaining risks: churn 에 임계를 두지 않았다 — 신호일 뿐 막지 않는다. 임계는 실데이터로 정할 값이다(O8 과 같은 종류).

## ✅ Outcome

- Result: 새 파이프라인을 만들지 않았다 — 답변과 같은 5단계를 트리거만 바꿔 탄다(D10, §7.1). 여기가 1~3단계이고 검수·게재는 4.6.4·4.6.3 이 잇는다.
- Result: 언어를 판정하지 않고 고정했다(FR-43). 근거 본문으로 판정하면 영문 지식이 늘었을 때 살아있는 문서 하나가 갱신마다 언어를 바꾼다 — 시험이 영어 근거로 그것을 센다.
- Result: 갱신은 직전 판본을 입력에 넣고 고친다. 백지에서 다시 쓰면 변경분 검수가 전문 검수와 같아진다 — diff 가 곧 전문이 되기 때문이다(§5.5.5). 직전 판본은 **승인된 것**만 쓴다: 승인 안 된 초안을 입력으로 삼으면 사람이 보지 않은 글 위에 다음 글이 쌓인다.
- Result: 무엇이 바뀌었는지 모델에게 묻지 않는다 — diff 는 셀 수 있으므로 세어서 만든다. 모델의 자기 신고는 검증할 수 없고 검수자가 믿고 넘기면 표시가 없는 것과 같다.
- Result: 낡은 근거는 빼되 **코드 변경이 부른 주기는 기다린다**. 답변은 낡은 근거라도 쓰고 강도를 내리지만 가이드는 무엇을 다룰지 고를 수 있다 — 다음 갱신에 들인다(§7.3 의 갱신이 W3 를 흡수한다). 다만 지식이 그 커밋을 아직 안 읽었는데 돌리면 같은 글이 나오고, 그것을 갱신이라 부르면 고쳤다는 기록만 남는다(§6.6.3 과 같은 순서).
- Result: 진행 표시를 나눴다 — last_run_at(본 시각)과 last_generated_at(모델을 실제로 돌린 시각). 기다리기로 한 주기가 시계를 밀면 한 번 미룬 타입이 꼬박 한 주기를 더 기다리고, 소스 커서를 먼저 옮기면 그 코드 변경이 아무것도 만들지 않은 채 소비된다.
- Result: 라이브 결함 ①: 직전 판본을 '# 제목' + 본문으로 넘겼더니 모델이 제목을 본문의 일부로 되받아 **갱신마다 제목이 한 줄씩 쌓였다.** 프롬프트에서 제목을 라벨로 분리하고, strip_title_heading 으로 코드가 지킨다.
- Result: 라이브 결함 ②: 갱신이 근거가 그대로인 문단까지 다시 썼다. 프롬프트로 막지 못하므로 **세어서 드러낸다** — churn(변경 줄 비율)을 결과와 run detail 에 싣는다. 라이브 재실행에서 15% 로 떨어졌다.
- Result: ADR-010 계단 006 — content_draft · content_run. SCHEMA_SQL 과 함께 고쳤고 asd migrate 의 선언 대조를 통과했다.
- Verification: uv run pytest -q — 778 passed (신규 30). 라이브(MiniMax-M3): 합성 지식 3건으로 가이드 첫 제작 → k-1 갱신 후 재실행까지 돌렸다. 결함 둘을 잡아 고쳤다. asd migrate 로 v5 DB 를 v6 로 올리고 선언 대조를 통과시켰다. /status 콘텐츠 현황과 Q3 건수를 화면에서 확인했다.
- Follow-up: WBS-4.6.3 문서 면 게재 — upsert_document 는 있다. **T4(자기 참조)를 여기서 함께 막아야 한다.**
- Follow-up: WBS-4.6.4 콘텐츠 검수 — Q3 화면, diff 검수, churn 과 인용 대조를 검수자에게 보여 주는 자리.
