---
id: TASK-2026-08-28-main-021
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-021
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.3.1
---

# TASK-2026-08-28-main-021 — WBS-4.3.1 티켓 도메인

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/operations/ticket.py`

- Description: 티켓의 다섯 상태와 전이를 세운다 — 자동종결/열림/진행중/보류/종결. QnA 항목과 별개 엔터티이며 상태가 서로를 결정하지 않는다 (FR-12·56, D15·D33, §6.7).
- Completion criteria: 다섯 상태만 존재하고 담당자·마감·우선순위 필드가 없다 (FR-12)
- Completion criteria: 허용되지 않은 전이가 거부된다
- Completion criteria: QnA 하나가 여러 티켓을 낳고, 한쪽이 닫혀도 다른 쪽이 열려 있다 (FR-56)
- Completion criteria: 보류는 대기열에 뜨지 않고 외부 응답이 오면 자동으로 열린다 (§6.7.1)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 14:42` 기준 티켓의 다섯 상태와 전이를 세운다 — 자동종결/열림/진행중/보류/종결. QnA 항목과 별개 엔터티이며 상태가 서로를 결정하지 않는다 (FR-12·56, D15·D33, §6.7).
- Next session starting point:
- Remaining risks: 보류 해제가 '후속이 하나라도 있으면' 으로 판정한다. 질문자가 아닌 사람(운영자 자신)이 남긴 후속도 응답으로 센다 — raw_followup.author_account 로 좁힐 수 있지만 누가 '외부' 인지의 정의가 아직 없다

## ✅ Outcome

- Result: **없는 것이 있는 것만큼 중요하다.** 담당자·마감·우선순위·SLA 필드를 두지 않았고 스키마에 그 열이 없다는 것을 시험이 지킨다 (FR-12). 정렬은 방치 비용 × 경과 시간이고, 경과 시간은 opened_at 에서 파생된다 — 약속이 아니라 관측이다
- Result: **종결과 자동종결은 종점이다.** 같은 질문이 또 오면 그것은 새로운 처리이고, QnA 하나가 여러 티켓을 낳을 수 있다는 것(FR-56)이 그 자리를 이미 마련해 뒀다. 되살리면 한 티켓에 여러 처리가 섞여 통계가 무너진다
- Result: **닫는 행위에 종결 기록을 묶었다** (§6.4.5). 1국면에서는 티켓 해결의 승격이 지식 성장의 주 경로인데, '완료' 체크로 닫으면 승격할 재료가 사라진다. 기록의 형식 검증은 WBS-4.3.2 의 몫이고 여기서는 존재만 요구한다
- Result: 보류 해제 판정에 ticket.state_at 열을 더했다. opened_at 으로 재면 **우리가 묻기도 전에 온 후속까지** 응답으로 세어 보류가 헛되이 풀린다. 비교의 양쪽을 모두 우리 시계(collected_at vs state_at)로 맞췄다 — created_at 은 모 시스템 시계라 섞으면 시계 차이만큼 어긋난다
- Result: 보류 해제를 배치가 수집 직후에 자동으로 한다. 응답을 알아채는 일까지 사람 몫이면 보류의 값이 사라진다 — 결국 열어 보게 되고 그것이 보류로 피하려던 반복되는 재판단이다 (§6.7.2)
- Result: contradiction.py 와 lint.py 가 티켓을 원시 SQL 로 만들던 것을 도메인 경유로 바꿨다. 상태를 아는 곳이 하나여야 전이 규칙이 새지 않는다
- Verification: uv run pytest -q — 326 passed (기존 303 + 신규 23). 라이브 상태 기계 완주: 발행(open, 대기열 1) → 진행중 → 보류(대기열 0) → 기록 없이 닫기 거부(ResolutionRequired) → 배치가 후속을 수집해 자동으로 열림(대기열 1) → 기록 후 종결 → 되살리기 거부(InvalidTransition).
- Follow-up: WBS-4.3.2 종결 기록 — 필수 4필드와 무효화 조건 강제 입력. ticket.transition 이 이미 기록의 존재를 요구하므로, 그 기록을 무엇으로 채워야 하는지가 4.3.2 다. 지금은 모순·Lint 티켓이 자리만 채운 한 줄을 넣고 닫는데 그것도 4.3.2 에서 형식을 갖춘다
- Follow-up: WBS-4.3.5 Q1 대기열 화면 — ticket.queue() 가 자료다. QUEUES 레지스트리에 Q1 자리가 이미 있고 S1 부터 뜬다
