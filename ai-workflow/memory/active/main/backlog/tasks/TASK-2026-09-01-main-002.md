---
id: TASK-2026-09-01-main-002
status: planned
created_at: 2026-09-01
source_anchor: generic-task-2026-09-01-main-002
source_path: backlog/2026-09-01.md
kind: generic
wbs: exempt
wbs_exempt_reason: 부트스트랩 완주 후의 품질 판정. 4.2 완주 판정에 딸린 측정이라 WBS leaf 를 새로 세우지 않는다
---

# TASK-2026-09-01-main-002 — 두 미러가 같은 kit 을 두 번 지식으로 만드는지 판정한다

## 📝 Description

- Status: planned
- Priority: high
- Request date: 2026-09-01
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: auto-trading 이 ai-workflow/ 아래 kit 사본을 품고 있어 지식의 48%가 kit 에서 나왔다. 남은 538묶음인 standard_ai_workflow 는 그 kit 의 원본이라 같은 개념을 다시 만들 수 있다.
- Completion criteria: 완주 후 제목 유사도와 본문 중복을 재어 중복 개념 수를 낸다
- Completion criteria: 중복이 유의하면 원천 구성을 정한다 — auto-trading 의 `ai-workflow/` 를 원천에서 제외할지, 미러 하나로 합칠지

## 🛠️ Implementation / Content

- Progress: `2026-09-01 08:53` 기준 auto-trading 이 ai-workflow/ 아래 kit 사본을 품고 있어 지식의 48%가 kit 에서 나왔다. 남은 538묶음인 standard_ai_workflow 는 그 kit 의 원본이라 같은 개념을 다시 만들 수 있다.
- Progress: **실측 (09-01 08:0x, 항목 204개 · auto-trading 미러 192/210 시점).** 출처 구성 — **kit(`ai-workflow/`) 98건 48%** · auto-trading 도메인 55건 27% · **근거 경로 없음 51건 25%**. 경로 영역 상위는 `ai-workflow/workflow_kit` 87 · `src/auto_trading` 61 · `ai-workflow/core` 22. 이 시점의 제목 유사도 0.72 이상 쌍은 **0개** — 중복은 아직 없다. 즉 판정은 두 번째 미러가 끝난 뒤에만 가능하다. 경로가 `ai-workflow/workflow_kit/x.py` 대 `workflow_kit/x.py` 로 달라 **같은 개념으로 인식해 갱신할지 보장이 없다.**
- Progress: **함께 관찰된 것 둘.** (1) **근거 경로 없는 항목 25%** — `source_provenance()` 가 원천에 실재하지 않는 경로를 걸러낸 결과라 방어는 작동했으나, 그 51개는 "이 커밋 어딘가"까지만 되짚을 수 있다. (2) **본문이 잘다** — 중앙값 822자, 1000자 미만이 63%, 제목이 절 번호로 시작하는 항목이 7개(`§16.3` · `§22.2` · `§24.3` 등). 원천에 설계 문서가 섞이면 모델이 **문서 구조를 그대로 따라가** ADR-003 의 "개념 단위로 굵게 잡는다"에서 멀어진다.
- Next session starting point: 부트스트랩 완주 후 중복 측정(제목 유사도 + 본문) → 결과에 따라 `ASD_PARENT_REPO_URL` 구성 재검토
- Remaining risks: **부트스트랩이 도는 동안은 원천 구성을 바꾸지 않는다** — 바꾸면 커서가 무효가 되어 748묶음을 처음부터 다시 읽는다.
- Remaining risks: 중복 판정을 제목 유사도만으로 하면 놓친다 — 같은 개념을 다른 제목으로 만들 수 있다. 본문·`used_paths` 도 함께 봐야 한다.

## ✅ Outcome

- Result:
- Follow-up:
