---
id: TASK-2026-09-01-main-002
status: done
created_at: 2026-09-01
source_anchor: generic-task-2026-09-01-main-002
source_path: backlog/2026-09-01.md
kind: generic
wbs: exempt
wbs_exempt_reason: 부트스트랩 완주 후의 품질 판정. 4.2 완주 판정에 딸린 측정이라 WBS leaf 를 새로 세우지 않는다
---

# TASK-2026-09-01-main-002 — kit 출처 지식을 어떻게 할 것인가 (원래 제목: 두 미러가 같은 kit 을 두 번 지식으로 만드는지 판정한다)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-01
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: auto-trading 이 ai-workflow/ 아래 kit 사본을 품고 있어 지식의 48%가 kit 에서 나왔다. 남은 538묶음인 standard_ai_workflow 는 그 kit 의 원본이라 같은 개념을 다시 만들 수 있다.
- Completion criteria: 완주 후 제목 유사도와 본문 중복을 재어 중복 개념 수를 낸다
- Completion criteria: **전제가 바뀌었다 (2026-09-01).** `standard_ai_workflow` 를 원천에서 물렸으므로 "두 미러가 같은 kit 을 두 번 만드는" 상황은 오지 않는다. 실제 물음은 **이미 만들어진 kit 출처 지식을 어떻게 할 것인가**로 옮겨갔다.
- Completion criteria: 중복이 유의하면 원천 구성을 정한다 — auto-trading 의 `ai-workflow/` 를 원천에서 제외할지, 미러 하나로 합칠지

## 🛠️ Implementation / Content

- Progress: `2026-09-01 08:53` 기준 auto-trading 이 ai-workflow/ 아래 kit 사본을 품고 있어 지식의 48%가 kit 에서 나왔다. 남은 538묶음인 standard_ai_workflow 는 그 kit 의 원본이라 같은 개념을 다시 만들 수 있다.
- Progress: **실측 (09-01 08:0x, 항목 204개 · auto-trading 미러 192/210 시점).** 출처 구성 — **kit(`ai-workflow/`) 98건 48%** · auto-trading 도메인 55건 27% · **근거 경로 없음 51건 25%**. 경로 영역 상위는 `ai-workflow/workflow_kit` 87 · `src/auto_trading` 61 · `ai-workflow/core` 22. 이 시점의 제목 유사도 0.72 이상 쌍은 **0개** — 중복은 아직 없다. 즉 판정은 두 번째 미러가 끝난 뒤에만 가능하다. 경로가 `ai-workflow/workflow_kit/x.py` 대 `workflow_kit/x.py` 로 달라 **같은 개념으로 인식해 갱신할지 보장이 없다.**
- Progress: **함께 관찰된 것 둘.** (1) **근거 경로 없는 항목 25%** — `source_provenance()` 가 원천에 실재하지 않는 경로를 걸러낸 결과라 방어는 작동했으나, 그 51개는 "이 커밋 어딘가"까지만 되짚을 수 있다. (2) **본문이 잘다** — 중앙값 822자, 1000자 미만이 63%, 제목이 절 번호로 시작하는 항목이 7개(`§16.3` · `§22.2` · `§24.3` 등). 원천에 설계 문서가 섞이면 모델이 **문서 구조를 그대로 따라가** ADR-003 의 "개념 단위로 굵게 잡는다"에서 멀어진다.
- Progress: **중복 측정 결과 — 0쌍.** 항목 295개 전수에서 제목 유사도 0.65 이상 쌍이 4개뿐이고, 그중 **강한 중복 후보(제목≥0.8 또는 본문≥0.6)는 0쌍**이다. ingest 의 "무엇을 갱신할지" 판단이 제대로 들었다는 뜻이다. 원래 우려는 성립하지 않았다.
- Progress: **대신 회복 불가 함정이 드러났다.** kit 출처 항목 115개 중 **107개의 무효화 조건이 전부 배제 경로(`ai-workflow/*`)를 가리켰다.** 구조가 이렇다 — **Lint 는 배제를 모른다**(auto-trading 의 `ai-workflow/**` 가 바뀌면 stale 로 표시한다), **ingest 는 그 경로를 읽지 않는다**(`ASD_SOURCE_EXCLUDE`). 따라서 **한 번 stale 이 되면 영영 회복하지 못한다.** kit 은 활발히 개발되고 auto-trading 이 벤더링하므로 "언젠가"가 아니라 "다음 동기화 때" 일어난다.
- Progress: **A 안으로 처리 — 순수 kit 출처 115개를 지웠다 (사용자 승인).** 출처가 섞인 항목은 **0개**라 깨끗하게 갈렸다(순수 kit 115 · 도메인 81 · 경로 없음 99). 지우기 전 id·제목·provenance 를 `TASK-2026-09-01-main-002-deleted-items.json` 에 남겼고, git 히스토리에도 그대로 있어 되돌릴 수 있다. 삭제 후 Lint 재실행 — **`clean = True`**, 목록 재작성 후 커밋 `dacbc6f`(115파일 삭제).
- Progress: **B~D 를 택하지 않은 이유.** B(배제를 Lint 에도 적용)는 **"영원히 낡지 않는 지식"**을 만들어 FR-8 의 취지를 정면으로 거스른다. C(배제 되돌리기)는 갱신 경로는 살리지만 지식의 40%가 모 시스템 것이 아니게 되고 속도도 되돌아간다. D(별도 지식베이스 분리)는 원칙적으로 옳으나 "지식베이스는 하나다"라는 전제 위에 선 구조라 큰 작업이다.
- Progress: **경로 없는 99개는 건드리지 않았다.** provenance 에 커밋만 있어 kit 인지 도메인인지 **판별할 수 없다.** 제목 기준 추정으로는 kit 7 · 도메인 13 · 애매 79 인데, **짐작으로 지식을 지우지 않는다.** 이것이 남은 물음이다.
- Next session starting point: (완료) 남은 물음 — 경로 없는 항목 99개(33%)의 출처를 어떻게 되짚을 것인가
- Remaining risks: **부트스트랩이 도는 동안은 원천 구성을 바꾸지 않는다** — 바꾸면 커서가 무효가 되어 748묶음을 처음부터 다시 읽는다.
- Remaining risks: 중복 판정을 제목 유사도만으로 하면 놓친다 — 같은 개념을 다른 제목으로 만들 수 있다. 본문·`used_paths` 도 함께 봐야 한다.

## ✅ Outcome

- Result: **중복 0쌍 — 원래 우려는 성립하지 않았다.** 대신 kit 출처 115개가 **갱신 불가 + stale 확정** 경로에 있음이 드러나 전부 삭제했다. 지식베이스는 **295 → 180개**, kit 출처 **0개**. Lint `clean = True` 유지.
- Result: **검증**: 삭제 후 Lint 단독 실행 — 모순 0 · 소견 0 · stale 0 · 못 읽는 파일 0 · 목록 재작성 커밋 `dacbc6f`. 삭제 목록은 `-deleted-items.json` 과 git 히스토리 양쪽에 남는다.
- Follow-up: **조사 완료 (2026-09-02) — 원인은 프롬프트가 아니라 원천 성격이었다.** 실행별 경로 없음 비율: 1차 `standard_ai_workflow` **97%**(41개 중 40) · 1차 두 저장소 71% · 2차 두 저장소 36% · **최종 auto-trading 단독+`ai-workflow` 배제 30%**(30개 중 9). **kit 문서를 읽던 실행이 97%, 코드만 읽는 실행이 30%다** — 산문 문서에서는 "어느 파일을 보고 썼는가"가 흐릿하고 코드에서는 또렷하다. kit 을 원천에서 뺐으므로 **가장 큰 원인은 이미 제거됐다.**
- Follow-up: **가설 하나는 틀렸다 — 기록해 둔다.** "모델이 `used_paths` 를 빠뜨린다"고 봤으나 A/B 에서 **옛 규칙도 8/8 채웠다.** 실제 실패 모드는 *묶음에 없는 경로를 대는 것*이다(저장소 전체를 아는 듯 쓰지만 그 묶음에서 읽은 게 아니라 `source_provenance()` 가 걸러낸다). 그 실패 모드를 겨냥해 규칙 7을 넣었고 묶음 실재율 87%(7/8) → 100%(11/11) 이지만 **n 이 작아 효과 크기는 주장하지 않는다.**
- Follow-up: **이미 만들어진 101개는 손대지 않는다.** 무효화 `refs` 로 provenance 를 채우는 것은 기술적으로 되지만 **출처를 모델에게 맡기는 일**이라 설계가 명시적으로 금지한 바로 그것이다(agent.py 상단 — "우리는 지금 어느 커밋을 읽는지 이미 알고 있다").
- Follow-up: (옛 기록) **경로 없는 항목 99개(전체의 55%)** — 출처에 커밋만 있어 kit/도메인 판별이 안 된다. `source_provenance()` 가 실재하지 않는 경로를 걸러낸 결과라 방어는 작동했지만, 그만큼 **되짚을 수 있는 범위가 커밋 단위로 뭉개진다.** 다음 과제 후보.
- Follow-up:
