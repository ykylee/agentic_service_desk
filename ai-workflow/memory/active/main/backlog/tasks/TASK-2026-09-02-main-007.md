---
id: TASK-2026-09-02-main-007
status: done
created_at: 2026-09-02
source_anchor: generic-task-2026-09-02-main-007
source_path: backlog/2026-09-02.md
kind: generic
wbs: exempt
wbs_exempt_reason: 기존 4.2 산출물의 관측이다. 신규 기능이 아니라 앞선 두 변경(11bb616 · 1671dd0)의 효과를 큰 표본으로 재는 일이다
---

# TASK-2026-09-02-main-007 — 부트스트랩 완주 재실행 — 116묶음을 빈 상태에서

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-02
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - 

- Description: maxTokens 수정과 메시지 분배 이후 처음으로 전체를 완주시킨다. 재시도·실패율과, 메시지를 못 받는 66묶음의 품질 영향을 큰 표본으로 본다
- Completion criteria: 116묶음 완주 — 묶음당 중앙값, 재시도 합, 실패 건수를 낸다
- Completion criteria: 메시지를 받은 묶음(50)과 못 받은 묶음(66)의 산출 차이를 낸다 — 이것이 미측정으로 남아 있던 위험이다
- Completion criteria: 완주 후 Lint 가 clean 이다

## 🛠️ Implementation / Content

- Progress: **라이브를 건드리지 않는다** — 빈 지식베이스 + 임시 ops DB 에서 `IngestRun` 을 그대로 돌린다(워커가 부르는 경로). 에이전트를 감싸 호출마다 소요·재시도·제안 수·메시지 유무를 남긴다. 배선 확인에서 함정 하나를 밟았다: **`exclude` 를 넘기지 않으면 212묶음**이 되고 `ai-workflow/` 의 kit 사본을 다시 읽어 **지웠던 kit 출처 지식 115개가 되살아난다**. 넘기고 나서 116묶음을 확인했다.
- Progress: **완주했다 — 116묶음 · 297.7분(4.96시간).** 소요 중앙값 **124.5초**(p25 79 · p75 198 · p90 274 · max 490 · min 14). A/B 에서 낸 추정 145초와 같은 자리다. 색인은 0 → 241항목으로 자랐고 그 사이 소요가 늘지 않았다 — 색인이 주범이 아니라던 앞선 판단이 전수에서 확인됐다.
- Progress: **재시도가 사라지다시피 했다 — 116호출에 5회(묶음당 0.043).** 이전 런은 묶음당 0.27~0.53 이었다. **그중 `maxTokens` 탓은 단 1건**(묶음 85, `사고만 하다 끝났다`)이고 나머지 넷은 형식 위반이다. 32,768 이 116회 중 한 번 모자랐다 — 올릴 근거가 되기엔 약하고, 이제는 조용하지 않다는 것이 요점이다.
- Progress: **실패 1건은 `maxTokens` 와 무관하다.** 묶음 30 이 `pi 가 600.0초 안에 끝나지 않았다`(HarnessError) 로 죽었다 — 출력 한도가 아니라 **응답 자체가 오지 않은 것**이다. 파일 2개짜리 작은 묶음이라 크기 문제도 아니다. 116회에 한 번이고 커서는 전진하지 않으므로 다음 주기가 다시 읽는다.
- Progress: **커밋 메시지 유무별 산출 — 위험이 실현되지 않았다.** 메시지를 받은 50묶음이 제안 140개(파일 175개), 못 받은 66묶음이 제안 193개(파일 229개)다. **파일당으로 고르면 0.80 대 0.84 로 사실상 같다.** 아무것도 못 낸 묶음도 5 대 3 이다. 즉 66묶음이 메시지를 못 받아 산출이 줄어드는 일은 **일어나지 않았다.**
- Progress: **다만 인과로 읽으면 안 된다.** `messages[i::n]` 이라 메시지가 **앞 50묶음에 몰려 있고**, 묶음 순서는 경로 정렬 순서라 앞뒤가 저장소의 다른 영역을 덮는다(뒤쪽이 `src/`·`tests/` 로 코드가 짙다). 무작위 배정이 아니므로 **"메시지가 없어도 줄지 않았다"까지가 이 표본이 말하는 전부**이고, "메시지가 쓸모없다"는 아니다.
- Progress: **완주 후 지식 항목 243개 · 못 읽는 파일 0 · Lint clean.** 런 중 **갱신이 73건** 일어났다 — 모델이 색인을 보고 기존 항목을 고른 횟수이고, 중복 대신 갱신으로 간 것이 그만큼이다.
- Next session starting point: (완료)
- Remaining risks: **같은 원천에서 항목 수가 재현되지 않는다** — 라이브는 184개인데 새로 지으니 243개다. 라이브는 kit 삭제와 증분 누적을 거친 것이라 직접 비교는 아니지만, **같은 저장소를 두 번 지으면 다른 지식베이스가 나온다**는 것은 그대로다. 개념 단위를 모델이 정하므로 예상되는 성질이되, 재적재를 결정할 때 이 값을 고려해야 한다.
- Remaining risks: **`pi` 가 600초 타임아웃으로 응답을 아예 안 주는 경우가 있다** (116회에 1). 원인 미상이고 재시도 대상이 아니다(`HarnessError` 는 다시 부르지 않는다 — 중단 신호와 구분되지 않기 때문).

## ✅ Outcome

- Result: **116묶음 완주 · 4.96시간 · 묶음당 중앙값 124.5초.** `maxTokens`(11bb616)와 메시지 분배(1671dd0) 이후 처음으로 전체를 돌렸다.
- Result: **재시도 묶음당 0.043** (이전 0.27~0.53). `maxTokens` 탓은 116회 중 1건뿐이다.
- Result: **메시지를 못 받는 66묶음의 품질 영향 — 미측정이던 위험이 닫혔다.** 파일당 제안이 0.80(메시지 있음) 대 0.84(없음)로 사실상 같다. **분배를 되돌릴 이유가 없다.** 단 배정이 무작위가 아니라 인과 주장은 하지 않는다.
- Verification: 116호출 전건 기록(`TASK-2026-09-02-main-007-rebootstrap.json`) · 완주 후 항목 243개 · 못 읽는 파일 0 · Lint clean · 라이브 지식베이스와 운영 DB 는 건드리지 않았다(빈 저장소 + 임시 DB).
- Follow-up: **긴 런 중 게재가 멈추는 문제가 이 런으로 실증됐다** — 4.96시간 동안 커밋이 끝에 한 번뿐이라 그 사이 `pin()` 이 성립하지 않는다. `TASK-2026-09-02-main-009`(WBS-5.6.1)로 잇는다.
- Follow-up: `MAX_COMMIT_MESSAGES=50` 상향은 **하지 않는다** — 메시지 없는 묶음이 손해를 보지 않았으므로 올릴 근거가 없다.
