---
id: TASK-2026-08-28-main-017
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-017
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.2.4
---

# TASK-2026-08-28-main-017 — WBS-4.2.4 Ingest 에이전트

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/ingest/agent.py`
  - `src/agentic_service_desk/ingest/run.py`
  - `src/agentic_service_desk/ingest/config_paths.py`
  - `src/agentic_service_desk/knowledge/serialize.py`
  - `src/agentic_service_desk/knowledge/repository.py`

- Description: 에이전트가 원천을 읽어 개념 단위 지식 항목을 생성·갱신한다. 출처는 코드가 부착하고, 1 회 ingest = 1 커밋으로 남기며, 설정값은 ingest 하지 않는다 (FR-3·4·5·9·53, ADR-003).
- Completion criteria: 지식 항목이 OKF frontmatter 파일로 저장되고 다시 읽힌다 (FR-5, FR-55)
- Completion criteria: 출처 없는 항목이 만들어질 수 없다 — 커밋 해시/QnA id 를 코드가 붙인다 (FR-4)
- Completion criteria: 1 회 ingest = 1 커밋 + log.md 한 줄 (FR-5)
- Completion criteria: 설정 파일 경로가 원천에서 기계적으로 배제된다 (FR-9)
- Completion criteria: QnA 원천은 산출물 필터를 지나서만 들어온다 (NFR-4)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 13:40` 기준 에이전트가 원천을 읽어 개념 단위 지식 항목을 생성·갱신한다. 출처는 코드가 부착하고, 1 회 ingest = 1 커밋으로 남기며, 설정값은 ingest 하지 않는다 (FR-3·4·5·9·53, ADR-003).
- Next session starting point:
- Remaining risks: 최초 부트스트랩에서 커밋 메시지가 50건으로 잘린다. 증분 실행은 닿지 않지만 전체 적재 시 '왜 그렇게 정했는가'의 상당량이 실리지 않는다 — 잘린 건수를 보고에 남기지만 히스토리 전용 ingest 경로가 따로 필요하다
- Remaining risks: 부트스트랩 한 번이 한 tick 안에서 다 돈다. 5만~50만 줄이면 매우 긴 tick 이 되고, 중간에 멈추면 커서가 안 옮겨져 처음부터 다시 한다 (안전하되 낭비)

## ✅ Outcome

- Result: **모델이 정하는 것과 코드가 정하는 것을 나눴다.** 개념·제목·본문·갱신 대상은 모델이, 출처·원천 구성·무효화 대비값·사람 편집 보호는 코드가 정한다. 출처를 모델에 맡기지 않는 이유는 틀린 출처가 붙어 있다는 사실 때문에 오히려 그럴듯해지기 때문이다 (§2.2.3 과 같은 논리)
- Result: 모델이 지어낸 경로는 출처로 인정하지 않는다 — 원천에 실제로 있던 경로만 통과시키고, 하나도 없으면 커밋만 붙인다. 출처 없는 항목은 어느 경로로도 만들어지지 않는다 (FR-4)
- Result: 설정 파일 배제(FR-9)를 프롬프트가 아니라 **경로 단위로 기계적으로** 집행한다. 경계는 의도적으로 넉넉하다 — .json·.yaml 까지 자른다. 아닌 것을 놓치는 대가보다 설정값이 지식으로 굳는 대가가 크다
- Result: 1 회 ingest = 1 커밋. 빈 커밋은 만들지 않는다 — 쌓이면 '어느 커밋이 지식을 바꿨는가'를 세는 일이 무의미해진다
- Result: QnA 진행 표시를 커서가 아니라 **목록(ingested_answer)** 으로 뒀다. 답변은 만들어진 지 한참 뒤에 ingest 자격을 얻을 수 있어(§5.3.2) 시각 커서를 쓰면 영영 건너뛰어진다
- Result: 소스 커서는 여기서 옮긴다 — 4.2.1 이 옮기지 않고 남겨 둔 자리다. 에이전트가 터지면 옮기지 않는다
- Result: 지식 저장소는 SourceMirror 와 별도 클래스다. 저쪽은 CO-1 로 쓰기를 막고 이쪽은 쓰는 것이 목적이라, 한쪽 규칙이 다른 쪽으로 새지 않게 나눴다
- Verification: uv run pytest -q — 231 passed (기존 175 + 신규 56). 실제 git 저장소 + mock QnA 로 e2e: 소스 1묶음 + QnA 2건 = 에이전트 3회 호출, 신규 항목 2건, 지식 저장소에 커밋 1개(23c552c)에 두 항목 + log.md 한 줄. app.yaml 이 원천에서 배제됨(FR-9). 출처는 각각 commit=aebe4c13/path=limit.py 와 qna=Q-2 로 코드가 부착.
- Follow-up: WBS-4.2.5 모순 표시와 사람 편집 보존 — IngestRun 이 이미 edited_by_human 항목을 덮어쓰지 않고 held_for_human 에 담는다. 여기를 **Q4 모순 대기열로 잇는 것**이 4.2.5 다
- Follow-up: WBS-4.2.6 Lint — repo.scan() 이 깨진 지식 파일 목록을 이미 돌려준다. 이것과 출처 커밋 실재 검사(ADR-002 결정 4)가 Lint 의 재료다
- Follow-up: 실제 pi + 로컬 LLM 으로 프롬프트를 한 번 태워 봐야 한다. 현재 시험은 전부 FakeHarness 라 **출력 형식 준수는 검증되지 않았다** — JSON 파싱은 방어적으로 짰지만 실측이 남았다
