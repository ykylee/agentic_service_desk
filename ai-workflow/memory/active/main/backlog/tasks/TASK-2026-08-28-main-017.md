---
id: TASK-2026-08-28-main-017
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-017
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.2.4
---

# TASK-2026-08-28-main-017 — WBS-4.2.4 Ingest 에이전트

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/ingest/agent.py`
  - `src/agentic_service_desk/ingest/run.py`
  - `src/agentic_service_desk/ingest/config_paths.py`
  - `src/agentic_service_desk/knowledge/serialize.py`
  - `src/agentic_service_desk/knowledge/repository.py`

- Description: 에이전트가 원천을 읽어 개념 단위 지식 항목을 생성·갱신한다. 출처는 코드가 부착하고, 1 회 ingest = 1 커밋으로 남기며, 설정값은 ingest 하지 않는다 (FR-3·4·5·9·53, ADR-003).
- Completion criteria: 지식 항목이 OKF frontmatter 파일로 저장되고 다시 읽힌다 (FR-5, FR-55)
- Completion criteria: 출처 없는 항목이 만들어질 수 없다 — 커밋 해시/QnA id 를 코드가 붙인다 (FR-4)
- Completion criteria: 1 회 ingest = 1 커밋 + log.md 한 줄 (FR-5)
- Completion criteria: 설정 파일 경로가 원천에서 기계적으로 배제된다 (FR-9)
- Completion criteria: QnA 원천은 산출물 필터를 지나서만 들어온다 (NFR-4)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 13:51` 기준 에이전트가 원천을 읽어 개념 단위 지식 항목을 생성·갱신한다. 출처는 코드가 부착하고, 1 회 ingest = 1 커밋으로 남기며, 설정값은 ingest 하지 않는다 (FR-3·4·5·9·53, ADR-003).
- Next session starting point:
- Remaining risks: 한 번의 실행에서 QnA 호출이 한 건 실패했으나(items 가 목록이 아님) 이후 11회 샘플에서 재현되지 않았다. 드문 출력 형태가 남아 있다는 뜻이며, 이제 실패 메시지가 받은 응답을 실으므로 다음에 걸리면 형태가 남는다

## ✅ Outcome

- Result: **라이브에서 버그를 하나 잡았다.** 지식이 하나도 안 바뀐 주기에 진행 표시를 건너뛰고 있었다 — 그래서 뽑을 개념이 없는 원천(내용 없는 봇 답변 '설정을 확인해 보세요')이 **매 주기 LLM 에 다시 실렸다.** 읽은 것과 지식이 된 것은 다르다. 진행 표시를 옮기지 않는 경우는 실패했을 때뿐으로 바꿨고, 그 보호를 changed 플래그가 아니라 명시적 실패 판정에 걸었다
- Result: 모델이 **개념 단위 갱신을 실제로 해냈다.** 소스에서 만든 '결재 한도 결정 규칙'을 Q-2 QnA 에서 같은 개념으로 알아보고 그 id 로 갱신했고, 출처가 [commit, Q-2] 로 합쳐졌다 — ADR-003 의 '근거가 여러 원천에 걸친다'가 실측으로 확인됐다
- Result: FR-9 가 프롬프트 수준에서도 작동했다. 모델이 한도 값(500만/300만)을 본문에 쓰지 않고 '매핑은 limit.py 에 정의되어 있다'로 남겼다. 경로 배제는 기계적 집행이고 이것은 그 위의 한 겹이다
- Result: JSON 파서를 굳혔다 — 울타리를 전부 훑어 처음 읽히는 객체를 쓰고, items 가 null 이거나 객체 하나면 읽어 주되(빗나감) items 키 자체가 없으면 실패로 올린다(어김). 실패 메시지에 응답 앞부분을 싣는다: 모델 출력은 재현이 어려워 증거 없이는 로그를 봐도 고칠 수 없다
- Verification: 라이브 검증 완료 (pi 0.84.1 + MiniMax-M3, 2026-08-28). 합성 저장소 + mock QnA 로 4회 연속 실행: 1회차 41초 신규2 갱신1 실패0, 2회차 0초(호출 없음), Q-3 해결표시 후 3초(새로 자격 얻은 답변 1건 호출), 4회차 0초. 모델이 출력 형식을 지켰고 코드 울타리는 파서가 벗겨냈다. uv run pytest -q — 239 passed.
- Follow-up: WBS-4.2.5 모순 표시와 사람 편집 보존 — IngestRun 이 이미 edited_by_human 항목을 덮어쓰지 않고 held_for_human 에 담는다. 여기를 **Q4 모순 대기열로 잇는 것**이 4.2.5 다
- Follow-up: WBS-4.2.6 Lint — repo.scan() 이 깨진 지식 파일 목록을 이미 돌려준다. 이것과 출처 커밋 실재 검사(ADR-002 결정 4)가 Lint 의 재료다
- Follow-up: 실제 pi + 로컬 LLM 으로 프롬프트를 한 번 태워 봐야 한다. 현재 시험은 전부 FakeHarness 라 **출력 형식 준수는 검증되지 않았다** — JSON 파싱은 방어적으로 짰지만 실측이 남았다
