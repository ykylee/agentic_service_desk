---
id: TASK-2026-08-28-main-020
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-020
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.2.7
---

# TASK-2026-08-28-main-020 — WBS-4.2.7 대시보드 최소

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/web/dashboard.py`

- Description: S0 운영자 화면 — Q4(모순) 대기열과 지식베이스 현황. 화면은 무엇을 고쳐야 하는지 지목만 하고 편집은 평소 도구로 한다 (FR-44·59, §8.3·8.5.5).
- Completion criteria: S0 에 Q4·Q8 만 보인다 (FR-59)
- Completion criteria: Q4 가 양쪽 주장과 파일 경로를 지목하고 판정할 수 있다 (§8.5.5)
- Completion criteria: 지식베이스 현황 — 항목 수·출처 구성·stale 비율·미해결 모순·최근 ingest 이력 (§8.3)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 14:29` 기준 S0 운영자 화면 — Q4(모순) 대기열과 지식베이스 현황. 화면은 무엇을 고쳐야 하는지 지목만 하고 편집은 평소 도구로 한다 (FR-44·59, §8.3·8.5.5).
- Next session starting point:
- Remaining risks: Q4 판정 버튼에 CSRF 보호나 인증이 없다. 사내 단일 운영자용이고 최종 이용자 인증은 모 시스템 책임(범위 밖)이지만, 이 화면 자체의 접근 제어는 아직 아무것도 없다 — 배포 전에 정해야 한다
- Remaining risks: 판정 결과(kept_human/took_agent/merged)의 분포를 기록만 하고 화면에 보여주지 않는다. 그 분포가 사람 편집과 ingest 프롬프트 중 어느 쪽이 틀리고 있는지를 말해 주는 지표다

## ✅ Outcome

- Result: **stale 을 Q5 에서 뺐다 (4.2.6 정정).** Q5 는 '근거가 낡은 게재 답변·살아있는 문서' 의 정정 후보이지 지식 항목 자체가 아니다(§8.2). 지식 항목의 stale 은 §8.3 의 현황 지표이며, 게재물로 번져 Q5 가 되는 것은 stale 전파(WBS-4.5.7)의 몫이다. 그대로 뒀으면 S0 에서 화면에 뜨지도 않는(FR-59) **보이지 않는 대기열이 쌓였다**
- Result: Q4 는 사람 쪽과 에이전트 쪽을 나란히 놓고 **파일 경로를 지목**한다. 에디터를 만들지 않는다 — 운영자가 개발자이고 지식이 파일이면 중복 투자다 (§8.5.5)
- Result: 대기열마다 **방치 비용**을 함께 보여준다. 방치 비용이 곧 우선순위다 — 여덟을 나란히 늘어놓으면 무엇부터 볼지 모른다 (§8.2)
- Result: Q8 은 비어 있고, **비어 있는 이유를 화면에 적었다.** 빈 목록과 '판정 자체가 아직 없다' 는 다르다
- Result: 현황에 Lint 소견 수를 넣었다. S0 에서 Q5 는 화면에 뜨지 않는데 참조 부재 같은 소견은 실제로 생길 수 있다 — 현황에도 없으면 완전히 보이지 않게 된다
- Result: DB 연결을 요청마다 열고 닫는다. 온라인과 배치가 같은 SQLite 파일을 쓰므로(ADR-002) 화면 하나가 연결을 붙들면 배치가 그 뒤에서 기다린다
- Verification: uv run pytest -q — 303 passed (기존 283 + 신규 20). 실제 uvicorn 기동 후 브라우저 경로 전부 확인: / (현황 4카드 + 대기열 2), /queues/Q4 (양쪽 나란히 + 파일 경로 + 판정 버튼 3), /queues/Q8 (빈 이유 설명). POST 판정 → 303 리다이렉트 → 목록에서 빠지고 홈의 미해결 모순이 1→0. 휠 빌드에 템플릿 4개가 포함되는 것도 확인.
- Follow-up: WBS-4.3 S1 수동 등록과 티켓 — Q1 티켓 대기열이 붙는다. QUEUES 레지스트리와 STAGE_QUEUES 에 이미 자리가 있다
- Follow-up: 4.2 완료 조건 판정 — 실제 모 시스템 저장소를 붙여 지식이 쌓이고 LintReport.clean 이 안정되는지 봐야 한다. 지금까지는 전부 합성 저장소와 mock QnA 다
