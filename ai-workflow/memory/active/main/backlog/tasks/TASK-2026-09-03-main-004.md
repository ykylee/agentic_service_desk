---
id: TASK-2026-09-03-main-004
status: done
created_at: 2026-09-03
source_anchor: generic-task-2026-09-03-main-004
source_path: backlog/2026-09-03.md
kind: generic
wbs: M-005/WBS-5.7.2
---

# TASK-2026-09-03-main-004 — WBS-5.7.2 대상별 정제 — 고객응대용과 개발자용

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-03
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/pipeline/audience.py`
  - `src/agentic_service_desk/pipeline/review.py`

- Description: 같은 답을 두 사람에게. 질문한 사람에게는 내부 경로가 소음이고 원인을 찾는 사람에게는 그것이 필요한 것이다
- Completion criteria: 같은 초안에서 두 글이 나오고 서로 다른 것을 담는다
- Completion criteria: 정제가 새 사실을 더하면 화면이 지목한다
- Completion criteria: 파이프라인은 건드리지 않는다 — 게재되는 글의 모양이 바뀌지 않는다

## 🛠️ Implementation / Content

- Progress: **파이프라인을 건드리지 않는 것이 첫 제약이다.** `AnswerPipeline` 은 실제 답변 경로(유입 → 검수 → 게재)와 같은 코드라, 거기에 대상별 산출을 넣으면 **게재되는 글의 모양이 바뀐다.** 정제는 이미 나온 초안을 다시 쓸 뿐인 콘솔 전용 단계(`pipeline/audience.py`)로 두고, 호출은 한 번만 더 쓴다 — 대상마다 부르면 둘이 되는데 같은 초안을 다르게 쓰는 것이라 한 번에 내는 편이 서로 어긋날 여지도 적다.
- Progress: **정제가 사실을 보태는지 기계적으로 한 번 더 본다.** 검수 P1(`check_ungrounded_numbers`)을 두 산출에 그대로 걸어, 근거 원문에 없는 수치가 섞이면 화면이 지목한다. **통과가 옳음의 증거는 아니다** — 수치만 보므로 근거 밖 고유명사나 과장은 이 검사를 지난다.
- Progress: **그 검사에서 공유 결함이 드러났다.** `_NUMERIC` 이 `[0-9][0-9,._]*` 라 **줄 앞 목록 번호 `1.` 을 수치로 잡는다.** 근거 원문에 그런 토큰이 있을 리 없으므로 **번호 목록을 쓴 초안은 통째로 P1 에 반려된다** — 콘솔만의 문제가 아니라 **실제 Q2 검수에서도 같은 일이 난다.** 라이브에서 정제 산출이 `1.` `2.` `3.` `4.` `5.` 로 걸리며 드러났다.
- Progress: **고쳤다.** 훑기 전에 줄 앞 목록 번호를 지우고, 꼬리의 구분자를 떼어 `3000.` 과 `3000` 이 같은 것으로 대조되게 했다(문장 끝 마침표 때문에 근거에 있는 수치가 없는 것으로 판정되는 것도 같은 종류의 거짓 반려다). **목록 안의 진짜 수치는 여전히 잡는다** — 번호만 빼는 것이지 목록을 봐주는 것이 아니다.
- Progress: **라이브에서 두 글이 뚜렷이 갈렸다.** 고객용은 파일 경로·식별자 없이 "현황 요약 엔드포인트", "스냅샷을 만들어내는 쪽(producer)" 으로 풀어 썼고 모르는 부분을 그대로 남겼다. 개발자용은 `dashboard_api/app.py` · `await` · `mypy strict` · 커밋 해시 · 시험 파일명까지 살렸다. 초안의 `추론` 진술은 양쪽 모두 "…로 보인다"로 남았다.
- Progress: **설계를 고쳤다 (사용자 지적, 2026-09-03).** 처음에 정제를 **콘솔 전용**으로 두었는데, 그러면 정작 **게재되는 본문은 그대로 내부 구현을 담은 채 나간다.** 확인해 보니 `Draft.body`(=진술을 이어 붙인 것)가 곧 게재 본문이고 `publication.py` 가 그대로 올린다 — 라이브 초안에 `dashboard_api/app.py` · `inquire_fills` · `odno` · `tot_ccld_qty` 가 들어 있었다. **질문한 사람에게 내부 경로가 나가고 있었다.**
- Progress: **정제를 파이프라인 4단계로 옮겼다 — 검수 앞이다.** 뒤에 두면 **검수가 본 글과 나가는 글이 달라져** FR-20("검수가 보는 것의 전부는 글에 적힌 것과 근거 원문")이 무너진다. 검수는 `body` 를 보고 그 값이 정제 단계에서 정해지므로, 앞에 두면 **검수가 본 글이 곧 나가는 글**이 유지된다.
- Progress: **진술 단위로 정제한다.** 글 전체를 다시 쓰면 진술과 강도의 대응이 끊겨 ADR-007 의 강도 표시가 어디에도 붙지 못한다. 진술 하나를 진술 하나로 다시 쓰고 **강도와 근거는 물려받는다** — 정제는 판정을 다시 하지 않는다. **개수가 어긋나면 통째로 버리고 원본을 내보낸다**: 합치거나 나눈 것은 다시 쓴 것이 아니라 새로 쓴 것이고, 그 제약이 "사실을 더하지 않는다"를 함께 지킨다.
- Progress: **실패해도 답은 남는다.** 모델이 없거나 정제가 터지거나 규약을 어기면 원본이 그대로 나간다 — 다듬기가 답을 무너뜨리면 안 된다. 단계 기록에 왜 원본을 뒀는지 적는다.
- Progress: **개발자용은 나갈 곳이 없다.** 모 시스템의 쓰기 표면은 답변 게재 하나뿐이고 그 독자는 질문자다 (§9.8.1). 개발자용은 운영자 화면 전용으로 남는다.
- Progress: **라이브가 FR-17 위반을 하나 더 냈다.** 정제를 붙이고 한국어 질문을 태우니 **고객용 글이 영어로 나왔다** — 1단계가 `ko` 로 판정하고 3단계가 한국어로 썼는데, 정제 지시에 언어가 없어 모델이 조용히 옮겼다. 질문한 사람이 읽을 수 없는 답이 나갈 뻔했다. 정제 지시에 언어를 못 박고(고객용·개발자용 둘 다) 파이프라인이 `analysis.language` 를 넘기게 했다.
- Progress: **콘솔의 중복 호출도 걷어냈다.** 정제가 파이프라인으로 옮겨 갔으므로 **고객용은 다시 만들지 않는다** — 화면은 `draft.body`(= 나가는 글)를 그대로 보여 주고 개발자용만 한 번 더 부른다. 안 그러면 화면에 보이는 글과 나가는 글이 갈리고 호출도 하나 더 든다.
- Progress: **라이브 확인 (2026-09-03).** 같은 질문에서 고객용은 "개요(overview) 응답을 다루는 부분" · "데이터 생성 쪽" 으로 풀렸고 **파일 경로·식별자가 하나도 없다.** 개발자용은 `dashboard_api/app.py` 를 그대로 들고 확인 순서를 매겼다. `추론` 진술은 양쪽 다 단정하지 않았다.
- Progress: **진술 1:1 제약을 버렸다 (사용자 지적, 2026-09-03).** 강도의 대응을 지키려고 진술 하나를 진술 하나로 옮기게 했더니 **문단 나열이 되어 사람이 쓴 글로 읽히지 않았다** — "…하는 것이 우선입니다", "또 다른 500 발생 양상이 있습니다" 같은 번역투가 뚝뚝 끊겼다. **정제의 목적이 읽히는 글을 만드는 것인데 그 목적을 제약이 이겼다.**
- Progress: **한 편의 글로 쓰게 바꿨다.** `Draft.rendered` 를 진술 목록에서 **문자열**로 바꾸고, 지시를 다시 썼다 — 결론부터 · 이어지는 글로 · **묻는 것에만**(아는 것을 다 늘어놓지 않는다, 길어질수록 답이 아니라 자료가 된다) · 내부 말 없이 · 인사와 맺음은 짧게. `[확인됨]` 은 단정해도 되고 `[추론]` 은 "…로 보입니다"로 남긴다.
- Progress: **강도는 원본에 남는다.** 나가는 글에 강도를 붙이지 않는 것은 **원래 규약**이다(강도는 운영자 화면에만 붙고 이용자에게는 가지 않는다). 그래서 1:1 을 버려도 ADR-007 이 깨지지 않는다 — 약한 지점은 `statements` 로 세고 화면이 그것을 보여 준다. 안전장치로 남은 것은 **검수가 정제된 글을 본다**는 것과 P1 수치 대조다.
- Next session starting point: (완료)
- Remaining risks: **답변마다 LLM 호출이 하나 늘었다** (생성 1 + 정제 1). 게재는 S3 부터라 당장 비용이 늘지는 않지만, 켜지는 날부터 답변 하나의 값이 두 배가 된다.
- Remaining risks: **정제가 뜻을 바꿔도 기계는 모른다.** 개수와 수치는 지키지만 의미가 어긋나는 것은 검수의 의미 판정(P2·P3·P5)에 맡긴다 — 그 판정은 정제된 글을 보므로 배선 자체는 옳다.
- Remaining risks: **P1 대조는 수치만 본다.** 정제가 근거 밖 고유명사를 지어내거나 어조로 과장하는 것은 잡지 못한다 — 그것이 §5.5.1 이 의미 판정으로 넘긴 몫이고, 이 화면에는 그 판정이 없다.
- Remaining risks: **호출이 하나 더 든다** — 질문 하나에 파이프라인 1회 + 정제 1회로 90~120초다.

## ✅ Outcome

- Result: **같은 초안이 두 글이 된다** — 고객응대용(경로·식별자 없음)과 개발자용(경로·식별자·에러 문구 살림). 초안의 `추론`·`근거 얇음` 은 어느 쪽에서도 단정하지 않는다.
- Result: **정제가 게재 경로 안으로 들어갔다** — 파이프라인 4단계, **검수 앞**. 게재 본문에서 내부 경로와 코드 식별자가 사라지고, **검수가 본 글이 곧 나가는 글**이 유지된다.
- Result: **검수 P1 의 거짓 반려를 함께 고쳤다** — 목록 번호를 수치로 세던 결함이다. 이건 콘솔이 아니라 **실제 Q2 검수의 결함**이었고, 정제 기능이 없었으면 드러나지 않았을 자리다.
- Verification: `uv run pytest -q` **1169 passed** (신규 13 — 정제 10 · P1 회귀 3) · 라이브 1건에서 두 글이 갈리는 것과 P1 대조가 실제로 도는 것을 확인했다.
- Follow-up: 근거 밖 **고유명사**는 여전히 못 잡는다 (§5.5.1 이 의미 판정으로 넘긴 몫).
