---
id: TASK-2026-09-01-main-003
status: done
created_at: 2026-09-01
source_anchor: generic-task-2026-09-01-main-003
source_path: backlog/2026-09-01.md
kind: generic
wbs: exempt
wbs_exempt_reason: 완주 후 성능 개선. 신규 기능이 아니라 기존 ingest 경로의 소요 단축이라 WBS leaf 를 새로 세우지 않는다
---

# TASK-2026-09-01-main-003 — ingest 묶음당 소요를 줄인다 — 재시도·사고블록·색인

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-09-01
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 부트스트랩 묶음당 중앙값 151초. 프롬프트의 43%가 원천이 아니라 기존 항목 색인이고, 재시도가 묶음당 0.42회 붙으며, 모델이 매번 사고 블록을 길게 써서 우리가 버린다. 748묶음이 하루를 넘기는 원인.
- Completion criteria: 묶음당 중앙값을 재고, 손댄 뒤 다시 재어 개선 폭을 숫자로 낸다
- Completion criteria: 색인을 줄이는 안을 택했다면 중복 개념 수가 늘지 않았음을 함께 보인다 (TASK-2026-09-01-main-002 의 측정과 같은 방법)

## 🛠️ Implementation / Content

- Progress: `2026-09-01 09:36` 기준 부트스트랩 묶음당 중앙값 151초. 프롬프트의 43%가 원천이 아니라 기존 항목 색인이고, 재시도가 묶음당 0.42회 붙으며, 모델이 매번 사고 블록을 길게 써서 우리가 버린다. 748묶음이 하루를 넘기는 원인.
- Progress: **분해 실측 (09-01 09:3x · 완료 235/748 · 항목 231개).** 프롬프트 구성 — 규칙 848자 · **원천 21,390자** · **기존 항목 색인 17,355자(43%)** · 총 39,995자. 색인은 항목당 약 75자라 **선형으로 커진다**: 항목 600개면 색인만 약 45,000자로 **원천 한도(`MAX_CHARS_PER_CALL` 24,000)의 두 배**가 된다. 매 호출마다 "이미 있는 항목 목록"을 통째로 다시 보내는 것은 ingest 절차 2단계(무엇을 갱신할지 모델이 고르게 한다)의 대가다.
- Progress: **아직은 색인이 주범이 아니다.** auto-trading 210묶음을 5구간으로 나눠 재니 색인이 75→215항목으로 3배 커지는 동안 묶음당 중앙값은 92 → 96 → 103 → 113 → 83초로 **거의 변하지 않았다.** 입력 처리가 생성 시간에 비해 싸거나 프롬프트 앞부분 캐시가 듣는 것으로 보인다. **주범은 재시도(묶음당 0.42회, +42%)와 사고 블록이다.** 중앙값 151초를 1.42로 나누면 호출 하나가 약 106초.
- Progress: **줄일 수 있는 것 넷과 각각의 걸림돌.** (1) 빈 응답 원인 제거 → 재시도가 사라져 **약 -30%**, 그러나 원인 미상이고 pi 가 rc·stdout·stderr 어디에도 이유를 안 남긴다(재시도 99건 중 stderr 붙은 것 0건). (2) 색인을 제목만 또는 최근 N개로 축소 → 프롬프트 -43%, 그러나 **갱신 인식이 나빠져 중복이 늘 위험**이 있어 `TASK-2026-09-01-main-002` 의 측정과 묶어야 한다. (3) 사고 블록 억제 → 효과 크지만 pi/MiniMax 쪽이라 우리 손 밖이다(`--thinking off` 는 08-31 실측에서 안 먹었다). (4) 묶음 크기 확대 → 호출 수는 줄지만 잘림 위험, 실측 필요(08-31 에 24k/12k/6k 는 셋 다 완결이었으니 상향 여지는 있다).
- Progress: **앞선 분해가 틀렸다 — 정정한다.** 그때는 커밋 메시지를 더미 하나로 채워 재서 통째로 빠져 있었다. 실제 구성(2026-09-02, 항목 180개 기준): 프롬프트 74,694자 = 규칙 848(1%) + **원천 파일 20,170(27%)** + **커밋 메시지 40,410(54%)** + 색인 13,036(17%). **주범은 색인이 아니라 커밋 메시지였고**, 게다가 같은 40,410자가 **116묶음 전부에 실렸다** — 한 런에 4.6M자를 다시 보낸 셈이다.
- Progress: **메시지를 묶음들에 나눠 실었다 (`_spread_messages`).** 근거는 설계 자신의 말이다 — 메시지는 맥락이 아니라 **원천**이고(D16 "히스토리가 '왜'의 1차 출처"), 원천이라면 파일처럼 한 번 읽히면 족하다. `messages[i::n]` 로 전량 분배해 **하나도 빠뜨리지 않고 두 번 싣지도 않는다.** 대가는 **짝이 어긋나는 것** — 어떤 묶음은 A 영역 코드와 B 영역 메시지를 함께 본다(통째로 실을 때에도 대부분 이미 어긋나 있었다). 규약을 못 박아 둔 시험 `test_커밋_메시지는_모든_묶음에_실린다` 가 깨졌고, 의도한 변경이라 새 규약 셋으로 다시 썼다. 1092 passed.
- Progress: **측정 — 전송량은 확정, 속도는 미확인.** 프롬프트 중앙값 **74,694 → 34,998자**, 런 전체 **8.66M → 4.02M자 (-54%)**. 그러나 A/B 4쌍(같은 묶음을 옛 방식/새 방식으로 각각 호출)에서 **속도 차이를 확인하지 못했다**: 옛 중앙값 84.4초(64~355) vs 새 160.1초(69~286). **표본 8개 중 5개가 빈 응답이거나 파싱 실패**라 신호가 잡음에 덮였다. 양쪽 다 정상 응답한 유일한 묶음(20번)은 64.0 vs 69.0초로 사실상 같았다. 숫자만 보면 새 방식이 느려 보이지만 **그렇게 읽으면 안 된다** — 나머지는 빈 응답의 대기 시간이다.
- Progress: **변경은 유지하기로 했다.** 값어치는 이제 *소요 단축*이 아니라 **토큰 비용과 컨텍스트 압박 절반**이다. 같은 40,410자를 116번 보내는 것은 어느 기준으로도 옹호하기 어렵고, 되돌리면 다시 잴 기회가 다음 대규모 런까지 없다. **위험은 남는다**: 묶음 116개 중 66개가 메시지를 하나도 못 받게 되고, 그 품질 영향은 **측정하지 않았다.**
- Progress: **막고 있던 것이 풀렸다 (2026-09-02).** 성능 측정이 불가능했던 이유는 표본의 절반이 빈 응답이어서였고, 그 원인이 밝혀졌다 — pi 의 `maxTokens` 기본값 **16,384** 를 우리가 지정하지 않아 모델이 예산을 사고에 다 쓰고 `stop=length` 로 잘렸다. `Settings.llm_max_output_tokens=32768` 로 정했고(`11bb616`) 잘렸던 묶음들이 살아났다. **이제 묶음당 중앙값을 의미 있게 잴 수 있다** — 그것이 이 task 의 남은 일이다.
- Progress: **재측정 완료 (2026-09-02, A/B 6쌍 12호출).** 같은 묶음을 새 방식(메시지 분배)과 옛 방식(묶음마다 전량 재전송)으로 각각 불러 쟀다. 짝마다 순서를 뒤집어 시간 표류를 상쇄했고, 워커는 멈춰 RPM 경합을 뺐다. **묶음당 중앙값 634.4초 → 145.0초 (-77%)**, 프롬프트 중앙값 67,675 → 32,737자 (-52%). **6쌍 전부에서 새 방식이 빨랐다.**
- Progress: **`maxTokens` 수정의 효과가 잡혔다.** 새 방식 6호출에서 **재시도 0 · 실패 0**이다 (이전 런은 묶음당 0.27~0.53회 재시도, 최악의 부트스트랩은 실패 59/62). 옛 방식은 같은 설정에서도 **재시도 7 · 실패 1** 이었다 — 즉 **32,768 은 프롬프트가 60k자를 넘어가면 여전히 모자란다.** 한도를 올리는 것과 프롬프트를 줄이는 것 중 후자가 실제로 들었다.
- Progress: **느림의 절반은 재시도가 아니다.** 옛 방식의 재시도 7회를 호출 수로 환산하면 13호출/4,190초 = 호출당 322초로, 새 방식 145초의 **2.2배**다. 프롬프트가 길면 한 번의 호출 자체가 느리고, 그 위에 재시도가 얹힌다.
- Progress: **품질은 이번 표본에서 나빠지지 않았다.** 제안 수는 새 8 · 옛 5 이고 실패는 새 0 · 옛 1 이다. 다만 **묶음 4 는 옛 방식이 5개를 내고 새 방식은 0개를 냈다** — 그 묶음의 개념이 커밋 메시지에 있었다는 뜻이고, 이것이 "메시지를 못 받는 66/116 묶음"의 실제 모습이다. **n 이 6이라 품질 차이는 주장하지 않는다** — 주장하는 것은 속도뿐이다.
- Next session starting point: (완료)

- Remaining risks: **메시지를 못 받는 묶음이 66/116 이고 그 품질 영향은 미측정이다.** 되돌리려면 `_chunks` 의 `_spread_messages(...)` 호출만 빼면 된다.
- Remaining risks: **`maxTokens` 32,768 이 긴 프롬프트에는 여전히 모자란다** — 옛 방식(60~110k자) 6호출에서 재시도 7 · 실패 1 이 났다. 지금 방식으로도 큰 파일 하나가 통째로 한 묶음이 되면(`MAX_CHARS_PER_CALL` 초과 파일) 같은 자리를 밟는다.
- Remaining risks: **부트스트랩이 도는 동안은 손대지 않는다** — 프롬프트나 묶음 구성을 바꾸면 커서가 무효가 되어 처음부터 다시 읽는다. 원천이 auto-trading 하나로 줄어 지금은 116묶음이고, 새 소요(145초)로 환산하면 약 4.7시간이다.
- Remaining risks: 색인 축소는 **속도와 중복을 맞바꾸는 선택**이다. 속도만 보고 정하면 중복이 늘어난 것을 나중에야 안다.

## ✅ Outcome

- Result: **묶음당 중앙값 634.4초 → 145.0초 (-77%).** 원인은 프롬프트다 — 커밋 메시지를 묶음마다 다시 싣지 않게 하자(`_spread_messages`) 프롬프트 중앙값이 67,675 → 32,737자로 줄었고 소요가 그만큼 내려갔다. 전송량은 런 전체로 8.66M → 4.02M자였다.
- Result: **재시도가 사라졌다** — 새 방식 6호출에서 재시도 0 · 실패 0. `maxTokens=32768`(`11bb616`) 이 빈 응답을 없앴고, 프롬프트 축소가 남은 재시도를 없앴다. 둘 중 어느 하나만으로는 부족했다는 것이 옛 방식 arm 에서 드러난다 — 같은 `maxTokens` 로도 재시도 7 · 실패 1 이었다.
- Result: **색인 축소는 하지 않았다.** 분해를 다시 해 보니 주범이 색인(17%)이 아니라 커밋 메시지(54%)였다. 따라서 "중복 개념이 늘지 않았음을 보인다"는 두 번째 완료 조건은 **해당 없음**이다 — 갱신 인식을 건드리지 않았다.
- Verification: A/B 6쌍 12호출(2026-09-02, 워커 정지 상태, 짝마다 순서 반전). 새 방식 중앙값 145.0초 · 재시도 0 · 실패 0 · 제안 8. 옛 방식 중앙값 634.4초(실패 제외) · 재시도 7 · 실패 1 · 제안 5. **6쌍 전부에서 새 방식이 빨랐다.** 측정은 `IngestRun._apply` 를 부르지 않아 지식베이스에 쓰지 않았다.
- Follow-up: **메시지를 못 받는 66/116 묶음의 품질 영향은 여전히 미측정이다.** 묶음 4 에서 옛 방식만 제안 5개를 낸 것이 그 위험의 실물이다. 표본을 늘려 재는 것이 다음 후보이고, 되돌리려면 `_chunks` 의 `_spread_messages(...)` 호출만 뺀다.
- Follow-up: **`MAX_COMMIT_MESSAGES=50` 이 116묶음에 나뉘어 절반 이상이 빈손이다.** 한도를 올리면 분배가 촘촘해지지만 전송량이 다시 는다 — 값을 정하려면 위 품질 측정이 먼저다.
- Follow-up: 원자료는 `TASK-2026-09-01-main-003-ab-measure.json` 에 있다 (12호출 전건, 프롬프트 크기·재시도·실패 포함).
