---
id: TASK-2026-08-28-main-030
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-030
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.4.5
---

# TASK-2026-08-28-main-030 — WBS-4.4.5 대시보드 확장 (Q2 답변 검수)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/pipeline/draft_store.py`

- Description: Q2 답변 검수 대기열 — 판정 화면이므로 목록과 버튼이면 된다. 약한 지점과 에이전트 판정을 함께 보여 사람이 어디를 먼저 볼지 알려준다 (FR-45, §6.4.4, §5.6.5).
- Completion criteria: 판정 화면이 작업 화면과 다르다 — 상태 기계 없이 목록과 버튼이다 (FR-45)
- Completion criteria: 약한 지점과 근거 밖 낱말이 표시돼 어디를 먼저 볼지 알려준다
- Completion criteria: 반려 시 사유가 P 분류로 기록되고 분포에 잡힌다 (FR-22)
- Completion criteria: S2 부터 뜬다 (FR-59)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 16:28` 기준 Q2 답변 검수 대기열 — 판정 화면이므로 목록과 버튼이면 된다. 약한 지점과 에이전트 판정을 함께 보여 사람이 어디를 먼저 볼지 알려준다 (FR-45, §6.4.4, §5.6.5).
- Next session starting point:
- Remaining risks: 초안이 Q2 로 가는 배선이 아직 없다. 파이프라인과 draft_store 는 붙었지만 질문 유입에서 자동으로 도는 경로는 WBS-4.5(게재와 추적)의 몫이며, 지금은 스크립트로 태워야 한다
- Remaining risks: 승인해도 게재되지 않는다. S2 는 초안 단계이고 게재는 S3(FR-24·25)다 — 승인된 초안이 쌓이기만 하는 구간이 존재한다는 뜻이며, 그것이 단계 설계상 정상인지 확인이 필요하다
- Remaining risks: unmatched_terms 를 화면에 12개로 잘라 보여준다. 잘린 것이 있다고 말하지 않아 긴 초안에서는 볼 곳을 놓칠 수 있다

## ✅ Outcome

- Result: Q2 는 판정 화면이라 상태 기계가 없다 (FR-45, §6.4.4). 작업 화면(Q1·Q4)과 달리 진행 중·보류가 없고 보고 누르면 끝난다 — 시험이 화면에 그 단어들이 없는지 본다
- Result: answer_draft 와 review 를 나눴다. 이쪽은 판정받는 물건이고 저쪽은 판정 사건이다 — 한 초안이 에이전트 검수와 사람 검수를 차례로 받으면 사건은 둘, 물건은 하나다
- Result: 진술을 통째로 담는다. 본문만 남기면 근거 강도 표시가 사라지고 사람이 어디를 먼저 볼지 알 수 없어 화면을 눈으로 훑어 넘기게 된다 — 그 습관화를 깨는 것이 그 표시의 목적이었다 (§5.6.5)
- Result: **에이전트가 못 본 것을 사람은 본다.** 질문 원문을 초안에 담아 사람 검수자에게만 보인다 — FR-20 의 분리는 에이전트 검수가 자기 채점이 되는 것을 막는 규칙이고(§5.5.2), 사람은 무엇을 물었는지 모르면 근거에 충실하되 엉뚱한 답을 통과시킨다. 이 비대칭은 의도된 것이다
- Result: 에이전트가 반려한 것도 대기열에 올린다. 버리면 사람이 뒤집을 기회가 사라지고 자동 판정이 틀렸을 때 알 길이 없어진다 — 1국면 사람 판정 기록이 2국면 자동 검수의 기준이 된다는 §5.5.3 이 성립하려면 양쪽이 다 남아야 한다
- Result: **라이브 화면을 보고 지표 결함을 잡았다.** 검수 분포가 에이전트 판정과 사람 판정을 섞어 세고 있었다. §8.3 의 '검수 반려율은 신뢰 지표'는 사람이 에이전트를 얼마나 믿는가를 뜻하므로 사람 판정만 세야 하고, 2국면 학습 자료가 되는 것도 사람 판정 기록이다 — 주체별로 갈랐다
- Result: 근거 원문이 사라졌으면 화면이 그렇게 말한다. 조용히 빈칸으로 두면 검수자가 근거가 없는 줄 모르고 승인한다
- Verification: uv run pytest -q — 522 passed (기존 502 + 신규 20). 실제 서버로 S2 전 구간: 질문 2건이 파이프라인을 지나 초안·에이전트 검수를 거쳐 Q2 에 올랐고, 화면이 진술별 강도·근거 원문·모른다고 밝힌 부분·근거 밖 낱말을 함께 보여줬다. 사유 없는 반려는 거부되어 대기열에 남았고(1), 사유를 붙이자 P5 로 기록되어 분포에 잡혔다.
- Follow-up: WBS-4.5 S3 답변 게재와 추적 — 게재 · 전건 티켓 발행 · 답변 이력 · QnA 추적 · 게재 판정 · 승격 경로 B · stale 전파와 정정 · 지표. 여덟 leaf 로 이번 묶음이 가장 크다
