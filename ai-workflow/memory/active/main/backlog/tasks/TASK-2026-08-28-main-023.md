---
id: TASK-2026-08-28-main-023
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-023
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-004/WBS-4.3.3
---

# TASK-2026-08-28-main-023 — WBS-4.3.3 질문 직접 등록

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `src/agentic_service_desk/operations/manual_entry.py`

- Description: 담당자가 메신저로 받은 질문과 자기 답변을 두 칸에 붙여넣어 티켓으로 남긴다. 시스템이 종결 기록 초안을 만들고 사람은 무효화 조건만 채운다 (FR-10·11, ADR-007 결정 4, D43·D44).
- Completion criteria: 두 칸(질문·답변) 입력만으로 등록이 끝난다 — 등록 부담이 유인을 상쇄하지 않는다
- Completion criteria: 등록이 QnA 항목과 티켓을 만들고 수동 등록임이 남는다 (W4 의 간접 지표)
- Completion criteria: 종결 기록 초안이 자동 생성되고 무효화 조건 칸은 비어 있다 (FR-11)

## 🛠️ Implementation / Content

- Progress: `2026-08-28 15:10` 기준 담당자가 메신저로 받은 질문과 자기 답변을 두 칸에 붙여넣어 티켓으로 남긴다. 시스템이 종결 기록 초안을 만들고 사람은 무효화 조건만 채운다 (FR-10·11, ADR-007 결정 4, D43·D44).
- Next session starting point:
- Remaining risks: ADR-007 결정 4 의 'QnA 에도 게재' 체크박스를 만들지 못했다. 모 시스템 API 일곱 표면에 **질문을 만드는 쓰기 표면이 없다** (§9.8.1 은 답변 게재까지다). XR-7 정정 경로 부재를 요구사항 단계에서 발견한 것과 같은 종류의 빈틈이다. 다만 컨셉이 그 항목을 선택으로 둔 이유가 '지식화가 QnA 게재 여부와 무관하게 성립' 이므로 핵심 축은 온전하다 — 티켓만 있어도 지식은 자란다 (§1.4.3)
- Remaining risks: 등록한 사람이 무효화 조건을 채울 화면이 아직 없다. Q1 대기열 화면(WBS-4.3.5)의 몫이며, 그 전까지 초안은 만들어지되 티켓이 닫히지 않는다
- Remaining risks: 라이브 초안의 답이 담당자 답변보다 조금 더 일반화됐다(동일 등급 구성원 전체로 확장). 사람 검수가 잡을 종류이지만, 초안 품질이 나쁘면 결국 다 고치게 되어 부담이 되돌아온다는 ADR-007 귀결이 그대로 적용된다

## ✅ Outcome

- Result: 등록은 온라인에서 즉시 끝나고 초안은 배치가 채운다. LLM 호출은 수십 초라 등록 응답을 붙들면 부담이 되돌아와 §1.4.4 의 유인이 상쇄된다 — 온라인 저지연 / 배치 정확도라는 실행 경로 분리(§9.5)와 같은 선이다
- Result: qna_item.parent_question_id 를 NULL 허용으로 바꾸고 origin(parent|manual)을 더했다. 메신저 문의는 모 시스템을 거치지 않았고, UNIQUE 는 NULL 을 여럿 허용하므로 수동 등록끼리 부딪히지 않는다. origin 건수가 W4 의 유일한 간접 지표다 (§1.4.6)
- Result: 파서가 무효화 조건을 후보로만 읽는다. 모델이 지시를 어기고 invalidation 을 확정값으로 보내도 후보로 내려앉는다 — 형식을 어긴 쪽이 조용히 이기지 않게 하는 자리다. resolution.draft() 가 애초에 그 인자를 안 받는 것이 1차 방어이고 이것이 2차다
- Result: 근거를 못 뽑았을 때 지어내지 않고 남아 있는 사실 하나를 쓴다 — 담당자가 그렇게 답했다(person). 프롬프트도 '추측해서 code 라고 하지 않는다' 를 명시한다. 라이브에서 실제로 person 을 골랐고 code_traceable=False 가 됐다 — 그것이 무효화 조건이 유일한 stale 장치가 되는 경우다 (§6.5.3)
- Verification: uv run pytest -q — 371 passed (기존 346 + 신규 25). 실제 서버에 폼 제출 후 pi + MiniMax-M3 로 초안 생성: 일반화된 질문에서 개인 식별자(김OO·사번 12345·지난주·300만원)가 모두 걷혔고, 근거는 person 으로 코드에 닿지 않음을 정직하게 표시했으며, 무효화 조건은 None(후보 3개만), 승격가능=False.
- Follow-up: WBS-4.3.4 승격 경로 A — resolution.promotable 이 자격 판정이고 Ground 에서 Provenance 로의 변환이 필요하다. person 근거는 커밋이 없어 Provenance(qna=...) 로 가야 한다. 모순·Lint 티켓의 기록은 승격 대상이 아니므로 출처로 걸러야 한다
- Follow-up: WBS-4.3.5 Q1 대기열 화면 — ticket.queue() 와 resolution.awaiting_invalidation() 이 자료다. 무효화 조건을 고르는 자리(후보 목록 + 직접 입력)가 그 화면의 핵심이다
