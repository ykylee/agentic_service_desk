"""콘텐츠 — **묻지 않아도 먼저 내보내는 산출물** (§7).

이 패키지가 소유하는 것은 **타입 선언과 그 집행**이지 생성 절차가 아니다. 절차는
`pipeline` 에 있다 — 답변과 콘텐츠는 **트리거만 다른 같은 것**이라(D10, §7.1)
분석 → 조회 → 생성 → 검수 → 게재를 그대로 쓴다. 여기서 콘텐츠 전용 파이프라인을
다시 만들면 §7.1 이 없앤 중복이 되살아난다.

**그래서 새 타입을 더하는 일이 코드를 짜는 일이 아니다** (FR-42, §7.5). 다섯을
선언하면 5단계가 그대로 돈다 — 주 입력 · 트리거 · 성격 · 게재처 · 검수 강도.
그렇지 않으면 타입마다 별도 구현이 쌓여 유지가 불가능해진다.

지배 요구사항: FR-36~43 · FR-39(전수 사람 승인) · FR-42(선언 확장) · FR-43(1차 언어).
"""
