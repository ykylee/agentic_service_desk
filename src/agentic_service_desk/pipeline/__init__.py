"""산출 파이프라인 — **5단계** (D6).

    ① 분석 → ② 지식베이스 조회 → ③ 생성 → ④ 검수 → ⑤ 게재

이 시스템이 바깥으로 내보내는 **모든 것**이 이 순서를 거친다. 답변이든 콘텐츠든
같은 실행기를 쓰며 **트리거만 다르다** (D10) — 질문 유입이냐 주기·임계냐.

두 가지가 이 파이프라인의 성격을 정한다.

**실패할 자유가 있다.** 각 단계의 실패는 사람의 대기열로 수렴한다 — 답변은 티켓,
콘텐츠는 검수 대기. 답을 만들지 못할 자유가 없으면 4단계 검수는 형식이 된다.

**판단할 수 없는 것은 답하지 않는다** (D17). 근거가 있는 부분만 답하고 없는 부분은
모른다고 밝힌다. 추정으로 빈칸을 채우면 **근거가 붙어 있어 더 믿게 되므로**
오답 중 가장 해롭다. 다만 과잉 침묵도 실패다 — 기본값은 **부분 답변 + 경계 명시**다.
"""
