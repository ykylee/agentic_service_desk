"""지식베이스 — **파일 + git** (D12, ADR-002).

DB 가 아닌 이유는 취향이 아니라 §3 차용의 논리적 귀결이다. llm-wiki 의 운영 모델
(1 커밋 = 1 ingest, `log.md` append, additive merge)과 OKF 의 번들 형식
(디렉터리 = 번들, 1 파일 = 1 개념)이 **둘 다 파일 트리를 전제**한다.

두 가지를 지킨다.
    - **출처(provenance)는 1급 시민** (D3). 어느 커밋 / 어느 QnA 에서 왔는지 없이는
      stale 판정도 교차 검증도 근거 부착도 성립하지 않는다
    - **경로와 별개의 불변 id** (ADR-002). OKF 는 경로를 개념 ID 로 보지만, 입도
      조정으로 파일은 실제로 이동한다. 이동해도 답변 이력의 근거 링크가 살아남아야 한다

지식 항목은 **개념 단위** 다 (ADR-003). 파일이나 함수가 아니다. 굵게 시작하고
질문이 반복해 찌르는 지점만 쪼갠다 — 어디를 세분화할지는 질문이 알려준다.
"""
