"""로컬 LLM 게이트웨이 (ADR-005).

**단일 인스턴스를 공유하고 온라인이 배치에 우선한다.** 질문 절대량이 적어
에이전트마다 인스턴스를 두면 GPU 가 대부분의 시간을 논다.

`NFR-1` — 여기를 지나는 모든 호출은 **로컬 모델**이어야 한다. 지식 항목은 소스코드
파생이므로 외부 API 로 보내는 것은 곧 소스코드 반출이다. 임베딩도 같다.
"""
