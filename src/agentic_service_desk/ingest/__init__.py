"""수집과 지식 구축 — 배치 경로.

원천은 둘이다 (D2, D16).
    - **소스 저장소** — 코드 + 히스토리. 히스토리가 "왜 그렇게 정했는가"의 1차 출처다
    - **QnA 이력** — 무엇이 자주 묻히는가. 사용자 표현 사전이 여기서 나온다

**원천이 ingest 로 들어가는 문은 하나다** (NFR-4, §9.7). `output_filter` 가 그 문이며,
우회 경로를 두지 않는다 — 규칙을 여러 곳에 흩어 두면 언젠가 한 곳이 새고, 새는 순간
오답이 지식이 되어 굳는다.

설정값은 ingest 하지 않는다 (FR-9). **지식과 현재 상태는 다르다** — "승인 한도는
부서 등급으로 결정된다"는 지식이고 "지금 이 부서의 한도는 300만 원"은 상태다.
후자를 굳히면 굳는 순간 stale 이 된다.
"""
