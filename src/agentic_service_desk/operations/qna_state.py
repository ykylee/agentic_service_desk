"""QnA 항목의 상태와 해결 등급 (§6.1, §5.3.1).

**한곳에 모아 둔다.** 유입(`intake`)·게재(`publication`)·수동 등록(`manual_entry`)·
추적(`tracking`)·대시보드가 모두 같은 값을 쓰는데, 각자 문자열을 들고 있으면 어긋날
자리가 다섯 생긴다 — 어긋나면 상태가 하나 사라지는 것이 아니라 **그 상태의 화면이
영원히 비어 보인다.** 값이 한글인 것은 저장소가 그렇게 정의돼 있기 때문이다.

여기에는 **아무것도 import 하지 않는다.** 상수만 있는 모듈이라 누구든 안전하게
가져다 쓸 수 있어야 하고, 그것이 순환을 막는다.
"""

from __future__ import annotations

# --- 상태 (§6.1) -------------------------------------------------------------

RECEIVED = "접수"
"""질문이 들어와 추적이 시작됐다."""

PUBLISHED = "게재됨"
"""5단계를 통과해 답변이 올라갔다. **끝이 아니다** — 해결 / 후속 / 무응답으로 갈린다."""

FOLLOWUP = "후속진행"
"""추가 질문·재질문·이의가 달렸다. 파이프라인이 **다시 돈다** (D9)."""

AWAITING_HUMAN = "사람대기"
"""티켓으로 넘어가 사람 손을 기다린다."""

RESOLVED = "해결"
"""해결됐다. **등급이 함께 있어야 의미가 있다** — 명시적이냐 암묵적이냐가
ingest 자격을 가른다 (D8)."""

UNRESOLVED_CLOSED = "미해결종료"
"""끝내 해결되지 못한 채 닫혔다.

**버려지는 데이터가 아니라 지식 공백의 신호다** (§6.2) — 지식베이스가 답하지 못한
영역을 가리키므로 ingest 우선순위와 소스 보강 지점을 알려준다. Q8 이 이것을 본다.
"""

TERMINAL = frozenset({RESOLVED, UNRESOLVED_CLOSED})
"""더 추적하지 않는 상태. 여기 닿으면 타임아웃 판정 대상에서 빠진다."""


# --- 해결 등급 (D8, §5.3.1) ---------------------------------------------------

EXPLICIT = "explicit"
"""사람이 확인해 종결됐다 — 이용자의 해결 표시(모 시스템), 운영자의 확인 종결(우리).

검증 신호이므로 봇 답변의 ingest 배제가 **해제된다** (FR-31).
"""

IMPLICIT = "implicit"
"""사람의 확인 없이 해결로 간주됐다 — 무응답 타임아웃, 에이전트 자체 판단.

기록·통계·FAQ 후보에는 반영하되 **지식 원천으로 쓰지 않는다.** 무응답을 검증으로
세지 않는 것이 이 구분의 핵심이다 — **만족해서 조용한 것과 포기하고 떠난 것은
데이터상 같은 모양**이고, 둘을 구분하지 못하는 신호에 지식의 자격을 줄 수는 없다.
"""
